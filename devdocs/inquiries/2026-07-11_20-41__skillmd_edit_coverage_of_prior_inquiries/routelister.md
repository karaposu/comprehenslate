## User Input

`_branch.md` + Routelister step (8/8) of the `/traverse` for inquiry `2026-07-11_20-41__skillmd_edit_coverage_of_prior_inquiries`. Territory = this inquiry's own artifacts. Route the onward field, honoring the critique's FLAG→PROCEED (the evaluation survives; 3 REFINEs absorbed: 04-12 relabel / DF4 aspect-split / DF5 secondary-dependent / vs-pre-edit quarantined; the clean truth = the SKILL layer NUDGES, the engine CLOSES). Enumerate + type each route; do NOT execute; keep GATED. Write routelister.md + _route.md in root.

---

# Route-Map — onward field from the SKILL.md coverage evaluation

**Goal (surfaced explicit):** course-correct the comprehenslate SKILL toward **closing the chain's diagnosed gaps** — the config-blind Meaning-Lock 3-Pass wiring (00-24 crux) + the whole-draft verification back-bracket (01-09) — honoring the critique's structural truth that **the SKILL-`.md` layer can only NUDGE, and the failure class is CLOSED only at the ENGINE layer**. Enumerate, type, and guide each move; select none; keep all **gated** (evaluate-only until the user authorizes).

**Mode:** root / project-space (breadth) · **Entry point:** fresh · **Grain:** all `project-space` (omitted from the index).

## Map Header
- **Identities enumerated:** 7 · **Excluded:** 1
- **High-priority (HIGH):** 3 (R1, R3, R5) · **Essential-count (`core`):** 4 (R1, R2, R4 — three phase-deferred; R3 supporting)
- **kind split:** teleological 4 (R1, R2, R4, R6) · epistemic 3 (R3, R5, R7)

## Route Index

| # | Direction | engagement-type | Priority | Essentiality | ✓ |
|---|-----------|-----------------|----------|--------------|---|
| R1 | Reorder Step 5 to the config-blind Meaning-Lock 3-Pass spine | DEVELOP | HIGH | core · @authorized | ☐ |
| R2 | Add the whole-draft verification back-bracket | DEVELOP | MED | core · @after-R1 | ☐ |
| R3 | Confirm the chunk number (5000 vs ~3,500) with the user | DIAGNOSE | HIGH | supporting | ☐ |
| R4 | Wire engine-level enforcement — the real close of the failure class | INVESTIGATE-FRONTIER | MED | core · @engine-phase | ☐ |
| R5 | Recover + measure the pre-edit SKILL.md to lift the "better" quarantine | TEST | HIGH | supporting | ☐ |
| R6 | Fix the broken `notes.md` reference (line 34) | DEVELOP | LOW | peripheral | ☐ |
| R7 | Consolidate the within-prose enforcement sub-gradient into memory | CONSOLIDATE | LOW | peripheral · @3rd-instance | ☐ |

---

## Route Records

### R1 — Reorder Step 5 to the config-blind Meaning-Lock 3-Pass spine
- **Goal:** the 00-24 root-cause crux closed at the strongest the SKILL-layer can reach
- **engagement-type:** DEVELOP (teleological) — build the settled S1 design into the SKILL.md text
- **Move:** rewrite Step 5 so 5.1 is a **config-BLIND Meaning-Lock** ("render each sentence with strict semantic fidelity, WITHOUT the TranslationConfig in view"), then Harmony Map, then a Target-Reconstruction pass that applies the config + policies + case_catalog + harmony and concretes — wiring harmony_layer.md's Pass 1→2→3 as the Step-5 spine
- **Lands:** meaning is comprehended before style/config can contaminate it; the existing 4-layer "dont concrete" scaffold is preserved as a reorder (not a teardown) — the **strongest SKILL-layer nudge** toward meaning-first
- **Touches:** `SKILL.md` Step 5.1 (← flip from config-first to config-blind meaning-lock) · Step 5.4 (← config/harmony move into the reconstruction pass) · `harmony_layer.md` Pass 1-3 (← reused as the spine, not re-invented)
- **WHY:** the config-contamination mechanism 00-24 diagnosed is what makes meaning + style collapse in one motion; reordering to a config-blind meaning-lock is what lets the end goal (comprehenslation integrity) rest on an un-contaminated comprehension — **but only as a nudge**: per the enforcement gradient this raises adherence probability, it does NOT physically close the failure class (that is R4).
- **Priority:** HIGH   **Confidence:** HIGH (design settled by S1; gated only)   **Essentiality:** core · @authorized
- **Guidance Mode:** full
  - "cite always with the ceiling-caveat: strongest SKILL-layer nudge, NOT a close" (bc the critique's binding caveat — a prose reorder is one motion the model can still ignore)
  - "reorder, don't rewrite — the 4-layer scaffold is a stepping-stone" (bc S4: the deferral is already right; only the config-blindness + config-into-Pass-3 are missing)
  - "GATED — no SKILL edit without explicit user authorization" (bc the standing constraint)
  - Meaning-gaps:
    - exact prose wording of the config-blind instruction — mid — must forbid config-in-view at Pass 1 unambiguously, else it collapses back to config-first
- **Depth-link:** none

### R2 — Add the whole-draft verification back-bracket
- **Goal:** the second of 00-24's two gates (the post-draft check) present at the SKILL-ceiling
- **engagement-type:** DEVELOP (teleological) — build the settled S2 design into the SKILL.md text
- **Move:** add a final Pass-4 / Step-6 config-INDEPENDENT always-run verification: after the full draft, re-read the whole against the source — content dropped? invented? sentence-boundaries preserved? large-scale structure survived? — and fix any failure
- **Lands:** a post-draft re-read exists (distinct from the during-translation harmony preservation), closing 01-09 / 04-48-back-bracket / 23-03-checkpoint at the ceiling
- **Touches:** `SKILL.md` (← new final verification step) · the 01-09 config-independent always-run checks (← the content) · optionally the config-derived reader-keyed agenda (← lighter rim)
- **WHY:** the check gate is the second thing the un-run 3-Pass would have provided; adding it gives the end goal a catch-net for dropped/invented content — but it is **DF4-dependent**: per 01-09 the verification content is "inert until the [meaning-lock] fix is built," so its coreness activates only after R1.
- **Priority:** MED   **Confidence:** HIGH   **Essentiality:** core · @after-R1
- **Guidance Mode:** compact
  - "sequence after R1 — verification is inert until the meaning-lock is wired" (bc 01-09's own text)
  - "same nudge-ceiling caveat as R1" (bc a prose re-read is one motion; the enforced version is a separate verification call — R4)
- **Depth-link:** none

### R3 — Confirm the chunk number (5000 vs ~3,500) with the user
- **Goal:** the one open residual (FF2) resolved
- **engagement-type:** DIAGNOSE (epistemic) — investigate why the number is 5000 (deliberate vs oversight)
- **Move:** ask the user one question — is Opus ~5000 deliberate (a different working corpus / their judgment), or should it align to the ~3,500 conservative floor 06-37 recommended (which sits below the demonstrated-safe 4,345)?
- **Lands:** the number-divergence resolved as either a confirmed user calibration choice or a corrected value — no longer an open flag
- **Touches:** `SKILL.md` Rule 7 (← the ~5000 figure) · finding 06-37's ~3,500 band (← the recommended alternative)
- **WHY:** the finding holds the number as a user-choice-to-confirm, not a defect; asking is what lets the end goal settle the last open cell without the pipeline over-ruling a choice the user deliberately made.
- **Priority:** HIGH   **Confidence:** HIGH   **Essentiality:** supporting
- **Guidance Mode:** compact
  - "a question, not an edit — surface as FF2 in the finding" (bc the pipeline cannot resolve it without the user)
- **Depth-link:** none

### R4 — Wire engine-level enforcement — the real close of the failure class
- **Goal:** the failure class 00-24 diagnosed physically CLOSED (not merely nudged)
- **engagement-type:** INVESTIGATE-FRONTIER (teleological) — open the identified-but-unentered engine layer
- **Move:** build the separate-call pipeline — a config-blind meaning-lock call → a reconstruction call → per-chunk calls → a verification call — using the `PipelineConfig` fields already defined in schemas.py but unwired
- **Lands:** the meaning-lock, chunking, and verification are physically enforced (the model cannot collapse them in one motion or exceed a chunk), closing the failure class rather than raising its probability — **this is where finding 04-12 actually lands**
- **Touches:** `schemas.py` `PipelineConfig` (chunking_budget / chunking_granularity / parallel_mode — defined, unwired) · the translation engine/pipeline (← the real enforcer) · R1/R2's prose passes (← what the calls physically enforce)
- **WHY:** per the enforcement gradient a `.md` can only nudge; only separate calls physically bound what the model sees and do — so wiring the engine is what lets the end goal actually CLOSE the failure class the SKILL edits only raise the odds against. This is the standing structural frontier; its coreness activates in the engine phase.
- **Priority:** MED   **Confidence:** MED (a substantial build; scoped by S5)   **Essentiality:** core · @engine-phase
- **Guidance Mode:** compact
  - "this is the REAL fix; R1/R2 are the interim SKILL-ceiling nudges" (bc S5 — prose is capped)
  - "the PipelineConfig scaffolding already exists in schemas.py, unwired" (bc reuse, don't re-invent)
- **Depth-link:** none

### R5 — Recover + measure the pre-edit SKILL.md to lift the "better" quarantine
- **Goal:** the "vs pre-edit = better" claim validated on measured, not inferred, evidence
- **engagement-type:** TEST (epistemic) — validate the better-than-pre-edit claim against the actual old artifact
- **Move:** recover the pre-edit SKILL.md from git history and measure its Step 5 directly (was it truly the "flat apply-everything-at-once list" 00-24 described?)
- **Lands:** the mechanism-independence quarantine on "vs pre-edit = better" lifted (or the claim corrected) — the baseline anchored in a measured diff, not 00-24's description
- **Touches:** git history of `SKILL/SKILL.md` (← the pre-edit version) · the critique's P2 gap (← what this closes) · finding 00-24's description of the old Step 5 (← the currently-relied-on inference)
- **WHY:** the critique quarantined "vs pre-edit = better" because its baseline was inferred, not measured; recovering the old version is the cheapest move that lets the end goal rest that leg of the verdict on evidence — a checked diff instead of a remembered description.
- **Priority:** HIGH   **Confidence:** MED (assumes git retained the pre-edit state)   **Essentiality:** supporting
- **Guidance Mode:** compact
  - "cheapest honesty win — a `git show` on the old SKILL.md" (bc it is a read, no build)
- **Depth-link:** none

### R6 — Fix the broken `notes.md` reference (line 34)
- **Goal:** the SKILL's Step-2 file list points only at files that exist
- **engagement-type:** DEVELOP (teleological) — repair the dangling reference
- **Move:** drop or repoint `SKILL.md` line 34's `references/core/notes.md` (the file `case_catalog.md` was summarized from; now absent)
- **Lands:** the model no longer hits a missing file at Step 2's "read all reference files"
- **Touches:** `SKILL.md` line 34 (← the dangling `notes.md` reference)
- **WHY:** a broken read-target at Step 2 is a small but real snag on every run; fixing it removes friction from the end goal's execution — honestly peripheral hygiene.
- **Priority:** LOW   **Confidence:** HIGH   **Essentiality:** peripheral
- **Guidance Mode:** compact
  - "GATED, and confirm with the user whether notes.md was renamed or removed" (bc don't guess the intent)
- **Depth-link:** none

### R7 — Consolidate the within-prose enforcement sub-gradient into memory
- **Goal:** the S6 authoring lesson reusable across future SKILL-design work
- **engagement-type:** CONSOLIDATE (epistemic) — aggregate the sub-gradient into the design-discipline memory
- **Move:** record that the enforcement gradient has a WITHIN-prose tier (flat-list < ordered/named-passes, both nudges) — a prose SKILL should maximize the nudge via explicit ordered/named passes
- **Lands:** the enforcement gradient in [[project_skill_design_discipline]] extended with the within-prose sub-tier (which explains *why* the edited Step 5 is better-than-pre-edit despite both being prose)
- **Touches:** [[project_skill_design_discipline]] (← the enforcement-gradient memory) · this inquiry + the edit itself (← the 2 instances)
- **WHY:** the sub-gradient is a small reusable authoring rule; consolidating it (when it earns a 3rd instance) is what lets future SKILL-design work reach the end goal of maximum-nudge prose deliberately — deferred, peripheral.
- **Priority:** LOW   **Confidence:** LOW (2 instances; modest)   **Essentiality:** peripheral · @3rd-instance
- **Guidance Mode:** compact
  - "keep DEFERRED — revive on a genuine 3rd instance" (bc the critique affirmed S6-DEFERRED; no memory promotion yet)
- **Depth-link:** none

---

## Excluded (candidate-concepts considered and rejected, with reasons)

- **Author the CONCLUDE finding (frontmatter relationships + `## Inherited Commitments Re-test` + the 3 REFINEs + the honest headline).** Excluded — this is a **control-flow / process-control move** of the current traverse (conclude the line), not a concept-direction toward the goal (§1.3 control-flow exclusion; §4.3 Process-coupling). Carried to CONCLUDE as an invocation flag, not routed. *(For the record: the finding must absorb the critique's 3 REFINEs [04-12 relabel; DF4 aspect-split; DF5 secondary-dependent re-rank] + 2 caveats [S1/S2 ceiling-caveat; vs-pre-edit quarantined]; carry the `## Inherited Commitments Re-test` of the 6 priors; headline = BETTER-than-pre-edit / PARTIAL-vs-recommendations, the SKILL layer nudges / the engine closes; model `claude-opus-4-8[1m]`, effort max.)*

## Telemetry
- **Mode:** root / project-space (breadth) · **Entry point:** fresh
- **Identities enumerated:** 7 · **routes by kind:** teleological 4 (R1, R2, R4, R6) / epistemic 3 (R3, R5, R7)
- **High-priority:** 3 (R1, R3, R5) · **Essentiality:** core 4 (R1 @authorized, R2 @after-R1, R4 @engine-phase, [R3 supporting]) — 3 phase-deferred / supporting 2 (R3, R5) / peripheral 2 (R6, R7)
- **Individuations made:** 7 · **kept split (lean-to-split):** R1 (reorder) / R2 (verification) held separate (distinct fixes, distinct dependencies — R2 @after-R1); R4 (engine) held separate from R1/R2 (different layer — the real close vs the nudges); R5 (baseline-recovery) separate from the coverage verdicts (an epistemic validation, not a design)
- **Uncertain individuations:** 0 · **Stale entries:** none (fresh index)
- **Convergence:** reached — territory (the inquiry's artifacts + the critique's onward threads) fully swept; no new identities on re-sweep
- **Frontier flags:** R4 is itself flagged as the standing structural frontier (the engine-level real close)
- **Failure modes checked:** LAYER 1 — Over-merge (avoided: R1/R2/R4 split), Under-coverage (all critique threads routed), Wrong-grain (identities not manifestations), Goal-loss (goal surfaced explicit + biases every route), Type-misassignment (each verb membership-tested), Index-drift (fresh) — none. LAYER 2 — Selection-creep (attributive Priority/Essentiality only, no winner picked), Process-coupling (CONCLUDE-authoring EXCLUDED as control-flow), Description-collapse (routes are prescriptive), Manifestation-dump (one route per identity) — none.
- **Self-assessment: PROCEED** — territory swept at identity resolution; no LAYER 1 flags; both files written; the field is complete, gated, and none selected.
