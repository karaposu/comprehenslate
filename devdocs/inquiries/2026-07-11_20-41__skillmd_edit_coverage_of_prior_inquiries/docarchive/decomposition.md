## User Input

`_branch.md` + decompose the evaluate-the-edit inquiry grounded on the stabilized sensemaking model (coverage scorecard / DF4-crux / DF5-verification-gap / baseline-relative better-worse / held-nuances / reach-gate). Span the reach fork (deliver-audit → gap-list-as-course-correction → apply-gated). Flag CONCLUDE Inherited Commitments Re-test (6 priors). Note B is the load-bearing cell OF A; B+C+D presume the priors are correctly stated (I7). Save to this folder.

---

# Structural Decomposition — DV1

## Step 1 — Coupling Topology (coupling map)

Elements: the 6 per-finding scorecard cells · the DF4-crux verdict (the 00-24 cell's full content) · the DF5 verification-gap (the 01-09 cell + 23-03 checkpoint) · the baseline-relative better/worse · the held-nuances (number / prose / minor) · the gap-list / reach · the commitment-re-test · FF2 residual.

Coupling reading (change-propagation):
- **Cluster 1 — THE SCORECARD (the evaluation core):** the 6 per-finding cells, each a semi-independent coverage judgment sharing one method (measured-against-the-SKILL.md-text). Two cells are special: the **00-24 cell IS the DF4 crux** and the **01-09 cell IS the DF5 gap** — tight coupling (B and C are the "zoomed-in" full treatments of those two cells).
- **Cluster 2 — THE DF4 CRUX:** the 00-24 split (anti-collapse-deferral-landed / config-blind-Meaning-Lock-first-NOT-wired). Tightly coupled INTO the scorecard as its load-bearing cell, but substantive enough to be its own piece.
- **Cluster 3 — THE DF5 VERIFICATION GAP:** the missing post-draft back-bracket. Coupled into A (01-09 cell) + 23-03. Its own piece.
- **Cluster 4 — THE BASELINE-RELATIVE VERDICT:** aggregates the scorecard across 3 baselines. Downstream of A (a valley on its input side — consumes A through a clean interface).
- **Cluster 5 — THE HELD-NUANCES:** number/prose/minor "not-defects." Loosely coupled — they qualify specific cells (06-37, 04-12) + add minor items. A valley separates them.
- **Cluster 6 — THE REACH-GATE / gap-list:** the "what to do next" (course-correction options), gated. Downstream of the gaps (B+C define it).

Dominant structure: the scorecard (A) is the core; the crux (B) and the verification-gap (C) are its two load-bearing cells zoomed in; the baseline-verdict (D) aggregates it; the held-nuances (E) qualify it; the reach-gate (F) consumes the gaps.

## Step 2 — Boundaries (top-down)

Six pieces at the low-coupling valleys: **A** coverage-scorecard · **B** DF4-crux-verdict · **C** DF5-verification-gap · **D** baseline-relative-verdict · **E** held-nuances · **F** reach-gate. Valleys: between the scorecard-core and its aggregator (D); around the nuances (E, loosely attached); around the reach-gate (F, downstream of the gaps). B and C sit *inside* A's two load-bearing cells (a recursive zoom, not a boundary error).

## Step 3 — Boundaries (bottom-up validation)

Atoms: "did 04-48 land?" · "did 04-12 land?" · "did 06-37 land?" · "did 01-09 land?" · "did 00-24 land?" · "did 23-03 land?" (→ A) · "is config-first a regression or a shortfall?" · "is the anti-collapse deferral enough?" (→ B) · "is there a post-draft verification pass?" (→ C) · "better or worse vs pre-edit / recommendations / ideal?" (→ D) · "is the number a defect or a choice?" · "is prose a miss?" · "broken ref?" (→ E) · "how far should the output reach / what's the gap-list?" (→ F).

Grouping: the 6 finding-atoms → A. The config-first / deferral atoms → B. The verification atom → C. The better/worse atoms → D. The number/prose/ref atoms → E. The reach atom → F. **Top-down and bottom-up AGREE** → high confidence. B and C are validated as *zoom-ins* of A's 00-24 and 01-09 cells (A summarizes; B/C give the full treatment) — a recursive relationship, not a split-atom error.

## Step 4 — Question Tree

**A — The coverage scorecard.** *Does the edit cover each of the 6 priors' recommendations (covered / partial / missing)?*
- [ ] each of the 6 cells carries a verdict grounded in the MEASURED SKILL.md text (not memory): 04-48 COVERED(mechanism) · 04-12 COVERED-at-ceiling · 06-37 PARTIAL · 01-09 MISSING · 00-24 PARTIAL · 23-03 PARTIAL
- [ ] the two load-bearing cells (00-24, 01-09) defer to B and C for their full treatment

**B — The DF4 crux verdict.** *Is the 00-24 root-cause fix landed?*
- [ ] states the split: anti-collapse deferral (the 4-layer "dont concrete until end") LANDED (partial) / config-blind Meaning-Lock-as-FIRST-pass NOT wired
- [ ] evidence: Step 5.1 comprehends "with most loyalty to config" (config-led, not config-blind); the 3-Pass is referenced at 5.4 but as the LAST layer (ordering inverted); one-motion prose (weak nudge per 04-12) ≠ a real separate pass
- [ ] verdict: PARTIAL — net-BETTER vs pre-edit, but the load-bearing shortfall; the failure class 00-24 diagnosed (meaning+style in one motion → dropped clauses / wrong word-sense) is NOT yet closed

**C — The DF5 verification gap.** *Is there a post-draft verification pass?*
- [ ] confirms ABSENT — preservation-during (Rule 5 / harmony Tier policy) ≠ verification-after (a re-read of the finished whole draft)
- [ ] it is the 2nd of 00-24's two gates; 01-09 (always-run checks) + 04-48 (back-bracket) + 23-03 (checkpoint) all converge on it

**D — The baseline-relative better/worse verdict.** *Better or worse — than WHAT?*
- [ ] vs pre-edit SKILL.md = BETTER (chunking added, one-motion softened, harmony + case_catalog referenced; nothing lost)
- [ ] vs the inquiry recommendations = PARTIAL (chunking half landed; 3-Pass-wiring + verification half did not)
- [ ] vs an ideal = the gap-list
- [ ] names the single most-useful sentence for the user

**E — The held-nuances (not defects).** *What looks like a gap but isn't?*
- [ ] number (5000 vs ~3,500) = user-choice-to-confirm (less-conservative-than-recommended; 06-37 quarantined the number; user set it) — NOT a defect
- [ ] prose-chunking = the honest ceiling of a `.md` (04-12 anticipated the engine) — NOT a miss
- [ ] minor: broken `notes.md` reference (line 34); harmony_layer.md method+policy still un-separated (00-24's structural precondition)

**F — The reach-gate.** *How far should the output reach?*
- [ ] enumerates the gap-list as course-correction OPTIONS: wire config-blind-Meaning-Lock-first 3-Pass; add the verification back-bracket; confirm the number; (later) engine-enforce chunking
- [ ] gated on the user's decision + explicit authorization (no SKILL edit without it); FF2 (confirm 5000 was deliberate) is a user-dependent confirm, not an analytic gap

## Step 5 — Interface Map

| # | Source → Target | What flows | Direction |
|---|---|---|---|
| I1 | A → D | the 6 per-cell verdicts → aggregated into the baseline-relative better/worse | one-way |
| I2 | B → A | the crux IS A's 00-24 cell — B provides the full split A summarizes | one-way (zoom) |
| I3 | C → A | the verification-gap IS A's 01-09 cell (+ 23-03 checkpoint) | one-way (zoom) |
| I4 | B + C → F | the two real gaps → the gap-list / course-correction options | one-way |
| I5 | E → A, D | the held-nuances qualify A's 06-37/04-12 cells + soften D's verdict (number/prose ≠ defects) | one-way |
| I6 | A,B,C,D,E → F | all verdicts + nuances → the reach decision is over the whole | one-way |
| **I7** | **the 6 priors' recommendations → A,B,C,D** | **LOAD-BEARING ASSUMPTION:** the coverage verdicts PRESUME each prior's recommendation is correctly stated (inherited from the Synthesis-Trigger priors, surfaced verbatim in surfacing). If a prior were mis-stated, its coverage cell changes. | inherited prereq |

**Assumptions-not-data check (Step 5 refinement):** I7 is the hidden-coupling risk — A/B/C/D assume the priors say what surfacing measured them to say (00-24 = config-blind-meaning-lock-first; 01-09 = post-draft always-run checks; 06-37 = ~3,500 floor; etc.). Surfaced explicitly (surfacing quoted the finding summaries verbatim). No other hidden assumption (the SKILL.md text is measured, not assumed).

## Step 6 — Dependency Order

- **Start (parallel):** B (crux) ‖ C (verification-gap) ‖ [A's other 4 cells: 04-48 / 04-12 / 06-37 / 23-03] — B and C feed A's two load-bearing cells; the other 4 are independent judgments.
- **Then:** A (assemble the scorecard) — after B + C provide the 00-24 and 01-09 cells (I2/I3).
- **Then (parallel):** D (baseline verdict) ‖ E (held-nuances) — D after A (I1); E qualifies A + D (I5).
- **Last:** F (reach-gate) — after all (I6); the gap-list is defined by B + C (I4).
- No cycles. I7 is an inherited prereq (the 6 priors), not an internal cycle.

Order: **(B ‖ C ‖ A-other-cells) → A → (D ‖ E) → F.**

## Step 7 — Self-Evaluation (7 dimensions)

| Dimension | Verdict | Note |
|---|---|---|
| **Independence** | PASS | Each piece answerable via its interface without reading siblings' internals (A: score-each-prior; B: is-00-24-landed; C: is-there-verification; D: better-worse; E: what's-a-non-defect; F: how-far). |
| **Completeness** | PASS | Scorecard (A) + crux (B) + gap (C) + verdict (D) + nuances (E) + reach (F) cover the whole stabilized model. Nothing falls through. |
| **Reassembly** | PASS | A+B+C+D+E+F reconstruct the finding: "better-than-pre-edit, partial-vs-recommendations; scorecard; crux = config-blind-meaning-lock-NOT-wired; verification MISSING; number/prose = non-defects; gap-list gated." |
| **Tractability** | PASS | Each piece is a single focused pass. |
| **Interface clarity** | PASS | 7 interfaces explicit incl. the load-bearing I7 (the priors-correctly-stated assumption). |
| **Balance** | PASS | A (scorecard) and B (the crux) are the two substantive halves; C/D/E/F lighter but genuinely distinct; no single piece is 80%. |
| **Confidence** | PASS (HIGH) | Top-down and bottom-up agreed on all six boundaries; B/C validated as zoom-ins of A, not split-atoms. |

**Determination-mechanism check:** the soundness questions — does it cover (A), is the crux real (B), is there a verification pass (C), is it better or worse (D) — are ANSWERED in A+B+C+D, not presupposed. The one runtime-USER-determination (was 5000 deliberate — FF2) is correctly placed in **F (reach)** as a confirm-with-user item, NOT left as a missing analytic piece. PASS — no missing determination piece.

## Relationships (for CONCLUDE)

- **Synthesis Trigger** consumes/re-tests the **6 priors** (06-37 / 04-48 / 04-12 / 01-09 / 00-24 / 23-03) → **CONCLUDE needs an `## Inherited Commitments Re-test`** naming each prior's commitment and re-testing whether the SKILL.md edit satisfies it (with cited SKILL.md evidence) or flagging it. The honest headline (BETTER-than-pre-edit / PARTIAL-vs-recommendations; the load-bearing shortfall = config-blind-Meaning-Lock-first NOT wired; verification MISSING; number/prose = non-defects; the reach gated) must reach the finding.
- **RELATED** (yardsticks): the 6 prior inquiry folders.
