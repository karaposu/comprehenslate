## User Input

Design-realization + reframe pass for the OPEN RESIDUAL (piece F — the reach-gate / gap-list as course-correction options). The evaluative core (A scorecard / B crux / C gap / D verdict / E nuances) is SETTLED by sensemaking — do NOT re-derive. Must address: F1 (HOW to wire config-blind Meaning-Lock-first in the current SKILL.md prose), F2 (HOW to add the whole-draft verification back-bracket in prose), F3 (the 5000-vs-3500 number-confirm as a user question), + the reframes (is the 4-layer "dont concrete" a stepping-stone or dead-end; is prose fundamentally capped by the enforcement gradient — contrarian; does this re-confirm the enforcement-gradient + two-gate model or surface something new). All GATED (evaluate-only; no SKILL edit without authorization). Full settled-ground in the invocation + `_branch.md`.

---

# Structural Innovation — Output

## Seed + Methodology-Mode Consideration

**Seed (design-realization + reframe):** realize the two course-correction fixes (config-blind Meaning-Lock reorder + verification back-bracket) that would close the DF4 crux + DF5 gap at the SKILL-ceiling, and surface the contrarian reframe (is prose fundamentally capped).

- **Inherited mode:** Standard default (elaborate the committed direction — the evaluative core is settled; produce ship-ready course-correction designs + reframes). Text signals: "generate ideas primarily for the OPEN RESIDUAL... course-correction designs + reframes."
- **Alternative named:** Contrarian-rethink (Framer-weighted) — challenge the whole "fixable-in-prose" premise run-wide.
- **What follows under the alternative:** the contrarian probe ("prose is capped; only the engine closes the failure class") IS explicitly requested as one reframe — so the contrarian LENS is honored LOCALLY at S5, not run-wide (F1/F2 are standard-default design elaboration).
- **Decision:** **Standard default**, contrarian honored locally at S5. `Methodology-mode-alternative-marked-inapplicable (run-wide): the evaluative core is settled by sensemaking; F1/F2 are ship-ready realizations, and the contrarian is scoped to the S5 reframe by the seed itself.`

## Mechanism Generation

### F1 — wire the config-blind Meaning-Lock-first → **S1**
- **Absence Recognition (patch):** SKILL.md Step 5 has NO config-blind meaning-lock step; 5.1 comprehends "with most loyalty to config." The redesign-level absence: if Step 5 were designed from scratch to 00-24's spec, its SPINE would BE harmony_layer.md's Pass 1 → Pass 2 → Pass 3, with config applied *inside* Pass 3 — not a config-first mapping.
- **Domain Transfer (compiler two-phase):** a compiler PARSES (builds the semantic tree, source-faithful) then does CODEGEN (target-specific rendering). Import: separate the **semantic parse (config-blind Meaning-Lock)** from the **target reconstruction (config-applied)**. Config is a codegen-time concern, not a parse-time one.
- **Constraint Manipulation (ADD):** "the meaning-lock pass MUST NOT see the TranslationConfig" → forces the reorder. **(REMOVE):** strip "and most loyalty to given TranslationConfig selections" from 5.1.
- **The minimal prose rewrite (design — a REPAIR/reorder of the existing Step 5, gated):**
  > **Step 5:** (Pass 1 — Meaning Lock) *First, render each sentence with strict semantic fidelity — WITHOUT the TranslationConfig in view — producing the accurate-but-choppy version. Do not concrete.* → (Pass 2 — Harmony Map) *analyze the source's inter-sentence relationships per harmony_layer.md.* → (Pass 3 — Target Reconstruction) *NOW apply the TranslationConfig (reader_level, fidelity, etc.), the policies, the case_catalog cases, and the harmony blueprint — reconstruct in the target language; you may concrete now.*
  This flips config from FIRST → WITHIN-Pass-3, makes Meaning-Lock the FIRST executed pass, and wires harmony_layer.md's 3-Pass as the Step-5 SPINE (closing the 00-24 crux at the SKILL-ceiling). The existing 4-layer "dont concrete until end" scaffolding is *preserved* — this is a reorder, not a rewrite.
- **Enforcement caveat (04-12):** even reordered, this is ONE-run prose = a weak nudge the model can ignore; it RAISES the adherence probability but does not physically close the failure class. The strong-end fix is the engine (a separate config-blind meaning-lock CALL whose output feeds a separate reconstruction call — see S5).
- **Disposition: ACTIONABLE-gated.** Cheap, high-value, directly closes the DF4 crux at the SKILL-ceiling; gated on the user's authorization.

### F2 — add the whole-draft VERIFICATION back-bracket → **S2**
- **Absence Recognition (patch):** no post-draft check exists. Add a **Pass 4 / Step 6 (config-independent always-run verification):**
  > *After the full draft is assembled, re-read the whole translation against the source and run the always-run checks: (1) was any source content dropped? (2) was anything invented/added? (3) were sentence boundaries preserved? (4) did the large-scale structure survive? Fix any failure before delivering.*
  This is 01-09's config-independent always-run spine + 04-48's back-bracket + 23-03's missing checkpoint — the SECOND of 00-24's two gates.
- **Combination:** pair the always-run spine (config-INDEPENDENT) with the config-DERIVED reader-keyed agenda from 01-09 (is this word too hard *for this reader*? register right?) — but the always-run spine is the load-bearing core; the config-derived agenda is the lighter rim.
- **Disposition: ACTIONABLE-gated.** Cheap prose add (a new final step); closes the second gate at the SKILL-ceiling; gated.

### F3 — the number-confirm → **S3** (a user question, not a design)
- **Absence Recognition:** the 5000-vs-3,500 divergence is a *decision*, not a design gap. Frame as ONE user question:
  > *"Is Opus ~5000 deliberate — a different working corpus, or your own judgment — or should it align to the ~3,500 conservative floor 06-37 recommended (which sits below the single demonstrated-safe source size of 4,345)?"*
- **Disposition: ACTIONABLE-small.** A clarifying question to surface in the finding (FF2); resolves the one residual OPEN. Not gated (it's a question, not an edit).

### THE REFRAMES

**Reframe A — is the 4-layer "dont concrete" a STEPPING-STONE or a DEAD-END? → S4**
- **Inversion:** what if the 4-layer ordering must be REPLACED, not repaired?
- **Reasoning:** the 4-layer already DEFERS finalization (the hard, load-bearing part of anti-collapse). The ONLY things missing for the real 3-Pass are (a) make 5.1 config-BLIND and (b) move config into Pass 3 — both small edits *on top of* the existing scaffold. So the 4-layer is a **STEPPING-STONE**: the scaffolding is right; it needs a reorder, not a teardown.
- **Disposition: ACTIONABLE.** This resolves S1's intervention-shape as **REPAIR (reorder)**, not REPLACE — the cheaper, correct move.

**Reframe B (contrarian, system-level) — is the whole in-SKILL.md-prose approach CAPPED by the enforcement gradient? → S5**
- **Inversion (L1):** what if NO prose edit can *close* the failure class — only the engine can?
- **Reasoning:** per 04-12/04-48/06-37, a one-run prose instruction is a weak nudge the model can quietly ignore. The 3-Pass in prose — even perfectly reordered (S1) — is STILL one model motion; the model can still collapse meaning + style together. The STRONG-end fix is engine-level: a **separate config-blind meaning-lock call** → a **separate reconstruction call** → **separate chunk calls** → a **separate verification call** (the PipelineConfig / separate-call layer that already exists in schemas.py but is unwired).
- **Inversion (L2, system-level):** SKILL.md is the WRONG LAYER to fully fix this — the enforcement belongs in the engine/pipeline; SKILL.md's role is the *nudge*, the engine's role is the *gate*. This RE-CONFIRMS 04-12's enforcement gradient and 00-24's two-gate model.
- **Honest balance (not nihilism):** prose nudges are NOT worthless — an ordered/named-pass instruction meaningfully raises adherence over a flat "apply everything" list (that is *exactly why* the edit is "better than pre-edit"). So S1/S2 are worth doing as the **interim SKILL-ceiling fix**, while the engine is the **real close**.
- **Disposition: RE-TEST TRIGGER / RESEARCH-FRONTIER.** The deepest yield: the SKILL.md-prose approach is CAPPED; the failure class is only truly closed engine-level (separate meaning-lock + reconstruction + verification + chunk calls). It re-tests whether F1/F2 are "enough" (they are the *ceiling of the SKILL layer*, not a close). Held: adopt S1/S2 as the interim fix; the engine is the standing frontier.

**Reframe C — the standing principle → S6**
- **Absence Recognition (concrete authoring lesson):** the enforcement gradient ([[project_skill_design_discipline]]) is usually stated as prose-instruction < emitted-artifact < separate-call. This inquiry surfaces a **gradient WITHIN prose**: a *flat "apply everything at once" list* < an *ordered/numbered/named-pass* instruction. Both are nudges, but the ordered-named-pass form raises adherence probability — which is precisely why the edited Step 5 ("dont concrete until end," 4 ordered layers) is better than the old flat list *even though both are one-run prose*.
- **Authoring implication:** a prose SKILL should MAXIMIZE the nudge — use explicit ordered/named passes (Pass 1 config-blind → … → Pass 3), not a flat bag — because structure raises adherence even at the weak end.
- **Disposition: DEFERRED (small authoring lesson).** Extends the enforcement gradient with a within-prose sub-gradient; modest (this + the edit itself as instances); links [[project_skill_design_discipline]] and [[feedback_translation_verification_pass]].

## Inherited Frame Audit
- **Seed central assumption:** "the gaps are fixable in the current SKILL.md prose form." **Challenged?** YES — S5 (contrarian) directly challenges it (prose is capped; only the engine closes the failure class). → **audit does NOT fire.**
- **Piece-level (property v — intervention-shape):** S1 commits REPAIR (reorder Step 5); alternative shapes tested = DO-NOTHING (crux persists) and REPLACE (overkill — the 4-layer scaffold is right per S4) → REPAIR justified. S2 commits ADD-CONTENT (Step 6); alternative = DO-NOTHING (second gate stays open) → ADD-CONTENT justified. Both satisfied.

## Assembly Check + Convergence

**Convergence (4+ mechanisms):** Absence-Recognition (no meaning-lock step / no verification) + Domain-Transfer (compiler parse/codegen split) + Constraint-Manipulation (meaning-lock-must-not-see-config) + Inversion (stepping-stone-not-dead-end; prose-is-capped) all converge on: **the SKILL-ceiling fix is a cheap Step-5 REORDER (5.1 → config-blind Meaning-Lock; config into Pass 3) + a Step-6 verification add — both gated, both nudges — with the engine (separate calls) as the real close.** High confidence.

**Emergent architecture (assembly):** the pieces compose into a **two-tier course-correction**: TIER 1 (SKILL-ceiling, cheap, gated) = reorder Step 5 to the 3-Pass spine (S1) + add the verification back-bracket (S2) + confirm the number (S3) → raises adherence probability and closes both gates *as nudges*; TIER 2 (engine, structural, standing-frontier) = separate meaning-lock / reconstruction / chunk / verification calls (S5) → physically closes the failure class. Tier 1 is the interim; Tier 2 is the real fix. The within-prose sub-gradient (S6) explains why Tier 1 is still worth doing.

## Telemetry
- Generators applied: 3/4 (Absence, Domain-Transfer, Combination; Extrapolation light in S5's engine-future reasoning).
- Framers applied: 3/3 (Inversion [S4 stepping-stone; S5 L1/L2 depth], Constraint-Manipulation [ADD+REMOVE], Lens-Shifting [S5 SKILL-layer-vs-engine-layer]).
- Convergence: **YES** — 4 mechanisms converge on the reorder+verification SKILL-ceiling fix with the engine as the real close.
- Survivors tested: 6/6 (S1-S6 via the 5-test cycle: novel / survives-scrutiny / fertile / actionable / mechanism-independent).
- Per-piece meta-decision log: S1 = property-v (REPAIR) → intervention-shape Inversion **satisfied** (REPAIR vs DO-NOTHING/REPLACE). S2 = property-v (ADD-CONTENT) → intervention-shape Inversion **satisfied** (ADD-CONTENT vs DO-NOTHING). S3/S4/S5/S6 = content-production / reframe.
- Failure modes observed: none (generation preceded testing; ≥1G+1F; not early-frame-locked — S5 contrarian surfaced after S1-S4; uncomfortable output [prose-is-capped] generated + tested).
- **Overall: PROCEED** — full coverage, convergence, all survivors tested, no failure modes; the S5 prose-is-capped RE-TEST-TRIGGER is the deepest yield and reaches CONCLUDE as the "engine is the real close" frontier; all design pieces stay GATED (evaluate-only).
