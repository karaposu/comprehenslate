## User Input

Decompose the evaluate-plus-design inquiry `2026-07-11_04-48__mandatory_chunking_char_limit_adherence` against the sensemaking-stabilized model (pivot resolved: adherence-under-load real & distinct, variable = transformation working-set; flip-on = budget-gated-always-on + boundary-snap + per-model + phase-split; whole-span = coexist-by-bracketing + 3-Pass entanglement + harmony-floor; rivals: redundancy-on-the-how / over-engineering-defused / deep-rival-two-levers; relationships: complementary-entangled 00-24, sibling 04-12, interacts 01-09, extends 06-14). Partition into independently-coherent pieces with interfaces + dependency order, spanning the reach fork; flag the CONCLUDE Synthesis-Trigger (3+ priors → Inherited Commitments Re-test). Full input in `_branch.md` + sensemaking.md + surfacing.md + articulate_warm.md.

---

# Structural Decomposition — Output (DV1)

## Step 1 — Coupling Topology

Elements in the whole (from the stabilized model):
- **e1** the pivot verdict (adherence real & distinct; load-magnitude axis; variable = transformation working-set; ~3500 approximate + model-dependent).
- **e2** the rival outcomes (redundancy lands on the "how" only; over-engineering defused by the budget-gate; deep-rival → two sibling load levers).
- **e3** the honest scoping ("how to chunk" is already designed → reuse; the new part is rationale + scale + flip).
- **e4** the flip-on policy shape (budget-gated-always-on, boundary-snapping, per-model, phase-split instruction-now/engine-later).
- **e5** the whole-span bracketing architecture (3-layer sequencing; 3-Pass entanglement; harmony floor on the budget).
- **e6** the prior reconciliation (complementary-entangled 00-24 / sibling 04-12 / interacts 01-09 / extends 06-14) + inherited-commitment re-test.
- **e7** the reach gate (evaluate-only / design / apply), phase-aware.

Coupling map (change-propagation):
- **e1 ↔ e2 ↔ e3** — STRONG. They are one judgment: the pivot verdict determines the rival outcomes and the honest scoping. Change the pivot → the rivals and scoping change. → cluster **A**.
- **e4 ↔ e5** — MODERATE, via the budget. e5's harmony floor and e4's adherence ceiling negotiate the same number (the budget); otherwise e4 (policy shape) and e5 (pipeline architecture) are separable concerns. → cluster **C**, split internally into C1 (policy) + C2 (architecture) — the evaluate-vs-design late-split honored INSIDE C.
- **e6** — MODERATE coupling to A (the verdict sets the relationships) but is its own coherent synthesis job (4 relationships + the re-test) → piece **B**.
- **e7** — WEAK coupling to all (it is a control-flow disposition atop the content) → piece **D**.
- **A → C** — MODERATE: the design (C) is only worth doing if the evaluation (A) says the proposal is sound (it does). C consumes A's verdict.
- **A → B** — MODERATE: B's relationships are read off A's verdict (complementary-not-rival rests on the pivot's distinctness).

Valleys (low coupling → boundaries): between A (evaluation) and C (design); between B (chain-reconciliation) and C (design); between D (reach) and everything.

## Step 2 — Boundaries (Top-Down)

Four top-level pieces: **A** (evaluation-core) · **B** (prior-reconciliation + commitment re-test) · **C** (fix-design, two sub-designs C1/C2) · **D** (reach gate). The dominant valley is the **evaluate | design** boundary (A/B on the evaluation side, C on the design side, D atop) — the same OUTPUT-REACH valley the chain's priors cut on.

## Step 3 — Validate Boundaries (Bottom-Up)

Irreducible atoms and their clustering:
- "is adherence-decay real?" · "is it distinct from the 3-Pass?" · "what's the variable?" · "does redundancy/over-engineering/deep-rival land?" · "what's genuinely new?" → all group into **A**. ✓
- "budget-gated or always-split?" · "where does the budget come from / per-model?" · "instruction-now or engine-later?" → **C1**. ✓
- "how do the whole-text obligations coexist with chunking?" · "what's the sequencing?" · "what bounds the budget below?" → **C2**. ✓
- "complementary or rival to 00-24?" · "sibling to 04-12?" · "does 01-09's whole-draft check survive chunking?" · "extends 06-14?" · "do the inherited commitments hold?" → **B**. ✓
- "evaluate-only / design / apply?" · "instruction-level now vs engine-level later?" → **D**. ✓

Top-down and bottom-up AGREE on all four boundaries → HIGH confidence. The only atom that could float is the **harmony-floor** (it belongs to both C1's budget and C2's architecture) — assigned to C2 (it is derived from the whole-span structures) and consumed by C1 as an interface (the floor constrains the budget). No atom is split across a boundary; no independent atoms are wrongly grouped.

## Step 4 — Question Tree

### Piece A — Evaluation-Core
**Q-A:** Is mandatory-chunking-for-adherence sound, and what exactly is new (vs the existing chunking design)?
- [ ] the pivot verdict stated: adherence-decay-under-load is REAL and DISTINCT from the un-wired-3-Pass (load-magnitude vs method-structure axis; complementary)
- [ ] the true variable named: the per-unit transformation working-set (source-unit + output + local tracking); source-char-count an approximate proxy; the fixed instruction load NOT reduced by source-chunking
- [ ] ~3500 characterized: real-in-kind, approximate + model-dependent in value (unvalidated — no evidence)
- [ ] rival outcomes: redundancy lands on the "how" only (reuse the 06-14 design); over-engineering defused by the budget-gate; deep-rival partially lands → chunking + staging = two sibling load levers
- [ ] the honest headline: the "how to chunk" is already designed; the genuinely-new yield is the RATIONALE (adherence) + the SCALE (~200× smaller budget) + the FLIP (opt-in→mandatory-policy)

### Piece B — Prior-Reconciliation + Commitment Re-test
**Q-B:** How does this sit in the inquiry chain, and do the inherited commitments survive?
- [ ] COMPLEMENTARY-AND-ENTANGLED with `2026-07-11_00-24` (un-wired 3-Pass): not a rival cause; Pass 2 (whole-text Harmony Map) is the bracket that makes safe chunking possible → the 3-Pass root-cause claim SURVIVES and gains an entanglement
- [ ] SIBLING to `2026-07-11_04-12` (staging): two levers under a bounded-load principle (chunking = transformation-load; staging = fixed-instruction/rule-count load); complete answer = both
- [ ] INTERACTS-WITH `2026-07-11_01-09` (whole-draft checks): the check runs on the ASSEMBLED output (post-chunking) so it still has the whole → the tiered-reliability + mechanical-spine SURVIVE; chunking is the generation layer, the check the post-assembly bracket
- [ ] EXTENDS `2026-06-14_00-50` + `2026-06-14_17-04` (chunking design): the HOW survives unchanged (reuse); the adherence rationale ADDS a 6th driver, re-scales the budget (capacity→adherence + harmony floor), flips the default (opt-in→mandatory-policy); Tier-1-preservation-HARD becomes the harmony floor
- [ ] the 4 inherited-commitment clusters re-tested with cited evidence (for CONCLUDE's Inherited Commitments Re-test)

### Piece C — Fix-Design (two coupled sub-designs; evaluate-vs-design late-split honored INSIDE C)
**C1 — Flip-on policy.**
**Q-C1:** IF we act, what is the "mandatory" policy shape?
- [ ] budget-gated-always-on (chunk only above budget; short texts pass through) — NOT always-split
- [ ] harmony-boundary-snapping (reuse `harmony-tier-aware`/`fixed-budget-with-snap`) so a split never cuts a Tier-1 chain
- [ ] per-model budget (the "lower for smaller models" instinct → `chunking_budget:int` per-model); budget = empirical/unvalidated (deferred calibration)
- [ ] phase-split: a model-facing SKILL.md INSTRUCTION now (engine unbuilt); the PipelineConfig policy later
- [ ] consumes the harmony floor from C2 (the budget cannot go below the Tier-1 span size)

**C2 — Whole-span bracketing architecture.**
**Q-C2:** IF we act, how does chunking coexist with the whole-text obligations?
- [ ] 3-layer sequencing: whole-text comprehension + Pass-2 Harmony Map (BEFORE) → chunked GENERATION (middle) → whole-draft structural check 01-09 (AFTER)
- [ ] 3-Pass entanglement: Pass 2 is the whole-text bracket; chunking is entangled-with (not independent-of) the un-wired 3-Pass
- [ ] the harmony floor: a Tier-1 structure whose span exceeds the adherence-safe chunk size sets a FLOOR on the budget (provides this floor to C1)
- [ ] the residual honest: adherence ceiling vs harmony floor negotiate the budget; very-long-span-structure + very-small-budget is a genuine bounded trade

### Piece D — Reach Gate
**Q-D:** How far does this inquiry travel, and at which phase-layer?
- [ ] per-proposal reach: evaluate-only [delivered by A+B] / +design [C1+C2] / +apply
- [ ] phase-aware: apply-as-instruction (SKILL.md, available now) vs apply-as-engine-policy (gated on the chunking engine being built)
- [ ] gated on user authorization; no SKILL edits without it

## Step 5 — Interface Map

| # | Source → Target | What flows | Direction |
|---|---|---|---|
| I1 | A → C | the pivot verdict "the proposal is sound + what's new" (design is worth doing only because A affirms it) | one-way |
| I2 | A → B | the pivot's distinctness verdict (complementary-not-rival rests on it) | one-way |
| I3 | C2 → C1 | the **harmony floor** (the minimum budget that won't fragment a Tier-1 span) constrains C1's budget | one-way |
| I4 | C2 → B | the 3-Pass entanglement (Pass 2 brackets chunking) feeds B's complementary-entangled-00-24 relationship | one-way |
| I5 | B → C | the "reuse 06-14, don't re-derive" scoping bounds C to design-the-new-parts-only (rationale/scale/flip), not re-design the mechanism | one-way |
| I6 | A, B, C → D | the delivered content D gates for reach (D consumes all to present the per-proposal reach) | one-way |
| I7 | (external) 00-24's 3-Pass application → C2 | **assumption/prerequisite**: C2's bracketing presumes the 3-Pass is (or will be) wired; chunking's whole-text bracket IS Pass 2 → C is gated on the 3-Pass fix, like the prior chain | one-way (prereq) |

**Assumptions-not-data check:** the load-bearing hidden assumption is **I7** — C2's bracketing architecture ASSUMES the un-wired 3-Pass will be wired (Pass 2 is the bracket). If the 3-Pass is never wired, chunking loses its whole-text bracket and the whole-span remainder re-opens. This is made explicit as a prerequisite interface (not left hidden). Second assumption: C1/C2 assume the chunking ENGINE exists for the engine-layer policy — made explicit via the phase-split (instruction-now works without the engine).

## Step 6 — Dependency Order

1. **A (evaluation-core)** — FIRST. No dependencies; produces the verdict everything reads.
2. **B (prior-reconciliation)** — after A (consumes I2); can run parallel to C's start but needs A's verdict + C2's entanglement (I4) to complete.
3. **C (fix-design)** — after A (I1) and bounded by B's reuse-scoping (I5); INTERNALLY **C2 before C1** (C2 provides the harmony floor I3 that C1's budget consumes); C gated on the 3-Pass prerequisite (I7).
4. **D (reach gate)** — LAST; consumes A + B + C (I6).

No circular dependencies. Parallelizable: A first; then B and C2 can proceed together (both read A); C1 waits on C2 (harmony floor); B completes after C2 (entanglement); D waits on all.

## Step 7 — Self-Evaluation

**Minimum 3 dimensions:**

| Dimension | Check | Verdict |
|---|---|---|
| **Independence** | Each piece answerable without reading siblings except via interfaces? | **PASS** — A (is-it-sound) stands alone; B (chain-fit) needs only A's verdict + C2's entanglement (interfaced); C (design) needs A's go + C2→C1 internal order (interfaced); D (reach) is a disposition over the delivered content. |
| **Completeness** | Do the pieces cover the whole? | **PASS** — pivot+rivals+scoping (A), 4 relationships + re-test (B), policy + architecture (C1/C2), reach (D). No aspect of the stabilized model falls through. |
| **Reassembly** | Pieces + interfaces reconstruct the whole? | **PASS** — A's verdict + B's chain-fit + C's two-part design + D's reach = the complete evaluate-plus-design finding. |

**Determination-mechanism check (refinement):** the load-bearing concepts with runtime determinations are (i) "is the adherence effect real & at what variable?" — ANSWERED in A (the pivot verdict), not a missing piece; and (ii) "does this unit exceed the budget?" (the budget-gate) — ANSWERED in C1 (the flip-on policy IS the budget-gate mechanism); and (iii) "which Tier-1 structures exceed the chunk size?" (sets the floor) — ANSWERED in C2 (the harmony floor). All three determinations have homes. **PASS** — no determination presupposed without a piece providing it.

**Selected full-eval dimensions:**
- **Tractability:** each piece is a single focused pass. PASS.
- **Balance:** C is the largest (two sub-designs) but the late-split keeps C1/C2 each tractable; A is substantial (the pivot). Roughly balanced; no single piece is 80%. PASS.
- **Interface clarity:** 7 interfaces explicit incl. the two prerequisite assumptions (I7 = the 3-Pass entanglement; the engine-phase split). PASS.
- **Confidence:** top-down and bottom-up agree on all four boundaries. HIGH.

**Over-decomposition guard:** C1/C2 are kept as sub-designs INSIDE C (not top-level pieces) because they share A/B/D and are coupled via the budget (I3) — forcing them to top-level would over-decompose, the same call the prior `04-12` inquiry made with its staged-Step-5 + changelog sub-designs.

---

## Handoff Notes for Downstream (Innovation / Critique / CONCLUDE)

- **Innovation** works **C** — the flip-on policy (C1) + the whole-span bracketing architecture (C2), reusing the 06-14 mechanism design (do NOT re-derive it), plus any non-obvious reframes of the pivot/relationships.
- **The determination-mechanism is answered, not missing:** the soundness question lives in A (pivot verdict); the runtime budget-gate lives in C1.
- **CONCLUDE — Synthesis Trigger flagged:** this inquiry consumes/relates to FOUR priors. The finding needs `refines:`/relationship frontmatter and an `## Inherited Commitments Re-test` of the **four commitment clusters** (Piece B): (1) `00-24` un-wired-3-Pass-as-root-cause [re-test: survives — complementary, entangled]; (2) `01-09` whole-draft-checks-need-the-whole [re-test: survives — the check runs post-assembly]; (3) `06-14` the chunking design [re-test: HOW survives/reused; WHY/scale/default extended]; (4) `04-12` reduce-active-load [re-test: generalizes into a two-lever bounded-load principle]. Plus the honest MISFIT-analog must survive into the headline: **the "how" is already designed; the new yield is rationale + scale + flip**, and chunking is **complementary-not-rival + one of two load levers** (not the whole adherence answer).
- **Reach (D)** is per-proposal AND phase-aware (instruction-now vs engine-later) — carried to the user, not chosen here.
