## User Input

Warm pass over `_branch.md` + `surfacing.md` + `articulate_simple.md` for inquiry `2026-07-11_04-48__mandatory_chunking_char_limit_adherence`. Conflict-detection note (from the runner): surfacing found chunking is already comprehensively DESIGNED (two prior findings), PARTIALLY-IMPLEMENTED-but-DORMANT (PipelineConfig knobs, off, unwired into SKILL.md), with a designed intake feeder; the user's genuinely-new contribution is the adherence-under-load rationale at a much smaller scale (~3500 chars ≈ 875 tokens ≈ 0.4% of the 200K window the existing design targets); the chunk-vs-whole tension is already the design's central HARD constraint with an unsolved whole-span remainder; rival-vs-complementary leans complementary; the true load variable is plural. Right instinct, refined form — non-severe. Mechanism-validity is the pivot.

---

# Warm (Post-Context) Articulation — Output

**Pass runs WARM by construction.** Item count carried = 1 (i1). The surfaced material is treated as **information**; the cold MQ answers are kept as **constraints**.

## Re-anchor (MQ2 re-fired) — the load-bearing move

**Cold MQ2** identified open context-needs (does the SKILL chunk / is 3500 grounded / were the 7 errors adherence or 3-Pass / long-span structures / rival-vs-complementary). **Warm MQ2 commits the anchor** against the surfaced reality:

> The task bears on the project reality that **chunking is already designed (two findings), dormant (PipelineConfig knobs default-off, unwired into SKILL.md), and unvalidated (its empirical validation is a never-run deferred MUST)**, with a **designed intake feeder** (sentence-segmentation "load-bearing for translation chunking"). Given that, the task's live anchor is a three-part cluster: **(i) the mechanism-validity pivot** — is instruction-adherence-under-load real AND distinct from the un-wired-3-Pass, and is the true variable source-size / total-context-load / active-rule-count (the user's "~3500 source chars" is one proxy, and it sits ~200× below the 200K capacity the existing design chunks for); **(ii) the flip-on decision** — whether to move the dormant apparatus from opt-in-off to a budget-gated always-on policy in the model-facing workflow, and at what budget; **(iii) the unsolved whole-span remainder** — how chunked generation coexists with Pass-2's whole-text Harmony Map + whole-passage havuz/ring/arc + the prior-01-09 whole-draft checks, which harmony-tier-aware chunking does not fully cover.

**Directional commitment:** the useful form is **refined, not the literal proposal** — not "introduce mandatory chunking from scratch" but "evaluate the adherence rationale (the pivot), decide whether to flip the dormant apparatus on as a budget-gated policy + at what budget, and reconcile the whole-span remainder" — all **complementary to and gated on** the same un-applied 3-Pass, and reach-gated.

## Anchor-settle check (material-change judgment) — no fetch

Does the committed context-need name a **materially different territory** than what surfacing already fetched? **No.** Surfacing already traversed regions A–F including the two dedicated chunking findings, the intake feeder, schemas.py, harmony_layer.md, and the error-chain priors. The warm anchor **concretizes onto material already in view** (the dormant apparatus, the 200K-vs-875-token scale gap, the whole-span remainder) — it did not relocate to un-surfaced territory.

**Termination: FIXPOINT at round 0** — 0 re-surfaces. (Expected path: the cold aim plus the rich surfacing were already right; the anchor moved from *open* to *committed* without moving *territory*.)

## On-trigger re-runs (did context actually move the output?)

- **MQ4 (boundary) — RE-RAN (moved).** Surfacing revealed new exclusions: **(a)** don't re-derive the chunking DESIGN — it already exists in the two `2026-06-14` findings; this inquiry's delta is the adherence rationale + the flip-on decision, not the mechanism design (per FF2 redundancy pressure); **(b)** don't re-derive the un-wired-3-Pass fix — that belongs to `2026-07-11_00-24`; **(c)** confirmed — pinning the exact "3500" stays out (no evidence exists; treat as illustrative); **(d)** NEW — the target is **translate-stage** chunking (intake already feeds it sentence-segmented text), NOT re-opening intake preprocessing.
- **MQ1 (verdict) — RE-RAN (moved).** `design-the-intervention` narrows to **revive/wire/flip-on the dormant apparatus + set the budget** (not design from scratch); `validate-the-mechanism` is promoted to **the PIVOT** (the design is unvalidated and the adherence rationale is the missing piece); `correct-the-variable` sharpens to the plural-load reality.
- **MQ3 (intent, WHAT) — RE-RAN (moved).** The dominant endpoint shifts from "add-a-pipeline-stage-from-nothing" to **"decide the flip-on policy (budget-gated-always-on vs opt-in) + budget"**; `establish-an-input-budget-principle` gains a concrete home (`PipelineConfig.chunking_budget`, incl. per-model tuning).
- **MultiDepth-WHY — RE-RAN (lightly moved).** `cross-model-robustness` gains a concrete home (chunking_budget per-model, currently no policy); `find-a-mechanical-lever` is reinforced (the intake feeder makes chunking structural/mechanical — model-judgment-independent). WHY-set stable but sharpened.
- **MQA — RE-RAN (≥2 moved).** Joint axis reconfirmed: **the mechanism-validity pivot gates the policy + design questions**, now sharpened by the dormant-apparatus reality — the "how to DESIGN chunking" sub-question **largely dissolves** (it is mostly done), leaving the pivot (is the adherence effect real & distinct at the claimed small scale) + the flip-on policy (budget-gated on/off + budget) + the whole-span remainder. Reach-gate sits orthogonally atop. Residual irreducible overlap (carried): `correct-the-variable` straddles the pivot (if the real variable is total-load/active-rule-count, the mechanism is *real but mis-described* and chunking-the-source is a partial lever) and the design (which load to reduce).

## Conflict-detection (warm-only)

**`content-conflict` — FLAG, severity NON-SEVERE (resolvable by re-anchoring).**

- **Identified mismatch (not adjudicated):** the premise frames chunking as something to **introduce** ("chunking should be mandatory maybe") and locates the driver in **source character-count (~3500)**. Surfaced reality shows chunking is **already designed + dormant + unvalidated**, and the ~3500-char figure is **unevidenced and ~200× below the 200K capacity** the existing design chunks for, while the true load variable is **plural** (source-size / total-context-load / active-rule-count).
- **Why NON-SEVERE (a refinement, not a contradiction):** the user's core instinct is **affirmed** by the reality, not contradicted — chunking matters for reliability (the design exists precisely because it does); the whole-vs-chunk tension is real (it IS the design's Tier-1-preservation HARD constraint); smaller-is-safer aligns with the design's own asymmetric-failure rule ("over-chunk under uncertainty"). Only the **form** refines: greenfield→dormant-apparatus (so the ask becomes flip-on + budget, not invent), and source-char-count→plural-variable-at-a-smaller-scale (so the mechanism-validity pivot must be evaluated honestly, not assumed). Same shape as the prior inquiries' locus-refinements.
- **Escalation ladder:** resolvable-by-re-anchoring → the re-anchor above already absorbs it. **No formulated clarifying question** (proceed). The pipeline (sensemaking/critique) resolves *whether the adherence mechanism is real & distinct* — the discipline only names and grades the mismatch, it does not rule on it.

**Conflict-block gate: PROCEED** (non-severe).

## Re-fired Rephrase — refreshed Considered Articulations (project vocabulary)

1. **(mechanism-validation — the pivot, now primary)** Evaluate whether instruction-adherence-under-load is real AND distinct from the un-wired-3-Pass, and identify the true variable (source-size vs total-context-load vs active-rule-count) — recognizing the existing design already chunks for 200K capacity, so the user's ~875-token-scale adherence claim, *if it holds*, is a genuinely new and much-smaller budget driver.
2. **(flip-on policy — refined from "introduce")** Decide whether to flip the already-designed-but-dormant chunking apparatus (`PipelineConfig.chunking_budget` / `chunking_granularity` / `chunking_mechanism_override`) from opt-in-off to a **budget-gated always-on** policy in the model-facing workflow, and at what budget, reconciling against the whole-draft structural-fidelity checks.
3. **(whole-span remainder)** Design how budget-gated chunking coexists with the whole-text obligations even harmony-tier-aware chunking doesn't fully cover — Pass-2's whole-text Harmony Map + whole-passage havuz/ring/arc + the prior-01-09 whole-draft checks — likely by chunking the **generation** while keeping a whole-text harmony pass + whole-draft check.
4. **(reframe-the-variable / contrarian)** Test whether the real lever is reducing **total active load / simultaneously-active rule-count** (connecting to staging `04-12`) rather than chunking the source — since chunking the source leaves the large fixed instruction load + the ~80 active principles untouched.
5. **(rival-vs-complementary — settle)** Confirm chunking is **COMPLEMENTARY** to (stacks on) the un-wired-3-Pass, not a rival root cause — it reduces per-chunk omission *opportunity* but doesn't fix the meaning+harmony collapse that recurs in every chunk — and gate it on the same 3-Pass wiring.
6. **(revive-not-invent / redundancy)** Recognize the "how" is largely already designed (two chunking findings) and unvalidated — so the deliverable is a **revive + validate + flip-on** decision plus the new adherence rationale, not a from-scratch chunking design.

## Carried unchanged

- **Itemize:** count = 1 (i1) — the proposal + its rationale remain one argument.
- **Deconstruct deliverable-shape:** evaluation-plus-conditional-design — unchanged.
- **MultiDepth-literal:** *"Chunking should maybe be mandatory; I think there's a limit to how much an AI session can follow rules and translate — above a certain character count (around 3500 for Claude Opus 4.8 max, probably lower for smaller models) it maybe starts skipping instructions."* — carried verbatim.

## Self-Assessment Verdict

**MED-FLAG** + `content-conflict` (NON-SEVERE, no clarifying-question payload).

The warm anchor settled at fixpoint (round 0, no re-surface). The MED-FLAG carries one flagged condition into the pipeline: the **premise-vs-reality refinement** — chunking is *designed/dormant/unvalidated* not greenfield, and the driver is a *plural load variable at a much smaller scale* than the capacity the existing design targets, so the inquiry's weight sits on the **mechanism-validity pivot** (is adherence-decay-under-load real & distinct, and at what scale/variable) and the **whole-span remainder**, with the "how to design chunking" sub-question largely dissolved into the two existing chunking findings. Right instinct, refined form; resolvable by re-anchoring; PROCEED to Sensemaking.
