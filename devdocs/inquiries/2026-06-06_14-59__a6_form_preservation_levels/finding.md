---
status: active
model: claude-opus-4-7[1m]
effort: max
refines: devdocs/inquiries/2026-06-05_14-14__translation_config_axes/finding.md
---
# Finding: A6 — Form Preservation (the 5 Levels Tied to harmony_layer)

## Changes from Prior

**Prior path:** `devdocs/inquiries/2026-06-05_14-14__translation_config_axes/finding.md` (root architectural finding; A6 plain-ordinal with 5 levels proposed; ties to `harmony_layer.md` Tier 1-4 system; activation-gate role for nazm-as-meaning policy at "light" threshold).

**Revision trigger:** User directed: "now lets do it for a6 form preservation." A6 is the second axis in the Strategy family. The substantive content artifact A6 ties to (`harmony_layer.md`'s Tier 1-4 system with 49 entries and the 3-Pass Meaning Lock / Harmony Map / Target Reconstruction methodology) provides direct operational anchoring.

**What's preserved:**
- A6 plain-ordinal pattern (root).
- A6 ties directly to `harmony_layer.md` Tier 1-4 system (root commitment).
- A6 activation-gate role for nazm-as-meaning policy at "light" threshold (root commitment).
- A5↔A6 orthogonality (root + A5).
- A4 per-purpose A6 defaults (A4 matrix; with naming refinement note here).
- NEW translator-strategy 4-component template from A5 (adapt to A6).
- Receptive-only NOT APPLICABLE pattern (A4 + A5).
- DOMESTICATE-disfavored cross-cutting policy lineage (informs A6 indirectly; A6 doesn't override).
- Chain-default via A4 matrix.
- Language-agnostic at concept level (with Tier 4 content-language-specific caveat).

**What's changed:**
- **A6 cardinality validated at 5** on substantive grounds. The harmony_layer.md Tier 1-4 system naturally produces 5 levels: an "off" floor + 4 tier-coverage levels above. Labels: `off | minimal | light | standard | maximum`.

**What's new:**
- **Per-level tier-coverage mapping** as the operational substance. Each A6 level maps to which harmony_layer tiers the translator-AI preserves:
  - `off` → preserve nothing structural; Pass-1 (Meaning Lock) only.
  - `minimal` → preserve Tier 1 (Non-Negotiable; 13 entries) only.
  - `light` → preserve Tier 1 + Tier 2 (High Priority; 12 entries). **ACTIVATION GATE for nazm policy.**
  - `standard` → preserve Tier 1 + Tier 2 + Tier 3 entries WHEN their PRESERVE-WHEN clause fires for the specific source (translator-AI runtime determination).
  - `maximum` → preserve all 4 tiers. Tier 1+2 always; Tier 3 respecting its conditional clauses; Tier 4 (language-specific aesthetic) attempted where target language permits, with sacrifices acknowledged in the harmony report when target language structurally cannot support the feature.

- **Tier 3 conditional logic INVARIANT across Levels 4-5.** harmony_layer.md's PRESERVE-WHEN/SACRIFICE-WHEN clauses are the SYSTEM's own adjudication mechanism; they are NOT a user-configurable property. A6 Level 5 doesn't override the Tier 3 conditional logic — it adds Tier 4 ON TOP of Tier 3 conditional handling. This commitment ensures A6 doesn't contradict the harmony_layer's own self-adjudication rule.

- **Tier 4 language-feasibility caveat at Level 5.** harmony_layer.md Tier 4 entries are explicitly language-specific ("Root echo — almost exclusively a Semitic language feature"; "Internal rhyme and assonance — deeply language-specific"). A6 Level 5 attempts Tier 4 preservation where target language permits and acknowledges sacrifices in the harmony report otherwise, per harmony_layer's own guidance ("Acknowledge it in the harmony report as 'source-language-specific, no equivalent'").

- **A6-adapted 4-component translator-strategy template** (parallels A5's template with A6-specific composition):
  1. Strategic stance description (which tiers preserved)
  2. Tier-coverage specification (the per-level Tier 1-4 mapping)
  3. Nazm-policy-activation marker (off at Levels 1-2; ACTIVE at Levels 3-5)
  4. Cross-axis interaction note (A4 matrix match + A5 orthogonality)

- **Default `light` via A4 chain** (casual → "moderate" → `light`). The project defaults to nazm-policy-active. The categorical equivalent of conservative-bias-LOWER propagates through A4's matrix to default A6 = `light`.

- **A4 finding refinement note** for matrix naming alignment. A4 used informal verbiage ("moderate", "high", "MAXIMUM") for A6 defaults; A6 uses precise labels. At next A4-finding maintenance pass, propagate exact A6 labels: A4 "moderate" → `light`; A4 "high" → `standard`; A4 "MAXIMUM" → `maximum`.

- **Receptive-only commitment DOES NOT APPLY to A6** (parallel to A4, A5). A6 is translator-strategy / user-configuration, not reader-property. Explicit non-inheritance.

- **Strategy-family-progress marker (2/3).** After this inquiry: Reader 3/3 + Purpose 1/1 + Strategy 2/3 = 6/8 axes complete.

**Migration:** No migration needed — first A6 specification. Schema commit: `form_preservation: Literal["off", "minimal", "light", "standard", "maximum"]`.

---

## Question

For A6 — Form Preservation (second axis in the Strategy family, plain-ordinal structural preservation tied directly to `harmony_layer.md`'s Tier 1-4 system, activation-gate for the nazm-as-meaning POLICY at the "light" threshold per root commitment), what should the 5 ordinal level names be, what per-level tier-coverage mapping (which of Tier 1, 2, 3, 4 each level preserves), where the activation-gate threshold sits, how this cross-validates with A4's per-purpose A6 defaults, how Tier 3's PRESERVE-WHEN/SACRIFICE-WHEN runtime conditionality interacts with A6 levels, and how the A5/A6 orthogonality holds — anchored to the project's Said Nursi corpus per level?

---

## Finding Summary

- **5 ordinal levels:** `off | minimal | light | standard | maximum`. Tied directly to `harmony_layer.md`'s Tier 1-4 system. Plain-ordinal.

- **Per-level tier-coverage mapping** (the operational substance):
  - `off` → preserve nothing structural
  - `minimal` → preserve Tier 1 only
  - `light` → preserve Tier 1 + Tier 2 (ACTIVATION GATE)
  - `standard` → preserve Tier 1 + Tier 2 + Tier 3 (respecting PRESERVE-WHEN/SACRIFICE-WHEN clauses)
  - `maximum` → preserve all 4 tiers (Tier 3 respecting conditional; Tier 4 attempted where target language permits)

- **Activation gate at Level 3 `light`** per root commitment. Below the gate (off, minimal): the AI runs partial 3-Pass methodology. At or above the gate (light, standard, maximum): full 3-Pass methodology fires and nazm-as-meaning policy is active.

- **Tier 3 conditional logic INVARIANT across Levels 4-5.** Level 5 ADDS Tier 4 ON TOP of Tier 3 conditional handling, NOT overrides Tier 3 adjudication. harmony_layer's own PRESERVE-WHEN/SACRIFICE-WHEN clauses are the system's structural rule.

- **Tier 4 language-feasibility caveat at Level 5.** Attempt where target language permits; harmony report acknowledges sacrifices when target language structurally cannot support the feature (e.g., Semitic root echo to non-Semitic target).

- **A6-adapted 4-component translator-strategy template** (Strategic stance + Tier-coverage + Nazm-policy-activation marker + Cross-axis interaction).

- **Default `light` via A4 chain.** A4 silent → casual → "moderate" → `light`. The project defaults to nazm-policy-active.

- **A4 finding refinement note** for matrix naming alignment (moderate→`light`; high→`standard`; MAXIMUM→`maximum`).

- **Receptive-only DOES NOT APPLY to A6** (parallel to A4, A5).

- **A5↔A6 orthogonality** confirmed. A5 = lexical/idiomatic surface; A6 = structural form (harmony_layer tiers).

- **Strategy family at 2/3 after this inquiry.** Reader 3/3 + Purpose 1/1 + Strategy 2/3 = 6/8 axes complete. A7 Scaffolding next; then A8 Depth.

---

## Finding

The root architectural inquiry committed A6 to plain-ordinal with 5 levels proposed, ties to `harmony_layer.md`'s Tier 1-4 system, and the activation-gate role for the nazm-as-meaning policy at "light" threshold. This inquiry operationalizes those commitments against the substantive harmony_layer content (49 entries across 4 tiers with the 3-Pass Meaning Lock / Harmony Map / Target Reconstruction methodology).

### 1. The Framework

#### 1.1 Cardinality validated at 5 substantively

The harmony_layer.md Tier 1-4 system has 4 natural tier strata:
- **Tier 1 (13 entries — Non-Negotiable):** meaning IS carried by harmony (cause-effect chains; conditional chains; hidden syllogisms; semantic escalation/de-escalation; convergence; ellipsis patterns; emotional arc; tense consistency; person-voice threading + iltifat; antonym pairing; etc.).
- **Tier 2 (12 entries — High Priority):** strongly supports meaning comprehension (grammatical parallelism; ring composition; chiastic structure; pronoun chains; clause-length patterning; evidence-claim rhythm; concession-rebuttal; thematic bracketing; etc.).
- **Tier 3 (13 entries — Context-Dependent):** each entry has own PRESERVE-WHEN / SACRIFICE-WHEN clauses (register consistency; synonym chaining; isotopy; callback/forward-reference; performative continuity; parallel panel structure; merismus; particle threading; etc.).
- **Tier 4 (11 entries — Low Priority, language-specific):** aesthetic polish (phonetic echo; internal rhyme/assonance; root echo; phonetic weight; maqta' harmony; etc.).

Adding an "off" floor produces 5 levels. Going to 4 levels would merge Tier 3 conditional with Tier 4 (loses operationally distinct runtime handling). Going to 6 would split Tier 1 internally (Tier 1's 13 entries are uniformly preserve-leaning per their own clauses; no split-point). 5 is the natural fit.

#### 1.2 Per-level tier-coverage mapping

The central operational substance. Per-level coverage:

| A6 Level | Tier 1 | Tier 2 | Tier 3 | Tier 4 | Nazm Policy |
|---|---|---|---|---|---|
| `off` | — | — | — | — | OFF |
| `minimal` | ✓ | — | — | — | OFF (Tier 1 preserved but full policy not active) |
| `light` | ✓ | ✓ | — | — | **ACTIVE (activation gate)** |
| `standard` | ✓ | ✓ | ✓ (per PRESERVE-WHEN) | — | ACTIVE |
| `maximum` | ✓ | ✓ | ✓ (per PRESERVE-WHEN) | ✓ (where target permits) | ACTIVE |

#### 1.3 Activation gate at Level 3 `light`

Root committed: "When A6 is at 'light' or higher, the always-on policy of nazm/form-as-meaning preservation activates."

Operationally:
- At `off`: AI runs Pass 1 (Meaning Lock) only. No Pass 2 (Harmony Map) or Pass 3 (Target Reconstruction). Acceptable for utility passes; semantic-only output.
- At `minimal`: AI runs Pass 1 + partial Pass 2/3 for Tier 1 entries only. Nazm-as-meaning policy is not yet considered fully active.
- At `light` (THRESHOLD): Full 3-Pass methodology fires. Nazm-as-meaning policy active. AI builds a Harmony Map covering Tier 1 + Tier 2 and applies it in Pass 3.
- At `standard` and `maximum`: Full 3-Pass methodology with Tier 3 (conditional) and Tier 4 (Level 5 only).

#### 1.4 Tier 3 conditional logic INVARIANT across Levels 4-5

A central commitment. harmony_layer.md's PRESERVE-WHEN/SACRIFICE-WHEN clauses are the SYSTEM's adjudication mechanism; they are NOT a user-configurable property of A6.

For Tier 3 entries (e.g., "Register consistency — PRESERVE when source uses register alternation as a structural device; SACRIFICE when source is register-uniform and target language requires register adjustment for naturalness"), the translator-AI examines the source at runtime and determines per Tier 3 entry whether the PRESERVE-WHEN or SACRIFICE-WHEN clause fires.

A6 Level 5 (`maximum`) does NOT override this. Level 5 adds Tier 4 preservation ON TOP of Tier 3 conditional handling. The distinction:
- "Override Tier 3 conditional at Level 5" would mean: preserve ALL 13 Tier 3 entries regardless of PRESERVE-WHEN/SACRIFICE-WHEN clause adjudication. This would violate harmony_layer's self-adjudication rule.
- "Add Tier 4 on top of Tier 3 conditional at Level 5" means: preserve Tier 3 entries WHEN PRESERVE-WHEN fires (same as Level 4); ALSO preserve Tier 4 entries where target language permits.

The latter is correct.

#### 1.5 Tier 4 language-feasibility caveat at Level 5

harmony_layer.md acknowledges Tier 4 features are language-specific:
- "Internal rhyme and assonance — deeply language-specific. Arabic assonance cannot transfer to Turkish or English."
- "Root echo — this is almost exclusively a Semitic language feature. Non-Semitic target languages simply don't have this mechanism. Acknowledge it in the harmony report as 'source-language-specific, no equivalent.'"
- "Phonetic weight balancing — tied to the specific phonology of the source language."

A6 Level 5 attempts Tier 4 preservation where target language permits and acknowledges sacrifices in the harmony report otherwise. This is consistent with harmony_layer's own guidance.

#### 1.6 A6-adapted 4-component translator-strategy template

Parallel to A5's NEW template, A6-adapted composition:

1. **Strategic stance description** — what the AI's overall stance is at this level (which tiers preserved).
2. **Tier-coverage specification** — the per-level Tier 1-4 mapping (Section 1.2 table for this level).
3. **Nazm-policy-activation marker** — whether the nazm policy is OFF (Levels 1-2) or ACTIVE (Levels 3-5).
4. **Cross-axis interaction note** — A4 matrix match + A5 orthogonality reminder.

#### 1.7 Receptive-only DOES NOT APPLY to A6

Parallel to A4, A5. A6 is translator-strategy / user-configuration, not a reader property. Explicit non-inheritance.

#### 1.8 Default `light` via A4 chain

When A4 is silent → A4 defaults to `casual` → A4's casual A6 default = "moderate" → A6 Level 3 `light`. Final A6 default = `light`.

This is the activation gate threshold — the project defaults to nazm-policy-active, reflecting the project's commitment to form-as-meaning as a baseline.

#### 1.9 A4 finding refinement note

A4 finding's matrix used informal verbiage:
- A4 "moderate" (casual A6 default) → A6 `light`
- A4 "high" (scholarly, devotional, language-learning A6 defaults) → A6 `standard`
- A4 "MAXIMUM" (performance A6 default) → A6 `maximum`

At next A4-finding maintenance pass, propagate exact A6 labels.

#### 1.10 Strategy family at 2/3

A6 is the second axis in the Strategy family. After this inquiry: Reader 3/3 + Purpose 1/1 + Strategy 2/3 = 6/8 axes complete. A7 Scaffolding remains; then A8 Analysis Depth.

### 2. The 5 Per-Level Definitions

Each level has the 4 components (strategic stance + tier-coverage + nazm-policy marker + cross-axis interaction).

#### 2.1 `off`

**Strategic stance.** No harmony work. Translator-AI runs Pass 1 (Meaning Lock) only. Output is semantic-fidelity-only; no structural preservation; no harmony report.

**Tier-coverage.** None.

**Nazm-policy marker.** OFF.

**Cross-axis interaction.** No A4 purpose defaults to `off` — this level requires explicit user override. A5 orthogonal (A5 still operative at any value).

**Said Nursi anchor.** Rare project use — internal AI utility passes; semantic extraction for indexing; rough draft for editorial review.

#### 2.2 `minimal`

**Strategic stance.** Preserve Tier 1 (Non-Negotiable; meaning-as-harmony) only. The AI preserves cause-effect chains, hidden syllogisms, conditional chains, semantic escalation/de-escalation, convergence (havuz), ellipsis patterns, and the other 7 Tier 1 entries because LOSING THEM CHANGES MEANING. Tier 2+3+4 sacrificed.

**Tier-coverage.** Tier 1 only.

**Nazm-policy marker.** OFF (Tier 1 preserved as meaning-bearing structures, but full nazm policy not yet active).

**Cross-axis interaction.** No A4 purpose defaults to `minimal` — this level requires explicit user override (e.g., utility translation that needs argument-structure preservation without aesthetic harmony).

**Said Nursi anchor.** Said Nursi's istilzam chains (cited explicitly by the user in `notes.md`: the cause-effect chain "Rahman → Rezzak → Rızk → Beka → Vücud → İlim/İrade/Kudret → Hayat") — Tier 1 preservation is critical for the argument structure even when no other harmony work is performed.

#### 2.3 `light` (ACTIVATION GATE)

**Strategic stance.** Preserve Tier 1 + Tier 2. Full 3-Pass methodology fires (Meaning Lock → Harmony Map → Target Reconstruction). Tier 1 entries (meaning-as-harmony) and Tier 2 entries (high-priority comprehension scaffolds: grammatical parallelism, ring composition, chiastic structure, pronoun chains, clause-length patterning, evidence-claim rhythm, etc.) are preserved. Tier 3 + Tier 4 sacrificed.

**Tier-coverage.** Tier 1 + Tier 2.

**Nazm-policy marker.** **ACTIVE — activation gate threshold per root commitment.**

**Cross-axis interaction.** A4 purpose default: `casual` (per A4 matrix's "moderate"). A4 silent → `light` via chain.

**Said Nursi anchor.** Casual edition of Risale-i Nur for general curious reader. The text's parallelism, ring composition, and chiastic structures are preserved (Tier 2); the meaning-bearing istilzam chains, emotional arcs, and tense shifts are preserved (Tier 1); the register-alternation conditionality (Tier 3) and phonetic echoes (Tier 4) are sacrificed.

#### 2.4 `standard`

**Strategic stance.** Preserve Tier 1 + Tier 2 + Tier 3 with conditional clauses respected. Translator-AI examines source at runtime and determines per Tier 3 entry whether PRESERVE-WHEN or SACRIFICE-WHEN clause fires. Tier 4 sacrificed.

**Tier-coverage.** Tier 1 + Tier 2 + Tier 3 conditional.

**Nazm-policy marker.** ACTIVE.

**Cross-axis interaction.** A4 purpose defaults: `scholarly`, `devotional`, `language-learning` (per A4 matrix's "high").

**Said Nursi anchor.** Scholarly/devotional edition of Risale-i Nur. Register alternation (Tier 3 PRESERVE-WHEN: "source uses register alternation as a structural device" — fires for Nursi per user's stored register-fidelity memory) is preserved. Synonym chaining (Tier 3 PRESERVE-WHEN: deliberate theological emphasis on divine names) — preserved. Callback and forward-reference (Tier 3 PRESERVE-WHEN: source's long-range echoes carry argument) — preserved. Sentence-length rhythm (Tier 3 PRESERVE-WHEN: source's tempo carries meaning) — preserved when Nursi's tempo is meaning-bearing.

#### 2.5 `maximum`

**Strategic stance.** Preserve all 4 tiers. Tier 1 + Tier 2 + Tier 3 conditional + Tier 4 where target language permits. The AI attempts maximum form preservation including phonetic echo, rhyme, cadence matching where target language structurally supports it; acknowledges sacrifices in the harmony report when target language cannot support the feature (e.g., Semitic root echo to non-Semitic target).

**Tier-coverage.** All 4 tiers (Tier 3 respecting conditional; Tier 4 attempted where feasible).

**Nazm-policy marker.** ACTIVE (maximum emphasis).

**Cross-axis interaction.** A4 purpose default: `performance` (per A4 matrix's "MAXIMUM"). Rhythm = meaning per harmony_layer commitment. A5 orthogonal — `maximum` form preservation can coexist with any A5 stance (heavily-foreignized + maximum form = scholarly devotional Quran-recitation; lightly-domesticated + maximum form = poetic translation that mirrors source rhythm in target's natural poetic vocabulary).

**Said Nursi anchor.** Performance/recitation edition of Risale-i Nur. Phonetic harmony attempted where Turkish-English target permits; cadence matched where possible; maqta' harmony explored; root echo acknowledged as "source-language-specific, no equivalent" in harmony report where the source's Arabic/Turkish root play cannot transfer.

### 3. Tier 3 Conditional + Tier 4 Language-Feasibility Special Handling

#### 3.1 Tier 3 PRESERVE-WHEN/SACRIFICE-WHEN runtime determination

For each Tier 3 entry, harmony_layer.md provides PRESERVE-WHEN and SACRIFICE-WHEN clauses. The translator-AI determines at runtime which clause fires per source. Example workflow:

1. AI examines source for register-consistency Tier 3 entry.
2. AI checks PRESERVE-WHEN clause: "source uses register alternation as a structural device."
3. AI checks SACRIFICE-WHEN clause: "source is register-uniform and target language requires register adjustment for naturalness."
4. AI determines which clause fires for the specific source passage.
5. AI applies preserve-or-sacrifice decision accordingly.

This runtime determination is INVARIANT across A6 Levels 4 and 5. The Tier 3 conditional logic is harmony_layer's structural rule, not a user-configurable property.

#### 3.2 Tier 4 language-feasibility at Level 5

Tier 4 entries are explicitly language-specific. A6 Level 5 attempts each Tier 4 feature where target language permits:
- Phonetic echo: attempted where target language has its own sonic possibilities.
- Internal rhyme and assonance: attempted; sacrificed when target language phonology doesn't support source's pattern.
- Root echo: Semitic-specific. For non-Semitic targets (English, Turkish, etc.), acknowledged as "source-language-specific, no equivalent" in the harmony report.
- Rhythmic cadence: attempted where target language syllable structure permits.
- Maqta' harmony: source-language-specific; acknowledged in harmony report.

The harmony report is the documentation channel where the translator-AI flags Tier 4 sacrifices that target-language structure made unavoidable.

### 4. Cross-Axis Boundaries + A4 Matrix Cross-Validation

#### 4.1 A5↔A6 orthogonality (re-validated)

A5 = lexical/idiomatic SURFACE. A6 = structural FORM (harmony_layer tiers).

Four-corners independence:
- `foreignized-max` A5 + `off` A6: rare — preserve foreign vocabulary while flattening rhythm (utility translation).
- `foreignized-max` A5 + `maximum` A6: common scholarly devotional (preserve both vocabulary and form).
- `lightly-domesticated` A5 + `off` A6: common casual mass-market (anglicize vocabulary, flatten rhythm).
- `lightly-domesticated` A5 + `maximum` A6: poetic translations that domesticate vocabulary but mirror rhythm.

#### 4.2 A4 matrix cross-validation

| A4 Purpose | A4 matrix A6 default | A6 5-level mapping | Tier-coverage |
|---|---|---|---|
| scholarly | high | `standard` | Tier 1+2+3 conditional |
| devotional | high (preserve rhythm/form) | `standard` | Tier 1+2+3 conditional |
| casual | moderate | `light` | Tier 1+2 (activation gate) |
| language-learning | high (preserve structure for pedagogy) | `standard` | Tier 1+2+3 conditional |
| performance | MAXIMUM (cadence; rhythm=meaning) | `maximum` | Tier 1+2+3 conditional + Tier 4 |

#### 4.3 A4 finding refinement note

A4 finding's matrix used informal verbiage. At next A4-finding maintenance pass, propagate exact A6 labels:
- A4 "moderate" → A6 `light`
- A4 "high" → A6 `standard`
- A4 "MAXIMUM" → A6 `maximum`

Naming consistency aids schema commit clarity.

### 5. Strategy Family Progress

After this inquiry:
- **Reader family CLOSED** (A1 + A2 + A3 = 3/3).
- **Purpose family CLOSED** (A4 = 1/1).
- **Strategy family at 2/3** (A5 + A6 specified; A7 Scaffolding remains).
- **6/8 axes complete.**

```python
class A1ReaderLevel:  # composite-axis
    vocabulary_breadth: Literal["very_basic", "daily", "conversational", "advanced", "native"]
    syntactic_processing_capacity: Literal["very_basic", "daily", "conversational", "advanced", "native"]
    idiom_recognition: Literal["very_basic", "daily", "conversational", "advanced", "native"]
    inference_capacity: Literal["very_basic", "daily", "conversational", "advanced", "native"]
    cultural_reference_recognition: Literal["very_basic", "daily", "conversational", "advanced", "native"]

domain_expertise: Literal["lay", "aware", "educated", "trained", "expert"]
source_culture: Literal["outsider", "acquainted", "familiar", "heritage", "source-native"]
purpose: Literal["scholarly", "devotional", "casual", "language-learning", "performance"]
source_fidelity: Literal["foreignized-max", "foreignized", "balanced", "lightly-domesticated"]
form_preservation: Literal["off", "minimal", "light", "standard", "maximum"]
```

**What's next.** A7 Scaffolding (next inquiry) and A8 Analysis Depth.

**What's still open per A6:**
- Per-target-language Tier 4 feasibility matrix — future inquiry on which Tier 4 features each target language can structurally support.
- Tier 3 runtime determination operational spec — full prompt-engineering pass for the translator-AI's source-text examination logic per Tier 3 entry.
- A4 finding maintenance pass for naming alignment.

---

## Inherited Commitments Re-test

**IC1 — A6 plain-ordinal.** Source: root. RE-TESTED OK.

**IC2 — A6 5-level cardinality.** Source: root. RE-TESTED & CONFIRMED. (Section 1.1)

**IC3 — A6 ties to harmony_layer.md Tier 1-4.** Source: root. RE-TESTED OK & OPERATIONALIZED. (Section 1.2)

**IC4 — A6 activation-gate at `light`.** Source: root. RE-TESTED & CONFIRMED at Level 3. (Section 1.3)

**IC5 — A6↔A5 orthogonality.** Source: root + A5. RE-TESTED OK. (Section 4.1)

**IC6 — A4 per-purpose A6 defaults.** Source: A4 finding. RE-TESTED & CROSS-VALIDATED with naming refinement note. (Section 4.2, 4.3)

**IC7 — NEW translator-strategy 4-component template.** Source: A5. RE-TESTED & ADAPTED for A6. (Section 1.6)

**IC8 — Receptive-only NOT APPLICABLE.** Source: A4 + A5. RE-TESTED OK. (Section 1.7)

**IC9 — Chain default via A4 matrix.** Source: A5. RE-TESTED & APPLIED — default `light`. (Section 1.8)

**IC10 — Language-agnostic at concept level.** Source: root + chain. RE-TESTED & REFINED — framework agnostic; Tier 4 content is language-specific. (Section 1.5, 3.2)

**IC11 — 5 ordinal levels `off | minimal | light | standard | maximum`.** NEW.

**IC12 — Per-level tier-coverage mapping.** NEW. (Section 1.2)

**IC13 — Tier 3 conditional INVARIANT across Levels 4-5.** NEW. (Section 1.4)

**IC14 — Tier 4 language-feasibility caveat at Level 5.** NEW. (Section 1.5, 3.2)

**IC15 — A4 finding refinement note (naming alignment).** NEW. (Section 4.3)

**IC16 — A6-adapted 4-component template.** NEW. (Section 1.6)

**IC17 — Strategy-family-progress marker (2/3).** NEW. (Section 5)

---

## Next Actions

### MUST

- **What:** Commit the A6 enum to schema: `form_preservation: Literal["off", "minimal", "light", "standard", "maximum"]`.
- **What:** Update A4 finding at next maintenance pass to align matrix-naming with A6 labels (moderate→`light`; high→`standard`; MAXIMUM→`maximum`).

### COULD

- **What:** Build per-target-language Tier 4 feasibility matrix at the audience or corpus level.
  - Future inquiry. GATED by MUST schema commit.
- **What:** AI prompt-engineering pass that embeds Sections 1-4 + harmony_layer.md content as system context.
  - After schema commit.

### DEFERRED

- **What:** HIP musical performance cross-domain illustration.
- **What:** Architectural restoration (Venice Charter) cross-domain illustration.
- **What:** Per-target-language Tier 4 feasibility matrix as a structured artifact.

---

## Reasoning

### Why 5 levels (not 4 or 6)

The harmony_layer.md Tier 1-4 system has 4 natural strata + an "off" floor = 5 natural A6 levels. Going to 4 merges Tier 3 conditional with Tier 4 and loses the runtime conditional-vs-language-specific distinction. Going to 6 over-stratifies (Tier 1's 13 entries don't split).

### Why activation gate at Level 3

Per root commitment. Operationally: at Level 3 `light`, the full 3-Pass methodology fires (Pass 1 + Pass 2 Harmony Map + Pass 3 Target Reconstruction) and the nazm-as-meaning policy is active. Levels 1-2 run only partial methodology.

### Why Tier 3 conditional is INVARIANT

harmony_layer.md's PRESERVE-WHEN/SACRIFICE-WHEN clauses are the SYSTEM's own adjudication mechanism. Overriding them at A6 Level 5 would violate harmony_layer's spec. Level 5 adds Tier 4 ON TOP of Tier 3 conditional handling; it doesn't replace Tier 3 conditional logic with unconditional preservation.

### Why receptive-only doesn't apply

Parallel to A4, A5. A6 is translator-strategy / user-configuration, not reader-property.

---

## Open Questions

### Monitoring
- A4 finding maintenance pass for naming alignment.
- Tier 3 runtime determination operational spec for prompt-engineering pass.

### Blocked
- Per-target-language Tier 4 feasibility matrix blocked by future audience-level or corpus-level inquiry.

### Research Frontiers
- Per-target-language Tier 4 feasibility matrix — structured artifact mapping each target language to which Tier 4 features it can structurally support.

### Refinement Triggers
- If user feedback shows the 5-level cardinality over-restricts or under-distinguishes, revisit.
- If Tier 3 runtime determination produces AI mistakes (e.g., misjudging PRESERVE-WHEN clauses), refine the prompt-engineering for source-text examination.

---

## Source Input

<details>
<summary>Raw user input</summary>

```text
now lets do it for a6 form preservation
```

</details>
