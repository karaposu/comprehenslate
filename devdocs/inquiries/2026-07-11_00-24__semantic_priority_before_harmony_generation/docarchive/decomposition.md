# Decomposition — The Semantic-Priority Diagnostic + Fix, Partitioned

## User Input

`_branch.md`. Stabilized model (sensemaking.md): the errors are possible because the SKILL's executed workflow contradicts its stated method — `harmony_layer.md`'s 3-Pass (Meaning Lock = "the foundation that cannot be violated") is orphaned; `SKILL.md` Step 5 imports that file only as "the harmony-layer Tier 1-4 preservation policy" (line 78), never invoking the method; the file double-serves as method + policy and the workflow wires only the policy → no locked-meaning foundation → meaning + harmony collapse into one fluent motion. Fork 1 = REFINES+COMPLEMENTS the prior (one cause, two gates). Fork 2 = locus is un-invoked PROCEDURE. Fix = invoke the existing 3-Pass. Span the OUTPUT-REACH fork; flag the Inherited Commitments Re-test (this refines a prior). Save to `decomposition.md`.

---

## Step 1 — Coupling Topology (coupling map)

**Elements** of the diagnostic-plus-fix whole:
E1 diagnosis-core mechanism (un-invoked 3-Pass · harmony_layer double-serves method+policy · Step 5 imports only the policy · workflow-contradicts-stated-method · layer-collapse) · E2 this-vs-prior reconciliation (one shared cause → two missing gates; refines+complements, not supersedes) · E3 locus verdict (instinct-right / locus-refined; the two wrong loci excluded) · E4 fix-design (invoke the 3-Pass as ordered phases; intermediate-artifact real-vs-simulated; granularity caveat; harmony_layer method/policy restructure) · E5 OUTPUT-REACH gate · E6 inherited-commitments re-test (the prior's 3 commitments, re-tested because this refines it).

**Coupling gradient (change-A→change-B?):**
- E1↔E3 **strong** (the verdict is read off the mechanism — once the 3-Pass is seen un-invoked, "instinct-right/locus-refined" follows). → one cluster.
- E2↔E6 **strong** (the re-test IS the concrete validation of the "refines" relationship; the relationship decides which commitments to re-test). → one cluster.
- E1→E2 **moderate** (the relationship reuses the mechanism's "one shared cause" but adds the prior finding as a second input and makes a distinct claim). Distinct piece.
- E1→E4 **strong** (the fix targets the named cause — invoke the 3-Pass).
- E2→E4 **moderate** (the fix must deliver BOTH gates; the re-test of the prior's fix-commitment informs the fix's relation to the post-draft check).
- {E1,E2,E3,E6}→{E4} **weak** — the diagnosis + reconciliation + verdict are complete without the fix; the fix is the *reach*. **Dominant boundary (OUTPUT-REACH valley).**
- E5 is a **scope gate**, weakly coupled to content, governing whether E4 is in final scope.

**Clusters → pieces:** A = {E1,E3} diagnosis-core + locus-verdict · B = {E2,E6} prior-reconciliation + commitment-re-test · C = {E4} fix-design · D = {E5} reach gate.

## Step 2 — Boundaries (top-down)
Lowest-coupling cut = the **OUTPUT-REACH valley** between the diagnosis {A,B} and the fix {C}, gated by D. Secondary cuts: A|B (mechanism-and-its-verdict vs the-prior-relationship-and-its-re-test).

## Step 3 — Boundary validation (bottom-up)
Irreducible atoms: {the mechanism}, {the verdict on the user's hypothesis}, {the prior-relationship claim}, {the prior's re-tested commitments}, {the fix design-space}, {the reach choice}. These group exactly into A (mechanism+verdict), B (relationship+re-test), C (fix), D (reach) — top-down and bottom-up **agree** → HIGH confidence. No atom split; none wrongly merged. (Four pieces, not six as in the prior inquiry — correct: this diagnosis is a *single unified mechanism*, so its core is one piece, not three; forcing more would be Over-Decomposition, failure mode #5.)

## Step 4 — Question Tree

**Q-A — Diagnosis Core + Locus Verdict** *(always in scope):* By what structural mechanism are the errors possible, and is the user's hypothesis right?
- [ ] mechanism grounded in literal SKILL text: `SKILL.md` Step 5 imports `harmony_layer.md` only as "the harmony-layer Tier 1-4 preservation policy" (line 78); the 3-Pass sits at the top of that file, un-invoked; the two-step principle is `translation_principals.md` line 16
- [ ] the method-vs-policy **double-service** named (the file is both a 3-Pass METHOD and a Tier-list POLICY; the workflow wires only the policy)
- [ ] the **workflow-contradicts-stated-method** internal inconsistency shown (definitional gap: the SKILL's stated method ≠ its executed workflow)
- [ ] verdict = **instinct-right / locus-refined**, with the two wrong loci explicitly excluded on evidence: NOT "missing principle" (stated in ≥3 files); NOT "harmony given priority over semantics" (harmony designed subordinate — Pass-3 "HOW not WHAT"; Tier-1 "meaning IS carried by this harmony")

**Q-B — Prior-Finding Reconciliation + Commitment Re-test** *(always — this refines a prior):* How does this relate to the prior finding, and do the prior's commitments survive re-test?
- [ ] relationship named **REFINES + COMPLEMENTS** (not supersedes), with the **one-cause-two-gates** structure (Step 5 doesn't run the 3-Pass → both the meaning-lock-FIRST gate AND the post-draft-check gate are missing)
- [ ] the prior's load-bearing commitments listed + each re-tested with a verdict: (i) "the errors = one fluency-first pass with no post-draft checkpoint" → **REFRAMED** (the no-checkpoint is one of TWO gates from one deeper cause, not the whole cause); (ii) "blame co-equal (application-failure + tool-deficiency)" → re-test; (iii) "fix = a scoped post-draft verification pass" → **REFRAMED** (that pass = Pass-3's gate; one wiring of the 3-Pass delivers it plus the meaning-lock gate)
- [ ] each verdict cites evidence, not assertion (guards against silent inheritance)

**Q-C — Fix-Design** *(gated by D):* How should the workflow change so the 3-Pass method is actually executed, meaning-first?
- [ ] names the edit locus: invoke the 3-Pass as ordered phases in `SKILL.md` Step 5 (and/or restructure `harmony_layer.md` so the method can't be imported without the ordering)
- [ ] addresses the **intermediate-artifact** question: does Meaning Lock produce a real locked-meaning intermediate the harmony pass respects, or is it a simulated in-one-pass sequence? (a runtime-determination-shaped sub-question)
- [ ] respects the **granularity caveat**: Pass-1 is a transient "accurate but choppy" foundation; Pass-3 harmonizes supra-sententially without changing meaning; checks stay word/clause-granular (don't force sentence-chopping)
- [ ] **additive** — preserves existing declarative content; relates the fix to the prior's post-draft check (one wiring delivers both gates)

**Q-D — Reach Gate** *(scope decision, surfaced to user):* How far past diagnosis should the deliverable travel?
- [ ] the three reaches named (diagnose-only / +fix-design / +apply-the-SKILL-edit)
- [ ] default recommended (A+B always delivered; C offered for user authorization)
- [ ] gate presented to the user, not silently resolved

## Step 5 — Interface Map

| Source | Target | What flows | Dir |
|---|---|---|---|
| surfacing (decisive locations) | A, C | the literal SKILL text (Step 5 line 78; 3-Pass; line 16) — prerequisite | → |
| sensemaking (stabilized model) | A | the mechanism + resolved forks (prerequisite) | → |
| A | B | the "one shared cause" (mechanism) the relationship builds on (data) | → |
| A | C | the named cause the fix targets (dependency) | → |
| prior finding | B | the 3 commitments to re-test (data) | → |
| B | C | the one-cause-two-gates structure + the re-test of the prior's fix (dependency — the fix must deliver both gates) | → |
| D | C | reach authorization (gate) | → |

**Assumptions-not-data check (hidden-coupling guard):** C assumes A's mechanism is right (explicit, A's output) AND that a workflow insertion point exists (explicit, from surfacing: SKILL.md Step 5). B assumes the prior finding's commitments are as stated (explicit — the prior finding is a named input). C assumes B's "one wiring delivers both gates" (explicit B→C interface). No unstated format/timing/state assumptions across boundaries. **No hidden coupling.**

## Step 6 — Dependency Order

1. **surfacing + sensemaking** (done) → the mechanism + the decisive locations.
2. **A** (mechanism + verdict) — first.
3. **B** (prior reconciliation + re-test) — after A (needs the mechanism + the prior finding).
4. **D** (reach gate) — decidable early; its resolution GATES C. Default: deliver A+B, offer C.
5. **C** (fix-design) — after A+B, if D authorizes.

No circular dependencies. For the pipeline: **Innovation** works the fix design-space (C) + any non-obvious reframings (esp. the intermediate-artifact and harmony_layer-restructure sub-questions); **Critique** adjudicates C + stress-tests A/B (esp. the refines-not-supersedes claim and the "one wiring delivers both gates" claim); **CONCLUDE** delivers A+B (including the Inherited Commitments Re-test) and presents D with C as the offered reach.

## Step 7 — Self-Evaluation

| Dimension | Verdict | Note |
|---|---|---|
| **Independence** | PASS | each Q answerable via defined interfaces alone (A from mechanism+text; B from A's cause + the prior; C from A+B+surfacing; D standalone) |
| **Completeness** | PASS | user's question = A (how possible + was I right) + C (how to fix); B (prior relationship + re-test) required because this refines a prior; D (reach). Nothing falls through |
| **Reassembly** | PASS | A+B (+D-gated C) + interfaces = the full diagnostic-plus-fix with preserved reach and the prior-commitment re-test |
| **Determination-mechanism** | PASS | the runtime-determination-shaped sub-question ("is the meaning-lock intermediate real or simulated?") lives inside C's verification — not presupposed without a piece |
| **Tractability** | PASS | each piece = one focused pass |
| **Interface clarity** | PASS | all flows explicit; assumptions checked; no hidden coupling |
| **Balance** | PASS | A and C carry the weight (appropriate — the mechanism IS the answer, the fix IS the reach); B moderate; D light. Not one-piece-80% |
| **Confidence** | HIGH | top-down (OUTPUT-REACH valley) and bottom-up (6 atoms → 4 pieces) agree |

**Decomposition version:** DV1 (single pass sufficient). No hidden coupling or wrong-boundary signals → no DV2 trigger. **CONCLUDE flag:** because Q-B re-tests a prior finding's ≥3 commitments, CONCLUDE must include the `## Inherited Commitments Re-test` section (the finding's frontmatter will carry `refines:` the prior).
