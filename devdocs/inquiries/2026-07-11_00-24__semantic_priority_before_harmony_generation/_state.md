# State: Semantic Priority Before Harmony Generation

## Flow-type
articulated-surfacing-routed

## Pipeline
A → Su → W → S → D → I → C → R (always)

## Progress
- [x] Articulate-Simple
- [x] Surfacing
- [x] Warm
- [x] Sensemaking
- [x] Decomposition
- [x] Innovation
- [x] Critique
- [x] Routelister

## Warm Loop
round: 0/2 | last-anchor: ordering-present-as-principle-absent-as-procedure (fixpoint) | status: settled

## Iteration
1

## Status
COMPLETE

## Next Discipline
—

## Relationships
- CONTINUES FROM: devdocs/inquiries/2026-07-10_23-03__translation_error_root_cause_skill_adherence/ (prior finding diagnosed the 7 errors as one fluency-first pass with no post-draft checkpoint; this inquiry tests a deeper/different hypothesis — missing semantic-comprehension-priority ordering — and must resolve whether it refines / supersedes / complements that finding)
- RELATED: devdocs/inquiries/2026-07-10_23-03__translation_error_root_cause_skill_adherence/finding.md (its commitments are re-tested here, not inherited)

## History
- 2026-07-11_01-05: CONCLUDE complete. Status COMPLETE. Answer: the errors are possible because the SKILL's translate step (SKILL.md Step 5) never runs the three-pass method its harmony reference file describes — it imports that file only as a Tier-1-4 preservation policy (line 78), leaving the method read-but-un-proceduralized; so meaning-first ordering is present-as-principle (≥3 files) but absent-as-procedure, and meaning + harmony collapse into one fluent motion. User's instinct RIGHT, locus REFINED (not missing-principle; not harmony-over-semantics — designed-subordinate + executed-co-equal). REFINES the prior finding with an explicit demotion (its "no post-draft checkpoint" = one of two gates downstream of the one cause). Fix = wire the three-pass method into Step 5 meaning-first + separate the harmony file's method from its policy; one architecture hosts both gates (sentence-granular config-blind Pass-1 IR; Pass-3 diff against it). Wrote finding.md (refines: frontmatter; Inherited Commitments Re-test of the prior's 4 commitments — 2 confirmed, 2 confirmed-but-frame-revised, none dropped). Archived 7 discipline files to docarchive/; kept _branch.md, _state.md, _route.md, finding.md, routelister.md in root. OUTPUT-REACH gate surfaced to the user (diagnose-only / +fix-design / +apply).
- 2026-07-11_01-01: Routelister complete (exhaust step). Mode root/project-space (breadth), fresh. 8 onward routes enumerated: R1 implement-the-IR-spine (DEVELOP, HIGH, core·@fix-reach), R2 validate-by-retranslating-the-chunk (TEST, HIGH, core·@fix-reach), R3 real-vs-simulated-empirical-comparison (TEST, MED), R4 update-prior-finding-with-demotion-note (REFINE, MED), R5 consolidate-diagnosis→procedure-gap-lesson-to-memory (CONSOLIDATE, MED), R6 branch-spec-the-sentence-granular-IR (PURSUE-SEED, MED), R7 branch-harmony_layer-method/policy-restructure (DEVELOP, MED), R8 diagnose-other-double-serving-reference-files (DIAGNOSE, LOW). Excluded: the OUTPUT-REACH decision (user disposition/scope-gate, not a concept-route — governs R1/R2 @fix-reach coreness) + the unsurfaced-error-population scan (belongs to the prior inquiry's field). Wrote routelister.md + _route.md, both in root (neither archived). No LAYER 1/2 flags. PROCEED. Next: CONCLUDE (must load protocols/conclude.md; write finding.md with the Inherited Commitments Re-test of the prior's 3 commitments [refines: frontmatter]; archive the 7 discipline files to docarchive/ keeping routelister.md/_route.md/_branch.md/_state.md in root; present the OUTPUT-REACH gate to the user).
- 2026-07-11_00-57: Critique complete. TERMINATE with ranked survivors; PROCEED. External grounding VALIDATED (read the actual files, quoted verbatim): SKILL.md line 73 "Then produce the translation. Apply:" + line 78 imports harmony_layer.md ONLY as "the harmony-layer Tier 1-4 preservation policy" + Rule 1 line 87 "Always read all reference files" (→ 3-Pass is IN-CONTEXT, not unseen); harmony_layer.md line 7 "The idea is to create a mode…" (descriptive), line 9 "Translate every SENTENCE" + line 17 "Splitting one sentence into two is forbidden", line 13 "HOW not WHAT". 8 weighted dims; frame-premise + project-risk + external-anchor all constructed. Verdicts: CAND-1 diagnosis SURVIVE+REFINE (sharpen "orphaned/never invoked" → "in-context but UN-PROCEDURALIZED" — Rule 1 forces reading the file; the fix's target is execution, not awareness). CAND-2 relationship SURVIVE+REFINE (sharpest): keep REFINES (fix survives as Pass-3's gate; prior's description not false) BUT add an explicit DEMOTION note — the prior's "no post-draft checkpoint" drops from ROOT-CAUSE to one of TWO proximate gates, both downstream of the one root cause; replace flat "co-equal/two faces" with "two temporally-distinct gates sharing ONE IR artifact (generation-time lock + verification-time diff), of which the prior saw only the verification face." CAND-3 IR-spine SURVIVE+REFINE: "one wiring delivers both gates" → "one architecture HOSTS two distinct gates; the Pass-3 diff is still distinct work"; the IR RELOCATES generation to an error-resistant mode, does NOT "force separation" (overclaim) nor eliminate mis-comprehension; KEY: the rubber-stamp objection VINDICATES the two-gate design (meaning-lock gate necessary-not-sufficient → post-draft gate load-bearing, not redundant). CAND-4 config-blind Pass-1 REFINE: implementable only robustly in the REAL separate-context variant; in one-context simulated it's "as ignorable as any instruction" → config-blindness is COUPLED to real-vs-simulated (2nd reason to prefer real; mild demotion of the DEFERRED simulated variant). CAND-5 descriptive→imperative REPAIR SURVIVE (load-bearing, not nitpicking; shape conditional — REPAIR if invocation thin, REORGANIZE if Step 5 spells out steps). NEW FINDING (granularity, source-grounded): innovation's "word/clause IR" is WRONG — Pass-1 is SENTENCE-granular (line 9) and splitting is forbidden (line 17); innovation imported the word/clause grain from the prior inquiry's CHECK grain and mis-applied it to the IR's form. REFINE: IR sentence-granular; word/clause belongs to the CHECK. Diagnosis-2c "designed subordinate": SURVIVE with mandated BOTH-HALVES framing (designed-subordinate [intent, true, lines 13/33] + executed-co-equal [un-proceduralized passes let fluency capture]) = same co-equal shape as inquiry 1 (SKILL-deficiency + execution-collapse); neither SKILL author nor AI let off. Structural check PASS. Next: Routelister (exhaust step — sweep the finished artifacts, enumerate the onward route-field).
- 2026-07-11_00-51: Innovation complete. Full coverage (4G+3F). Seed = the wiring gap; methodology-mode Standard-default (Contrarian concern routed to the Inherited Frame Audit). CONVERGENCE (Combination + Domain-Transfer[compiler-IR] + Inversion[system-level] + Absence[redesign]) → the **Meaning-Lock IR spine**: (1) restructure harmony_layer.md — separate METHOD (3-Pass) from POLICY (Tier list) AND rewrite the 3-Pass descriptive→imperative; (2) rewire SKILL.md Step 5 from flat apply-bag → the 3-Pass ordered spine (Pass-1 Meaning-Lock config-BLIND, emits an accurate-but-choppy word/clause IR → Pass-2 Harmony Map → Pass-3 Target Reconstruction, meaning held to the IR, prior inquiry's omission-diff runs here against the IR = the UNIFICATION). 3 ACTIONABLE (IR-spine; **Pass-1 config-blind**; the descriptive→imperative REPAIR of harmony_layer.md). Inherited Frame Audit didn't fire at firing-condition (assumption was challenged by F1-contrarian/F2-L2/F3-REMOVE) but tie-breaker surfaced the load-bearing correction: "invoke the existing 3-Pass" is under-specified — the 3-Pass is itself descriptive prose ("the idea is to create a mode…"), so the fix touches BOTH files and includes a semantic rewrite, not a one-line pointer (corrects the "cheapest possible" reading). DEFERRED: simulated-intermediate+attestation (revival: if real-IR emission too costly ×3 runs); schemas.py stage field (revival: if prose-only ordering fails to fire). RESEARCH FRONTIER: real-vs-simulated is empirical (needs matched test translations). Intervention-Shape-Axis Inversion satisfied (REPAIR+REORGANIZE vs ADD-CONTENT/ADD-TEST — REPAIR dominates because it removes the un-gated path). Axis coverage 5/5. Structural check PASS. Next: Critique (adjudicate the IR-spine; stress-test refines-not-supersedes + one-wiring-delivers-both-gates + the config-blind-Pass-1 claim + real-vs-simulated).
- 2026-07-11_00-44: Decomposition complete. DV1; 4 pieces (A diagnosis-core+locus-verdict · B prior-reconciliation+commitment-re-test · C fix-design · D reach-gate). Boundaries top-down (OUTPUT-REACH valley) + bottom-up (6 atoms→4 pieces) AGREE, HIGH conf; no hidden coupling. Fewer pieces than the prior inquiry (6) by design — this is a single unified mechanism, so its core is one piece (forcing more = Over-Decomposition). A→B always-delivered; D gates C. Determination-mechanism check PASS (the "intermediate-artifact real-vs-simulated" sub-question lives in C). CONCLUDE flagged: Q-B re-tests the prior finding's ≥3 commitments → finding needs frontmatter refines: + an Inherited Commitments Re-test section. Self-eval 7/7 PASS. Next: Innovation (works fix design-space C + the intermediate-artifact / harmony_layer-restructure sub-questions).
- 2026-07-11_00-41: Sensemaking complete. SV1→SV6 stabilized. MODEL: the errors are possible because the SKILL's executed workflow CONTRADICTS its stated method — harmony_layer.md's 3-Pass (Meaning Lock = "the foundation that cannot be violated") is orphaned; SKILL.md Step 5 imports that file ONLY as "the harmony-layer Tier 1-4 preservation policy" (line 78), never invoking the method. harmony_layer.md double-serves as METHOD (3-Pass) + POLICY (Tier list); the workflow wires only the policy → no locked-meaning foundation is produced → meaning + harmony collapse into one fluent motion → fluency captures word-choices. FORK 1 resolved: REFINES+COMPLEMENTS the prior (HIGH) — one shared cause (Step 5 doesn't run the 3-Pass as gates) → two missing gates (meaning-lock-FIRST + post-draft-check); prior's fix survives as Pass-3's gate, so NOT superseded. FORK 2 resolved: locus = un-invoked PROCEDURE (HIGH) — not missing-principle (≥3 files), not harmony-over-semantics (designed subordinate). Rival (a) killed: semantic-priority adds a locus + deeper cause + cheaper fix, not fluency-bias re-told. Rival (b) resolved: fix viable — Pass-1 is a TRANSIENT "accurate but choppy" foundation, Pass-3 harmonizes supra-sententially; caveat = keep checks word/clause-granular + real-vs-simulated intermediate open. Eliminated 5 rivals. Structural check PASS. Next: Decomposition.
- 2026-07-11_00-36: Warm complete. MED-FLAG. Re-anchor settled at FIXPOINT round 0 (0 re-surfaces) — anchor = present-as-principle/absent-as-procedure gap, confirmed+concretized (not moved). MQ4 re-ran (added principle-layer exclusion: fix is NOT authoring the principle from scratch — it exists; gap is purely procedure/enforcement). MQ1 sharpened toward REFINES/COMPLEMENTS prior (away from supersedes). MQA reconciled onto "one structural cause → one structural fix" (Step 5 doesn't run the 3-Pass → wire it in, addresses both missing-pre-ordering AND missing-post-check). Non-severe content-conflict fired: the user's INSTINCT is affirmed (semantic priority wasn't operative; deeper than "no checkpoint") but the LOCUS is refined — not "instructions missing" (principle present in ≥3 files) and not "harmony without semantic priority" (harmony is designed subordinate); it's UNENFORCED-PROCEDURE. Conflict-block gate: PROCEED. Structural check PASS. Next: Sensemaking.
- 2026-07-11_00-33: Surfacing complete. 16 items / 8 regions (9 core). DECISIVE result: the comprehend-first/semantic-priority ordering is NOT missing as PRINCIPLE — stated in ≥3 places (harmony_layer.md Pass-1 "Meaning Lock = foundation that cannot be violated"; Pass-3 "change HOW not WHAT"; translation_principals.md line 16 "two-step process: comprehend first, then validate"). It IS missing as enforced PROCEDURE: SKILL.md Step 5 flat-applies everything ("produce the translation. Apply: [bag]") and imports harmony_layer.md ONLY for its Tier 1-4 list — the 3-Pass procedure is orphaned, never invoked; PipelineConfig has no stage ordering. Fork resolved → PRESENT-AS-PRINCIPLE, ABSENT-AS-PROCEDURE. FF2: user's "harmony without semantic priority" is right-instinct/wrong-locus (harmony_layer DECLARES priority; the procedure just isn't run). FF3: prior "no post-draft checkpoint" + this "no enforced ordering" = two faces of one cause (Step 5 doesn't run the 3-Pass as steps). Structural check PASS. Next: Warm.
- 2026-07-11_00-28: Created. Question: how are the errors possible if harmony should sit on top of a locked semantic understanding — is the SKILL missing an explicit comprehend-first-then-generate-under-harmony ordering, and how to fix it? Articulation: HIGH-PROCEED (Itemize count = 1; flagged: none). Layer Commitment = PROCESS (ordering with semantic priority). Two preserved forks: this-vs-prior relationship (refines/supersedes/complements); ordering MISSING vs PRESENT-BUT-UNENFORCED (Meaning Lock is literally Pass 1 of harmony_layer.md). Next: Surfacing.
