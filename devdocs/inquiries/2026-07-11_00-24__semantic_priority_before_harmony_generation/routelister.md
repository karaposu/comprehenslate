# Routelister — Onward Route-Field: Semantic-Priority Diagnosis + IR-Spine Fix

## User Input

Territory = this inquiry's finished artifacts (`_branch.md`, `articulate_simple.md`, `surfacing.md`, `articulate_warm.md`, `sensemaking.md`, `decomposition.md`, `innovation.md`, `critique.md`, `_state.md`). Entry point: fresh. Goal (from `_branch.md`): a diagnostic-plus-fix understanding — WHY the 7 errors are possible if harmony should sit atop a locked semantic understanding, whether the user's "missing semantic-priority ordering" hypothesis is right, and how to fix it; fix gated by the OUTPUT-REACH axis. Stabilized content to route FROM: (1) DIAGNOSIS — Step 5 imports harmony_layer.md only as its Tier policy (line 78), never proceduralizing the in-context-but-un-proceduralized 3-Pass method; REFINES+COMPLEMENTS the prior with an explicit DEMOTION (prior's "no checkpoint" = one of two proximate gates downstream of the one root cause, sharing one IR artifact). (2) FIX — the ACTIONABLE Meaning-Lock IR spine (restructure harmony_layer.md method/policy + descriptive→imperative; rewire Step 5 to the 3-Pass spine; sentence-granular config-blind IR; Pass-3 diff against the IR); config-blind coupled to real-vs-simulated; DEFERRED simulated + schema field; RESEARCH FRONTIER real-vs-simulated (empirical). (3) OUTPUT-REACH fork (piece D) open for the user. Enumerate the typed prescriptive onward route-field; do not choose. Write `routelister.md` + `_route.md` to the inquiry root.

---

## Map Header

- **Identities enumerated:** 8 routes (5 epistemic · 3 teleological)
- **High-priority:** 2 (R1 implement · R2 validate)
- **Essential-count (`core`):** 2 — both `core · @fix-reach` (activate only if the OUTPUT-REACH gate authorizes the +apply / +validate reach)
- **Excluded:** 2 (the OUTPUT-REACH scope-gate itself; the unsurfaced-error-population scan — belongs to the prior inquiry's field)

## Route Index

| # | Direction | Engagement | Priority | Essentiality | ✓ |
|---|---|---|---|---|---|
| R1 | Implement the Meaning-Lock IR spine in the SKILL | DEVELOP | HIGH | core · @fix-reach |  |
| R2 | Validate the fix by re-translating the İKİNCİ HÜCCET chunk | TEST | HIGH | core · @fix-reach |  |
| R3 | Real-vs-simulated empirical comparison (matched translations) | TEST | MED | supporting |  |
| R4 | Update the prior finding with the demotion note | REFINE | MED | supporting |  |
| R5 | Consolidate the diagnosis→procedure-gap lesson into memory | CONSOLIDATE | MED | supporting |  |
| R6 | Branch: the sentence-granular Pass-1 IR spec | PURSUE-SEED | MED | supporting |  |
| R7 | Branch: the harmony_layer.md method/policy restructure design | DEVELOP | MED | supporting |  |
| R8 | Diagnose other double-serving reference files | DIAGNOSE | LOW | peripheral |  |

---

## Per-Route Records

### R1 — Implement the Meaning-Lock IR spine in the SKILL
- **Goal:** the fix, actually applied — Step 5 executes the 3-Pass meaning-first
- **engagement-type:** DEVELOP *(teleological)*
- **Move:** rewire `SKILL.md` Step 5 from the flat "produce the translation. Apply: [bag]" into the 3-Pass ordered spine, and restructure `harmony_layer.md` so its method is separable and imperative
- **Lands:** Step 5 runs Pass-1 Meaning Lock (config-blind, emits a sentence-granular accurate-but-choppy IR) → Pass-2 Harmony Map → Pass-3 Target Reconstruction (meaning held to the IR; the omission-diff runs here) — one architecture hosting both gates
- **Touches:** `SKILL.md` Step 5 (line 73 "produce the translation. Apply:" → the ordered spine) · `SKILL.md` line 78 (the import that currently pulls only the Tier policy → invoke the method too) · `harmony_layer.md` lines 7-13 (the 3-Pass, descriptive→imperative + split METHOD from the Tier POLICY)
- **WHY:** wires the SKILL's own already-in-context method into its executable workflow, so the end goal gains the meaning-first execution that prevents the 7 errors at generation rather than only catching them after
- **Priority:** HIGH  **Confidence:** MED  **Essentiality:** core · @fix-reach
- **Guidance Mode:** full
  - "invoke the existing 3-Pass rather than authoring new machinery" *(bc it is present-as-principle in ≥3 files — promotion, not authoring)*
  - "keep the Pass-1 IR sentence-granular, not word/clause" *(bc harmony_layer.md line 9 says 'translate every sentence' and line 17 forbids splitting sentences — the word/clause grain belongs to the CHECK, not the IR)*
  - "prefer the real separate-context intermediate over the simulated one-context sequence" *(bc config-blindness and rubber-stamp-resistance are both robust only when the config literally isn't in Pass-1's context)*
  - **Meaning-gaps:**
    - real-vs-simulated intermediate — **high** — the whole fix's robustness turns on it; guessing wrong risks re-collapse (resolve via R3 before or alongside build)
    - where the imperative mood lives (Step 5 vs harmony_layer.md) — **mid** — decides whether the harmony_layer edit is REPAIR or REORGANIZE (per critique CAND-5)
    - exact Pass-3 diff operationalization — **mid** — the post-draft gate's word/clause diff against the IR needs a concrete procedure (inherits the prior inquiry's design)
- **Depth-link:** none (not yet drilled) — R7 is its method/policy-restructure sub-design

### R2 — Validate the fix by re-translating the İKİNCİ HÜCCET chunk
- **Goal:** evidence that the wired 3-Pass actually removes the 7-error signature
- **engagement-type:** TEST *(epistemic)*
- **Move:** re-translate the same İKİNCİ HÜCCET passage under the same config (A1=conversational … A5=lightly-domesticated …) but with the IR-spine wired, and check for the 7 named errors
- **Lands:** a pass/fail signal on the fix — do "allegorical", the dropped "basit avamın fehmine gelecek" clause, "work" for *iş*, the dense construction, etc. recur or not?
- **WHY:** the fix's claim ("meaning-first execution prevents these errors") is unproven until run against the very passage that produced them, so the end goal gains confirmation-or-refutation rather than a designed-but-untested remedy
- **Priority:** HIGH  **Confidence:** MED  **Essentiality:** core · @fix-reach
- **Guidance Mode:** compact
  - "diff the new output against both the source AND the prior errored output" *(bc the prior output is the concrete baseline the 7 errors were named against)*
  - "hold config constant — only the wiring changes" *(bc a config change would confound the fix's effect)*
- **Depth-link:** none

### R3 — Real-vs-simulated empirical comparison (matched translations)
- **Goal:** resolve whether the meaning-lock intermediate must be a real emitted artifact or a simulated in-context sequence
- **engagement-type:** TEST *(epistemic)*
- **Move:** translate matched chunks two ways — one emitting a real config-blind IR then reconstructing from it, one running the passes as a simulated single-context sequence — and compare for the 7-error signature
- **Lands:** an evidence-based verdict on the RESEARCH FRONTIER, promoting or killing the DEFERRED simulated variant
- **WHY:** the critique found config-blindness is *coupled* to this axis (robust only in the real variant), so the end goal gains a grounded default instead of an assumption
- **Priority:** MED  **Confidence:** LOW  **Essentiality:** supporting
- **Guidance Mode:** compact
  - "this can run before R1 to de-risk the build, or after as validation" *(bc R1's highest meaning-gap is exactly this axis)*
- **Depth-link:** none

### R4 — Update the prior finding with the demotion note
- **Goal:** coherence across the two findings — the prior located precisely, not overwritten
- **engagement-type:** REFINE *(epistemic)*
- **Move:** annotate `2026-07-10_23-03__…/finding.md` that its "one fluency-first pass, no post-draft checkpoint" is demoted from root-cause to one of two proximate gates, both downstream of the single root cause (the un-proceduralized 3-Pass)
- **Lands:** the prior finding carries a forward-pointer to this inquiry's deeper cause; neither finding contradicts the other
- **WHY:** this inquiry REFINES (not supersedes) the prior, so the end goal gains a consistent two-finding record where the prior's remedy survives as Pass-3's gate and its causal claim is precisely re-scoped
- **Priority:** MED  **Confidence:** HIGH  **Essentiality:** supporting
- **Guidance Mode:** compact
  - "annotate, don't rewrite — the prior's description isn't false, just re-located" *(bc supersede would wrongly discard a surviving remedy)*
- **Depth-link:** none

### R5 — Consolidate the diagnosis→procedure-gap lesson into memory
- **Goal:** a reusable lesson so the failure class is caught earlier next time
- **engagement-type:** CONSOLIDATE *(epistemic)*
- **Move:** write a memory capturing the general pattern — *a SKILL can state a method as principle yet never proceduralize it into its executable workflow; a described-but-un-invoked method runs "in spirit as one motion" and fails silently*
- **Lands:** the persistent memory gains a check-time heuristic ("verify stated methods are wired as executable steps, not just imported as policy")
- **WHY:** the pattern generalizes past this SKILL (any declarative-rich/procedurally-thin spec), so the end goal gains prevention-at-authoring, not just this one repair — links [[feedback-translation-verification-pass]]
- **Priority:** MED  **Confidence:** HIGH  **Essentiality:** supporting
- **Guidance Mode:** compact
  - "frame it as a spec-authoring check, not a translation rule" *(bc the lesson is about method-proceduralization, applicable to any SKILL)*
  - **Meaning-gaps:**
    - scope of the lesson (this SKILL vs all specs) — **low** — safely stated general; refinable if counter-cases appear
- **Depth-link:** none

### R6 — Branch: the sentence-granular Pass-1 IR spec
- **Goal:** a precise specification of what the Meaning-Lock IR *is*
- **engagement-type:** PURSUE-SEED *(teleological)*
- **Move:** open a focused inquiry on the IR's exact form — sentence-granular, config-blind, "accurate but choppy" — how literal, how it represents polysemy/register decisions deferred to Pass-3, how omission-visibility is achieved without sub-sentence splitting
- **Lands:** a buildable IR spec that R1 can implement without re-deriving the granularity question
- **WHY:** the critique corrected the grain but left the full spec open, so the end goal gains a de-risked R1 (its second-highest meaning-gap closed)
- **Priority:** MED  **Confidence:** LOW  **Essentiality:** supporting
- **Guidance Mode:** compact
  - "anchor on harmony_layer.md line 9 ('translate every sentence') + line 17 (no splitting)" *(bc those are the literal constraints the IR must satisfy)*
- **Depth-link:** none

### R7 — Branch: the harmony_layer.md method/policy restructure design
- **Goal:** a restructure so the 3-Pass method can't be imported without its ordering
- **engagement-type:** DEVELOP *(teleological)*
- **Move:** design the concrete split of `harmony_layer.md` into a METHOD section/file (the imperative 3-Pass) and a POLICY section/file (the Tier 1-4 list), so Step 5's import must name which it pulls
- **Lands:** a restructure spec that makes the double-service impossible — the wiring can't pull the policy while orphaning the method
- **WHY:** the double-service is the structural root that let Step 5 wire only the policy, so the end goal gains a fix at the root, not just at the call-site — prevents recurrence
- **Priority:** MED  **Confidence:** MED  **Essentiality:** supporting
- **Guidance Mode:** compact
  - "this is R1's sub-design — can fold into R1 or run standalone" *(bc R1 already includes the restructure; a separate inquiry only if the split is contentious)*
  - **Meaning-gaps:**
    - one-file-two-sections vs two-files — **mid** — affects how the import reference reads and whether accidental policy-only import stays possible
- **Depth-link:** none

### R8 — Diagnose other double-serving reference files
- **Goal:** know whether the orphaned-method pattern is isolated or systemic
- **engagement-type:** DIAGNOSE *(epistemic)*
- **Move:** scan the other reference files (`advanced_principles.md`, `translation_principals.md`, `notes.md`) for methods/procedures that Step 5 imports only partially (as WHAT-to-preserve content, never as executable steps)
- **Lands:** a verdict on whether harmony_layer.md is the only orphaned-method file or one of several
- **WHY:** if systemic, the fix generalizes beyond the 3-Pass, so the end goal gains scope-awareness — but it is off the critical path to *this* fix
- **Priority:** LOW  **Confidence:** LOW  **Essentiality:** peripheral
- **Guidance Mode:** compact
  - "look for the same signature: a procedure described in a reference file that Step 5's flat apply-bag never invokes as steps" *(bc that is the diagnostic marker this inquiry established)*
- **Depth-link:** none

---

## Excluded (candidate-concepts considered, not routed — with reasons)

- **The OUTPUT-REACH decision (diagnose-only / +fix-design / +apply-the-SKILL-edit).** This is a *disposition / scope decision the user makes*, not a concept-direction — per the NOT-list (§1.3), routelisting perceives salience/coreness as attributes but does not decide disposition or emit control-flow moves. It is not a route; it is the **gate** that activates R1/R2's `core · @fix-reach` coreness. Surfaced to the user by CONCLUDE, not resolved here.
- **Scan the two produced translation chunks for the unsurfaced error population.** A legitimate onward move, but it belongs to the **prior inquiry's route-field** (already enumerated at `2026-07-10_23-03__…/routelister.md`), whose goal was the per-error diagnosis of the produced output. This inquiry's goal is the semantic-priority mechanism + fix; re-routing the error-scan here would be Goal-loss / over-coverage. Noted, not re-routed.

---

## Telemetry

- **Mode:** root / project-space (breadth) · **Entry point:** fresh (no prior `_route.md` in this inquiry folder)
- **Identities enumerated:** 8 · **kind split:** epistemic 5 (R2 TEST, R3 TEST, R4 REFINE, R5 CONSOLIDATE, R8 DIAGNOSE) / teleological 3 (R1 DEVELOP, R6 PURSUE-SEED, R7 DEVELOP)
- **High-priority:** 2 (R1, R2) · **Essential-count:** 2 `core` (both @fix-reach) · supporting 5 · peripheral 1
- **Individuations made:** R1 and R7 kept split (R7 is R1's restructure sub-design, but individuated separately — lean-to-split, mergeable if folded); R2 and R3 kept split (validate-the-fix vs resolve-the-open-axis are different tests); R6 and R1 kept split (spec-the-IR vs build-the-spine)
- **Uncertain individuations flagged:** R1/R7 (foldable) — consumer may merge
- **Stale entries:** none (fresh index)
- **Convergence:** territory swept at identity resolution; a further sweep yields no new goal-relevant identities
- **Frontier flags:** none (territory small + bounded)
- **Failure modes checked:** LAYER 1 — Over-merge (avoided: R1/R7, R2/R3 split not merged), Under-coverage (implement/validate/resolve-open/reconcile-prior/memorialize/branch-seeds/generalize all covered), Wrong-grain (routes are identities, not manifestations), Goal-loss (error-scan excluded as off-goal), Type-misassignment (each engagement-type checked against the membership test), Index-drift (n/a fresh). LAYER 2 — Selection-creep (avoided: no route ranked as "the move"; OUTPUT-REACH disposition excluded), Process-coupling (avoided: no control-flow moves — the reach-gate is excluded as disposition), Description-collapse (routes are prescriptive, not explanatory), Manifestation-dump (compact identity-level map).
- **Self-assessment:** **PROCEED** — territory swept at identity resolution; no LAYER 1 flags; both files written.
