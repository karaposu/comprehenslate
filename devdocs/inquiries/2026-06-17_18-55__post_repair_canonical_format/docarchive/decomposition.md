# Decomposition — post-repair canonical format

## User Input

Substrate: `_branch.md` + `articulate_simple.md` + `surfacing.md` + `sensemaking.md`. SV6 stabilized model: three-layer architecture PRESERVED with canonical-layer format SWAPPED (HTML5 replaces JSON-AST); markdown hand-edit + EPUB 3 publishing unchanged. Decision-mode = REFINE prior canonical-format inquiry. Capability matrix is the load-bearing evidence artifact. NEW provenance + confidence dimension is load-bearing.

---

## Step 1 — Perceive Coupling Topology

Elements of the finding-to-be:

| Element | Role |
|---|---|
| Executive summary | One-paragraph headline answer |
| Architectural commitment + decision-mode + relationship-label (refines:) | The framing-semantic + relationship-label meta-decision |
| Capability matrix | The load-bearing evidence artifact |
| F1 — HTML5 as canonical (NEW) | The substantive new commitment |
| F2 + F3 brief preservation acknowledgment | markdown hand-edit + EPUB 3 publishing inherited unchanged |
| Calibration-corpus HTML5 patterns | Per-policy element-class-data-* conventions |
| Rejected candidates rationale | JSON-AST / monolithic / custom / dual / TEI / RTF / MOBI |
| Inherited Commitments Re-test | Prior canonical-format REFINED + Decisions 2-5 PRESERVED |
| Transition plan + Next Actions | What changes vs preserves; downstream tasks |
| Open Questions / Frontier | Schema profile; class conventions; provenance encoding; HTML-to-md round-trip |

### Coupling gradient

**Strong coupling (must stay together):**
- Architecture commitment + decision-mode + relationship-label (form one meta-decision)
- Each rejection's candidate-name + structural-reason
- Each per-policy HTML5 pattern

**Moderate coupling (boundary candidates):**
- Matrix → F1 (HTML5 spec) — one-way evidence-flow
- Matrix → rejections — one-way evidence-flow
- F1 → calibration patterns — HTML5 conventions determine policy mappings
- Architecture → Inherited Re-test — what's preserved + what swaps drives verdict
- Inherited Re-test → Transition plan — what's refined drives engineering changes

**Weak coupling (clean boundaries):**
- Capability matrix vs F1 (matrix is evidence; F1 is commitment derived from it; cleanly separable)
- F1 (new) vs F2+F3 (preserved) — different commitment-types
- Rejections vs forward-looking pieces — independent rationale
- Executive summary vs everything else — last-step digest

### Coupling clusters

| Cluster | Members |
|---|---|
| **M (meta)** | Architectural commitment + decision-mode + relationship-label |
| **E (evidence)** | Capability matrix |
| **N (new commitment)** | F1 — HTML5 as canonical |
| **P (preserved)** | F2 + F3 brief acknowledgment |
| **A (application)** | Per-policy HTML5 patterns |
| **R (rejections)** | Rejected candidates rationale |
| **I (inheritance)** | Inherited Commitments Re-test |
| **F (forward)** | Transition plan + Next Actions |
| **O (open)** | Open Questions / Frontier |
| **S (summary)** | Executive summary |

Ten clusters; boundaries fall cleanly between them.

---

## Step 2 — Detect Boundaries (Top-Down)

| Boundary | Couplings crossing | Strength |
|---|---|---|
| M ↔ E | Architecture commitment uses matrix as evidence (one-way) | clean |
| E ↔ N | Matrix justifies HTML5 (one-way) | clean |
| N ↔ P | F1 (new) and F2+F3 (preserved) are different content types | clean |
| N ↔ A | HTML5 spec defines policy-mapping conventions | one-way clean |
| E ↔ R | Matrix evidence justifies rejections | one-way clean |
| M + N ↔ I | Architecture preservation + HTML5 swap drive re-test verdict | one-way clean |
| M + N + I ↔ F | What changes + what's preserved drives transition plan | one-way clean |
| N + A + I ↔ O | What's committed determines what's open | one-way clean |
| All ↔ S | Last-step digest | last-step dependency |

### Initial piece set

- **P1**: Executive summary / one-paragraph headline
- **P2**: Architectural commitment + decision-mode + relationship-label (meta-decision)
- **P3**: Capability matrix (evidence artifact)
- **P4**: F1 — HTML5 as canonical (the new commitment)
- **P5**: F2 + F3 brief preservation acknowledgment (markdown hand-edit + EPUB 3 publishing)
- **P6**: Calibration-corpus HTML5 patterns (per-policy mappings)
- **P7**: Rejected candidates rationale
- **P8**: Inherited Commitments Re-test (meta-decision)
- **P9**: Transition plan + Next Actions
- **P10**: Open Questions / Frontier

**10 pieces.** Within target (8-12).

---

## Step 3 — Validate Boundaries (Bottom-Up)

| Piece | Atoms | Predicted cluster | Match? |
|---|---|---|---|
| P1 | "headline answer" | S | ✓ |
| P2 | architecture preservation · canonical SWAP · refines: relationship | M | ✓ |
| P3 | 5 candidates × 18 requirements with per-cell evidence | E | ✓ |
| P4 | HTML5 living-standard spec · semantic markup · lang/dir/class/data-* · Pandoc ↔︎ · schema validation strategy | N | ✓ |
| P5 | F2 markdown preservation reference · F3 EPUB 3 preservation reference + HTML5→EPUB near-identity note | P | ✓ |
| P6 | 7 per-policy HTML5 patterns (hashiye / Mevlana / Bismillah / Arabic spans / voice / archaic / honorifics) | A | ✓ |
| P7 | 7 rejections (JSON-AST / monolithic / custom / dual / TEI / RTF / MOBI) | R | ✓ |
| P8 | Prior canonical-format inquiry's three-format verdict (5 cells re-tested) + original intake-concepts Decisions 2-5 (4 re-tested) | I | ✓ |
| P9 | What changes (1 cell) + what's preserved (architecture + 4 cells + 4 decisions) + MUST/COULD next actions | F | ✓ |
| P10 | 6+ open questions / frontier items | O | ✓ |

No atoms split across boundaries. No atoms grouped together that should be separate. **High-confidence boundaries.**

---

## Step 4 — Question Tree

### P1 — Executive Summary
**Q:** "What is the post-repair canonical format, in one paragraph?"
**Verification criteria:**
- [ ] One-paragraph headline
- [ ] Names HTML5 as canonical with brief rationale
- [ ] Names markdown hand-edit + EPUB 3 publishing as preserved from prior
- [ ] Anchors back to user's literal question ("EPUB? md? html?")
- [ ] Captures `refines:` relationship to prior canonical-format finding

### P2 — Architectural Commitment + Decision-Mode (META-DECISION)
**Q:** "What architectural commitment underwrites the answer, and how does it relate to the prior canonical-format inquiry?"
**Verification criteria:**
- [ ] Three-layer architecture explicitly preserved (intake/translate-canonical + hand-edit + publishing as USE-CASE distinctions)
- [ ] Canonical-layer format SWAP named: was JSON-AST; now HTML5
- [ ] Decision-mode = REFINE (not overturn; not re-affirm-unchanged)
- [ ] `refines: devdocs/inquiries/2026-06-17_17-49__canonical_intake_format_deep_dive/finding.md` relationship label committed in frontmatter
- [ ] NEW evidence that drives the swap named: reconstruction-is-heavy reframe + provenance + confidence dimension + emergent HTML5-as-canonical candidate that wasn't in the prior solution space
- [ ] Why the prior's three-layer architecture STANDS: the three temporal layers are USE-CASE distinctions with different optima; format-per-layer survives because use-case-per-layer survives

### P3 — Capability Matrix (EVIDENCE ARTIFACT)
**Q:** "What does the capability matrix show, and how does it justify the HTML5 canonical choice?"
**Verification criteria:**
- [ ] Matrix populated for 5 candidates (JSON-AST · Pandoc-md · HTML5 · EPUB 3 · custom .compldoc) × the load-bearing requirements (Arabic lang/dir; structure-preservation; apparatus targets; provenance + confidence at A18 + A19; round-trip; human-readability; web-standard; EPUB-publishing-path)
- [ ] Each cell carries native (✓✓) / via-extension (✓+ext) / via-workaround (✓wa) / not-supported (✗) classification
- [ ] Per-cell evidence brief: W3C-spec / Pandoc-fact / EPUB-spec citation
- [ ] HTML5 Pareto-dominance demonstrated for canonical layer
- [ ] The provenance + confidence row (A19) shown as the differentiator that makes markdown insufficient at canonical layer

### P4 — F1: HTML5 as Canonical (THE LOAD-BEARING NEW COMMITMENT)
**Q:** "What is the HTML5 canonical format, in detail?"
**Verification criteria:**
- [ ] W3C HTML5 Living Standard identified
- [ ] Polyglot syntax (valid as both HTML5 and XHTML5) committed
- [ ] Semantic elements used for structure: `<section>`, `<article>`, `<aside>`, `<header>`, `<footer>`, `<figure>`, `<blockquote>`, `<p>`, `<span>`, `<em>`, `<strong>`
- [ ] Attributes used: `lang=`, `dir=`, `class=`, `id=`, `data-*` for per-element provenance + confidence
- [ ] Pandoc ↔︎ HTML5 relationship: `pandoc -f html -t json` and reverse for AST access on-demand; `pandoc -t html5` from any source
- [ ] Schema validation strategy: HTML5 DTD or project RNG schema
- [ ] In-memory IntakeDoc relationship: in-memory shape is the parsed HTML5 DOM (or Pandoc AST, both equivalent); on-disk is HTML5 file
- [ ] Why HTML5 wins for the canonical role: document AND data-shape; native lang/dir/data-*; explicit-tree; W3C standard; Pandoc-native; near-identity to EPUB 3
- [ ] How HTML5 differs from JSON-AST in the user-facing experience: HTML5 is a DOCUMENT (openable in any text editor; renders in any browser); JSON-AST is DATA

### P5 — F2 + F3: Preserved Layers (markdown hand-edit + EPUB 3 publishing)
**Q:** "What are the hand-edit and publishing formats, given the canonical swap?"
**Verification criteria:**
- [ ] F2 (hand-edit) = Pandoc's markdown with the canonical extension set — **PRESERVED unchanged from prior canonical-format finding**
- [ ] Hand-edit workflow updated for HTML5 canonical: user opens markdown → edits → `pandoc -f markdown -t html5` → intake re-loads HTML5
- [ ] F3 (publishing) = EPUB 3 — **PRESERVED from prior**; generated via `pandoc -f html -t epub3` (NEAR-IDENTITY since EPUB IS xhtml + manifest)
- [ ] Reference to prior canonical-format finding for full specs of F2 and F3
- [ ] Brief; this piece is acknowledgment-of-preservation, not re-specification

### P6 — Calibration-corpus HTML5 patterns
**Q:** "How do the calibration corpus's seven policy targets map onto HTML5?"
**Verification criteria:**
- [ ] Per-policy concrete HTML5 pattern (element + class + data-* attributes)
- [ ] **Hashiye (`SourceApparatusPolicy`)**: `<aside class="marginalia" id="..." data-source="..." data-confidence="...">...</aside>` referenced from body via `<a href="#..." class="marginalia-ref">`
- [ ] **Mevlana couplets (`EmbeddedPoetryPolicy`)**: `<figure class="couplet" data-attribution="Mevlana"><blockquote>...</blockquote><figcaption>...</figcaption></figure>`
- [ ] **Bismillah (`FormulaicOpeningPolicy`)**: `<p class="formulaic-opening" data-tradition="islamic">...</p>` with position-at-section-start constraint
- [ ] **Arabic spans (`NonMainLangPartsPolicy`)**: `<span lang="ar" dir="rtl" data-source="ocr" data-confidence="...">...</span>`
- [ ] **Voice transitions (`VoiceMarkingPolicy`)**: `<span class="voice-cited">...</span>` or `<blockquote class="voice-cited" cite="...">...</blockquote>`
- [ ] **Archaic register (`ArchaicRegisterPolicy`)**: `<span class="archaic-register" data-archaism-type="...">...</span>`
- [ ] **Honorifics (`HonorificsPolicy`)**: `<span class="honorific" data-tradition="...">...</span>` attached to preceding name span
- [ ] NFC diacritic normalization noted (applied at intake; HTML5 storage is UTF-8 native)
- [ ] Per-element provenance pattern noted: `data-source` (`"text-layer"` / `"ocr-tesseract"` / `"hand-edit"`) + `data-confidence` (0.0-1.0) + `data-intake-pass` (which repair pass)

### P7 — Rejected Candidates Rationale
**Q:** "What candidates were rejected as canonical, and why?"
**Verification criteria:**
- [ ] **JSON-AST rejected** for canonical: explicit-tree but DATA-not-document; user pattern signals against; HTML5 captures explicit-tree + document properties together. JSON-AST remains on-demand-reachable via `pandoc -f html -t json` without persisting as second canonical.
- [ ] **Monolithic single-format rejected**: three temporal layers have different optima (canonical = lossless+provenance; hand-edit = reading-order-natural; publishing = packaged-book); architecture survives.
- [ ] **Custom format rejected**: HTML5 + class/attribute conventions covers project-specific invariants; custom format reinvents.
- [ ] **Dual JSON-AST + HTML5 rejected**: synchronization debt; one canonical must be authoritative.
- [ ] **TEI rejected**: Pandoc-read absent (per Pandoc format matrix, TEI is output-only as TEI Simple); custom TEI reader breaks architectural lever; verbosity.
- [ ] **RTF rejected**: unchanged from prior (editor-fragility; raw-typography wrong-kind-of-richness); remains as accepted user-provided input via Pandoc.
- [ ] **MOBI rejected**: unchanged from prior (Amazon-deprecated in 2022; Pandoc unsupported).
- [ ] Each rejection cites a specific structural property (not preference)

### P8 — Inherited Commitments Re-test (META-DECISION)
**Q:** "What is the verdict on inherited commitments?"
**Verification criteria:**
- [ ] **Prior canonical-format inquiry's three-format verdict**: REFINED. Three-layer architecture PRESERVED. Canonical-layer format SWAP: JSON-AST → HTML5. Markdown hand-edit + EPUB 3 publishing PRESERVED unchanged.
- [ ] Prior's "Pandoc-AST-as-JSON canonical" rationale re-tested: documented; not invalidated; but new evidence (provenance + confidence dimension + user pattern + emergent HTML5 candidate) makes HTML5 stronger for the role.
- [ ] **Original intake-concepts inquiry's Decisions 2-5 PRESERVED**:
  - Decision 2 (structure-preservation) — preserved and strengthened (HTML5 explicit-tree storage)
  - Decision 3 (IntakeDoc tree+cross-ref-flat shape) — preserved in-memory; on-disk is HTML5
  - Decision 4 (7-policy split) — preserved; detector specs refine to HTML5-element references (semantic refinement, not redesign)
  - Decision 5 (Pandoc + OCR architectural lever) — preserved and strengthened (all three formats Pandoc-native)
- [ ] The relationship label `refines:` is structurally appropriate (architecture preserved; one cell swapped; not OVERTURN; not CORRECT)

### P9 — Transition Plan + Next Actions
**Q:** "What changes from the prior canonical-format finding, and what are the next actions?"
**Verification criteria:**
- [ ] What CHANGES:
  - On-disk canonical: was JSON-AST; now HTML5
  - Per-element provenance + confidence: NEW (added via HTML5 `data-*` attributes)
  - HTML5 → EPUB packaging path: NEW (near-identity transform; replaces the prior's JSON-AST → EPUB Pandoc-render path)
- [ ] What STAYS THE SAME:
  - Three-layer architecture (canonical / hand-edit / publishing)
  - Hand-edit format = Pandoc's markdown
  - Publishing format = EPUB 3
  - In-memory IntakeDoc shape (tree-of-containers + cross-referenced flat collections)
  - The 7 policy-perception detectors (specs refine to AST-or-HTML5-element references)
  - The OCR sub-pipeline
  - Quality target = structure-preservation
  - Pandoc as architectural lever
- [ ] Engineering migration cost: NEAR-ZERO (v0.2 not built; reversal at architecture-commit time)
- [ ] Next-actionable inquiries listed:
  - **MUST 1**: design the HTML5 schema / validation profile (which HTML5 features; project RNG; pydantic layer)
  - **MUST 2**: design per-policy class-attribute conventions (`class="x"` vs `data-policy="x"`; uniform pattern across the 7 policies)
  - **MUST 3**: design per-element provenance encoding pattern (`data-source` + `data-confidence` + `data-intake-pass` exact field names + value vocabularies)
  - **COULD 1**: design the HTML5-to-EPUB packaging script (since HTML5 → EPUB is near-identity, this is minimal — specify manifest + cover + metadata)
  - **COULD 2**: define the HTML5-to-markdown round-trip stability subset for the hand-edit workflow
  - **COULD 3**: prototype on a Risale-i Nur volume (the integration test seed; tests intake → HTML5 → EPUB 3)

### P10 — Open Questions / Frontier
**Q:** "What's open after this finding?"
**Verification criteria:**
- [ ] HTML5 schema profile (which subset; project RNG) — RESOLVABLE in MUST 1
- [ ] Per-policy class-attribute conventions — RESOLVABLE in MUST 2
- [ ] Per-element provenance encoding pattern — RESOLVABLE in MUST 3
- [ ] HTML5-to-markdown round-trip subset definition — RESOLVABLE in COULD 2
- [ ] HTML5-to-EPUB packaging specifics — RESOLVABLE in COULD 1
- [ ] TEI Simple as future archival output (frontier; revival trigger: scholarly archival need)
- [ ] Pandoc version pinning policy (operational; documented in engineering setup)
- [ ] JSON-AST on-demand reachability via `pandoc -f html -t json` (works today; documented as available)
- [ ] Cross-corpus format-architecture validation (frontier; revival trigger: scaling beyond Risale-i Nur)
- [ ] HTML5 living standard version stability (operational concern; pin a version)

---

## Step 5 — Interface Map

| # | Source | Target | What flows | Direction | Coupling type |
|---|---|---|---|---|---|
| I1 | P2 (architecture commit) | P4 (HTML5 spec) | Canonical-layer role definition; HTML5 fills it | one-way | explicit |
| I2 | P2 | P5 (md + EPUB acknowledgment) | Architecture preserves the other two layers | one-way | explicit |
| I3 | P3 (matrix) | P4 (HTML5 spec) | Matrix evidence justifies HTML5 choice | one-way | explicit |
| I4 | P3 (matrix) | P7 (rejections) | Matrix evidence justifies each rejection | one-way | explicit |
| I5 | P4 (HTML5 spec) | P6 (policy patterns) | HTML5 element conventions define policy mappings | one-way | explicit |
| I6 | P2 + P4 | P8 (re-test) | Architecture preservation + HTML5 swap drive prior-verdict = REFINED | one-way | explicit |
| I7 | P2 + P4 + P8 | P9 (transition + next) | What changes + what's preserved drives engineering plan | one-way | explicit |
| I8 | P4 + P6 + P8 + P9 | P10 (open questions) | What's committed determines what's still unfixed | one-way | explicit |
| I9 | P2 + P3 + P4 + P5 + P8 | P1 (exec summary) | Last-step digest | one-way | explicit |
| I10 | EXTERNAL (prior canonical-format finding) | P8 (re-test) | Prior's three-format verdict is the substrate of re-test | one-way | external |
| I11 | EXTERNAL (original intake-concepts finding) | P8 (re-test) | Decisions 2-5 inherited | one-way | external |

**Assumptions-not-data check:**

| Hidden-assumption risk | Check | Result |
|---|---|---|
| P4 assumes the matrix in P3 is correctly populated | P3 cites W3C-spec / Pandoc-fact per cell; P4 cites the matrix as evidence | ✓ explicit |
| P5 assumes the prior canonical-format finding's F2 + F3 specs are still valid | The architecture is preserved at those layers; the prior's specs apply unchanged | ✓ explicit |
| P6 assumes HTML5 conventions can hold all 7 policy targets | P6 provides concrete patterns per policy with cited element + class + attribute choices | ✓ explicit |
| P7 assumes the rejection rationales survive scrutiny | Each rejection cites a specific structural property (not preference) | ✓ explicit |
| P8 assumes the prior finding's commitments are READABLE (file accessible) | The path is documented; the inquiry inherited from it | ✓ external |
| P9 assumes the migration cost is NEAR-ZERO | v0.2 not built; no engineering to migrate | ✓ explicit |

No hidden coupling detected.

---

## Step 6 — Dependency Order

```
Order      Piece                       Depends on               Can parallelize with
─────      ─────                       ──────────               ────────────────────
1a         P2 (architecture)            —                        P3 (matrix)
1b         P3 (capability matrix)       —                        P2
2a         P4 (HTML5 spec)              P2 + P3                  P5, P7
2b         P5 (md + EPUB brief)         P2                       P4, P7
2c         P7 (rejections)              P3                       P4, P5
3a         P6 (policy patterns)         P4                       P8
3b         P8 (re-test)                 P2 + P4                  P6
4          P9 (transition + next)       P2 + P4 + P8             —
5          P10 (open questions)         P4 + P6 + P8 + P9        —
6          P1 (exec summary)            ALL                      LAST
```

**Critical-path:** P2 ‖ P3 → (P4 ‖ P5 ‖ P7) → (P6 ‖ P8) → P9 → P10 → P1.

No circular dependencies.

---

## Step 7 — Self-Evaluation

### Minimum 3-dimension evaluation

| Dimension | Check | Verdict | Note |
|---|---|---|---|
| **Independence** | Can each piece be worked on without others beyond interfaces? | **PASS** | P4/P5/P7 independent of each other given P2+P3; P6/P8 independent given P4; P1+P9+P10 declared as last-step depends. |
| **Completeness** | Pieces cover the finding's whole? | **PASS** | Architecture + matrix + 3 layer commits (one new, two acknowledgments) + per-policy patterns + rejections + inherited re-test + transition + open = comprehensive. |
| **Reassembly** | Pieces + interfaces → coherent finding? | **PASS** | Concatenated in dependency order, the 10 pieces produce a readable `finding.md`. |

### Full 7-dimension evaluation

| Dimension | Check | Verdict |
|---|---|---|
| Independence | (above) | **PASS** |
| Completeness | (above) | **PASS** |
| Reassembly | (above) | **PASS** |
| **Tractability** | Each piece small enough for one focused Innovation pass? | **PASS** — P3 (matrix) and P4 (HTML5 spec) are heaviest but bounded; P5 (md+EPUB brief) is intentionally light (preservation acknowledgment); P6 has 7 per-policy patterns × ~50 words each. |
| **Interface clarity** | All cross-piece flows explicit? | **PASS** — I1-I11 mapped; assumptions-not-data check ran clean. |
| **Balance** | Roughly equal complexity? | **INTENTIONAL IMBALANCE** — P5 is much lighter than P4 by design (P5 is preservation acknowledgment; P4 is the load-bearing new commitment). Acceptable. |
| **Confidence** | Top-down ↔ bottom-up agree? | **HIGH** — atoms group as predicted. |

### Determination-mechanism piece check

Concepts requiring runtime determination:
- "Is the canonical file HTML5 or something else?" — file-extension based; downstream of this finding (format detection).
- "Which Pandoc-md round-trip-stable subset features does the hand-edit workflow guarantee?" — defined in COULD 2 of P9 (named, not defined here).

These mechanisms are NAMED in the finding (P9, P10) but DEFINED downstream. No missing piece in this finding.

### Failure-mode self-scan

- **Premature decomposition:** ✗ Sensemaking ran first; SV6 stabilized.
- **Wrong boundaries:** ✗ Top-down + bottom-up agree.
- **Hidden coupling:** ✗ All flows mapped; assumptions-not-data ran.
- **Missing pieces:** ✗ Completeness check + Determination-mechanism check.
- **Over-decomposition:** ✗ 10 pieces; each substantive.
- **Ignoring dependencies:** ✗ Critical path mapped.
- **Imbalanced decomposition:** ⚠ Intentional (P5 light; documented).

### Self-assessment verdict

**PROCEED to Innovation.** 10 pieces with explicit interfaces and dependency ordering. The capability matrix (P3) and the HTML5 spec (P4) are the load-bearing pieces; the others are bounded support.

---

## Final Deliverable — Summary Table

| # | Piece | Question | Order |
|---|---|---|---|
| P1 | Executive summary | What is the post-repair canonical format? | last |
| P2 | Architectural commitment + decision-mode (META) | What architecture underwrites the answer; how does it relate to prior? | wave 1 |
| P3 | Capability matrix (EVIDENCE) | What does the matrix show; how does it justify HTML5? | wave 1 (parallel with P2) |
| P4 | F1 — HTML5 as canonical (LOAD-BEARING NEW) | What is the HTML5 canonical format in detail? | wave 2 |
| P5 | F2 + F3 preservation acknowledgment | What are the hand-edit and publishing layers (preserved)? | wave 2 (parallel) |
| P6 | Calibration-corpus HTML5 patterns | How do the 7 policy targets map onto HTML5? | wave 3 |
| P7 | Rejected candidates rationale | What was rejected, and why? | wave 2 (parallel) |
| P8 | Inherited Commitments Re-test (META) | What is the verdict on inherited commitments? | wave 3 (parallel) |
| P9 | Transition plan + Next Actions | What changes; what's preserved; what's next? | wave 4 |
| P10 | Open Questions / Frontier | What's open after this finding? | wave 5 |
