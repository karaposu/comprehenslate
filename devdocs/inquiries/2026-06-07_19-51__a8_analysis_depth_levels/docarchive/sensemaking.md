# Sensemaking — a8_analysis_depth_levels

## User Input
`/Users/ns/Desktop/projects/comprehenslate/devdocs/inquiries/2026-06-07_19-51__a8_analysis_depth_levels/_branch.md` (with surfacing output at the same folder)

---

## SV1 — Baseline Understanding

Define ordinal levels for A8 — Analysis Depth (sole axis in Depth family; FINAL axis closing the entire 8-axis framework; plain-ordinal interpretive-material-depth in separate-sections apparatus). A8 has three special characteristics: (1) FRAMEWORK-CLOSING axis (after this inquiry: 8/8 axes complete); (2) PROPOSES 4 LEVELS not 5 (inherited from DEPTH_PROFILE — needs substantive validation); (3) lives in SEPARATE-SECTIONS apparatus channel, NOT text-surface (A7↔A8 boundary is the most consequential cross-axis boundary).

Decide cardinality + level names + per-level depth + content-type-by-level table + A7↔A8 boundary + harmony-report-location + multi-meaning analysis at A8 + A4 cross-validation + cross-axis boundaries + Said Nursi per level + edition exemplars + receptive-only NOT APPLICABLE + chain default + DEPTH-FAMILY + FRAMEWORK closure markers.

---

## Phase 1 — Cognitive Anchor Extraction

### Constraints
- **C1** Plain-ordinal pattern (root committed).
- **C2** A8 inherits from DEPTH_PROFILE knob (root); inheritance must be substantively validated.
- **C3** A8 produces separate-sections apparatus content (NOT text-surface); A7 produces text-surface (different channel).
- **C4** A4 matrix gives per-purpose A8 defaults (scholarly=deep; devotional=standard; casual=surface; language-learning=deep+scholarly; performance=surface).
- **C5** A8 closes Depth family at 1/1 AND closes framework at 8/8 — the only inquiry with full-framework-closure implications.
- **C6** A7 finding flagged A7↔A8 cross-validation, harmony-report-location, and multi-meaning analysis as A8-inquiry concerns to resolve.
- **C7** A1/A2/A3 reader competence informs A8 content STYLE (not permission gating like A7).
- **C8** Receptive-only NOT APPLICABLE pattern (A4+A5+A6+A7) — A8 inherits as translator-strategy axis.

### Key Insights

- **KI1** Cardinality settles at 5 levels — `none | surface | standard | deep | scholarly` — NOT root's proposed 4. Substantive reason: A8 differs from all other ordinal axes (A6, A7) which explicitly have an `off` level for the "no feature" case. The DEPTH_PROFILE inheritance has 4 levels because it was originally a `none-implicit-at-`surface`` knob — the `.env.example` value `surface` was treated as "minimal/almost-none" by the legacy code. At the A8 axis level, the framework benefits from explicit-`none` parallel to A6=`off` and A7=`off`: A4 performance purpose explicitly wants NO analysis section (just clean recitation text), and A7=`off` + A8=`none` produces a maximally-clean pure-translation configuration that the framework should support as a first-class state. The 5-level structure `none | surface | standard | deep | scholarly` preserves the DEPTH_PROFILE labels at positions 2-5 (no naming refinement of the inheritance) while adding the explicit-`none` at position 1. This means the DEPTH_PROFILE inheritance is VALIDATED with REFINEMENT (add `none`; keep other 4 labels).

  **Critical alternative considered:** keep root's 4 levels (`surface | standard | deep | scholarly`) and treat `surface` as covering both "no analysis" and "minimal analysis." The 4-level option is rejected: collapses operationally-distinct cases (oral-recitation-clean-text vs Penguin-paperback-with-brief-intro); makes the A4 matrix's `performance=surface` and `casual=surface` cases produce the same A8 output despite operationally different intentions (performance wants ZERO analysis; casual wants minimal-but-nonzero analysis); breaks the cross-axis pattern where every other ordinal Strategy/Depth axis has an explicit-zero level.

- **KI2** Level names `none | surface | standard | deep | scholarly` validated. DEPTH_PROFILE inheritance preserved at positions 2-5; `none` added at position 1. Alternative `none | low | medium | high | max` (generic) loses the edition-tradition signal at `scholarly`. Alternative `none | brief | substantive | thorough | apparatus-edition` loses the user-recognizability of DEPTH_PROFILE labels.

- **KI3** Per-level depth specification has TWO complementary components (parallel to A7's budget specification):
  1. **Qualitative depth** — the verbal threshold ("scholarly" = full critical apparatus edition; "deep" = Norton-Critical-style analysis chapter; "standard" = brief introduction + select notes; "surface" = clean translation with minimal pointers; "none" = pure translation, no separate analysis sections).
  2. **Content-type-density-per-major-passage operational guidance** — concrete unit AI can monitor (none=0 content-types active; surface=0-2 content-types active (introduction + maybe glossary); standard=2-4 content-types active routinely; deep=4-7 content-types active per major passage; scholarly=all 12 content-types fire as relevant). These are NOT hard caps but orientation thresholds.

- **KI4** A8 has NO action-permission table (unlike A7). A7 controls budget for A1/A3 actions because A7's role is to gate text-surface scaffolding which A1/A3 actions produce. A8 produces separate-sections content DIRECTLY — there's no analogous A1/A3-action gating because A8 doesn't constrain other axes' actions; it produces its OWN content (etymology notes, rhetorical analysis, exegetical history, etc.). This structural distinction is load-bearing: A8 has a CONTENT-TYPE-by-level table (which content-types fire at each level) rather than an action-permission table.

- **KI5** Content-type-by-level table per level (12 content-types surfaced; mapped to 5 levels):

  | Content-type | none | surface | standard | deep | scholarly |
  |---|---|---|---|---|---|
  | Introduction (front matter) | — | — | brief | scholarly | comprehensive |
  | Glossary entries | — | — | major terms only | extensive | exhaustive |
  | Etymology notes | — | — | — | major terms | every key term |
  | Rhetorical analysis (nazm structure) | — | — | — | per major passage | per passage |
  | Cross-references (intra-corpus) | — | — | major passages | extensive | exhaustive |
  | Exegetical history (polysemy senses) | — | — | brief footnote | paragraph per major concept | full commentary tradition |
  | Lexical-history (target-equiv drift) | — | — | — | major terms | every key term |
  | Target-language-equivalent analysis | — | — | — | major choices | every translation choice |
  | Theological/conceptual commentary | — | — | — | major concepts | extensive |
  | Historical-critical context | — | — | — | per major passage | full |
  | Philological apparatus | — | — | — | — | full |
  | Cross-tradition references | — | — | — | — | full |

  Reading: at `none`, no content-types fire (no separate-sections apparatus at all). At `surface`, only Introduction (if any) and Glossary (for the most-needed terms). At `standard`, brief introduction + glossary + major-cross-references + brief exegetical-history footnotes. At `deep`, full introduction + extensive glossary + etymology + rhetorical analysis + extensive cross-references + paragraph-level exegetical history + lexical-history + target-language-equivalent analysis + theological commentary + historical-critical context. At `scholarly`, all 12 content-types fire with full apparatus.

- **KI6** A7↔A8 boundary settled with THREE complementary framings (all three apply simultaneously):

  1. **By LOCATION:** A7 = in-page apparatus (footnotes / glosses on the reading page); A8 = separate-sections apparatus (front matter / endnotes / appendix / sidebars).
  2. **By SCOPE:** A7 = per-reference scaffolding (the specific cultural reference at this word); A8 = per-passage / per-corpus analysis (full passage commentary; cross-references; structural analysis).
  3. **By AUDIENCE-INTERACTION:** A7 = inline interruption (reader looks down then back up at the same passage); A8 = deferred study session (reader reads translation first, then turns to analysis section).

  All three framings converge on the same operational rule: text-surface vs separate-sections, per-reference vs per-passage/corpus, inline vs deferred. This is the most consequential cross-axis boundary in the framework; making it operationally clear is critical for the translator-AI to not bleed A8 content into A7's channel and vice versa.

  Operational test for the AI: when producing content, ask "does this belong on the reading page next to the source word (A7) or in a separate analysis section after the translation (A8)?" Footnote citing a single word's cultural meaning → A7. Multi-paragraph analysis of the passage's rhetorical structure → A8. Brief inline gloss of `nefs` → A7. Full essay on the development of the `nefs` concept across Sufi tradition → A8.

- **KI7** Harmony-report-location settled: **A6 harmony report stays in its A6 channel regardless of A8 level, with cross-references between channels at high A8.** Resolution of 4 candidates:
  - Candidate 1 (stays standalone always): WINS at base case; preserves A6↔A8 orthogonality (each axis owns its channel).
  - Candidate 2 (migrates to A8 at high A8): REJECTED; would conflate channels and erode A6's harmony-report-as-distinct-apparatus identity.
  - Candidate 3 (merges into A8 when both high): REJECTED; loses A6's meta-analytic-commentary identity (harmony report is translator's perspective on what was preserved; A8 is reader-facing analysis content).
  - Candidate 4 (cross-references between channels): ACCEPTED as refinement of Candidate 1 — at high A8 (deep / scholarly), the harmony report can cite A8 sections ("see Analysis Chapter 3 for full rhetorical analysis of this nazm structure") and A8 sections can cite the harmony report ("the translator's notes on Tier 3 preservation appear in the harmony report"). Cross-references preserve orthogonality while enabling reader navigation.

  This resolves A7 finding's frontier flag (harmony-report-location open question).

- **KI8** Multi-meaning analysis at A8 settled as DISTINCT from A7's multi-meaning render: A7 controls HOW preserved senses appear at the text surface (footnote / parenthetical / inline-paired / apparatus); A8 produces EXEGETICAL-HISTORY analysis of the polysemous concept in the separate-sections channel at high A8 levels. The two are complementary — A7 makes the polysemy visible in the translation; A8 (at high levels) explains the sense-tradition in analysis. Multi-meaning POLICY invariant (WHETHER preserved); A7 controls HOW rendered in translation; A8 controls HOW analyzed in separate sections at high A8 levels.

  Per-level rule:
  - A8=none: no exegetical-history analysis (no separate sections at all).
  - A8=surface: no exegetical-history analysis (separate sections are minimal — introduction/glossary only).
  - A8=standard: brief lexical note in the glossary on key polysemy (e.g., glossary entry for `din` noting "judgment / religion / truth").
  - A8=deep: exegetical-history paragraph per major polysemous concept in analysis section (e.g., dedicated paragraph tracing the `din` sense-tradition).
  - A8=scholarly: full exegetical-history apparatus per polysemous concept (commentary tradition + lexical-philological argument + cross-tradition references).

- **KI9** Default-when-A8-silent settled: **`standard` via dual-tier resolution.** Two competing principles:
  1. A4 chain default (consistent with A5/A6/A7 pattern): chain through A4 silent → A4 casual default → A4 matrix casual A8 = `surface` → A8 = `surface`.
  2. Conservative-bias fallback (root finding's principle): preserve more form/scaffolding/depth; user dials DOWN.

  At first glance these conflict — A4 chain says `surface`, conservative-bias says `standard` or `deep`. Resolution: the A4 chain default applies when A4 is explicitly set (the per-purpose default matrix tells the AI what A8 to use given the purpose); the conservative-bias default applies when A4 is also silent (the absolute fallback at framework-cold-start). Per root finding: "When Purpose-driven defaults don't apply or the user hasn't set Purpose, fall back to conservative-bias defaults."

  Final default: A8 silent + A4 explicitly set → A4 matrix's per-purpose A8 value. A8 silent + A4 silent → A8 = `standard` (conservative-bias-fallback midpoint).

  Note: this is the first axis where the dual-tier default applies explicitly. A5/A6/A7 chain through A4 because their A4 matrix defaults were operationally clear; A8's A4 matrix gives 5 distinct values (surface, standard, standard, deep+scholarly, surface), and the casual default at `surface` is genuinely sparse for cold-start. Conservative-bias `standard` is more defensible at cold-start.

- **KI10** A4 finding refinement note ASSESSMENT: A4's per-purpose A8 verbiage (`surface / standard / deep / deep+scholarly / surface`) maps cleanly to A8's `surface | standard | deep | scholarly` labels EXCEPT for the language-learning case (`deep+scholarly` is ambiguous — does it mean A8 = `deep` OR A8 = `scholarly` OR both depending on context?). A4 refinement note required: A4 matrix language-learning A8 should be re-stated as `scholarly` (full parallel-text apparatus edition). The `surface | standard | deep | scholarly` mapping plus the language-learning refinement does NOT require adding `none` to A4 — the A4 matrix had no purpose explicitly mapping to "no analysis at all" so the `none` addition is A8-internal-only.

- **KI11** A8 ↔ A5 / A6 / A7 orthogonality re-confirmed:
  - **A5↔A8:** A5 foreignized + A8 high produces extensive etymology + source-language semantic-drift analysis (content shifts but A8 still produces its content-types). A5 lightly-domesticated + A8 high produces target-language-equivalent justification analysis (why this English word over alternatives). Content KIND varies; EXTENT/CHANNEL is A8's.
  - **A6↔A8:** A6 maximum + A8 high produces rhetorical-analysis content in A8 channel (nazm structure analyzed in apparatus); A6 off + A8 high — A8 still produces other content-types but no rhetorical-structural analysis. Orthogonal — each axis owns its dimension.
  - **A7↔A8:** A7 high + A8 high → maximum apparatus configuration (both channels rich); A7 off + A8 scholarly → clean translation + heavy analysis section; A7 scholarly + A8 surface → full in-page + clean post-translation (inverse pattern); A7 off + A8 none → maximally-clean pure-translation configuration. All combinations valid; orthogonality preserved.

- **KI12** NEW A8-adapted 4-component template (parallels A5/A6/A7 with A8-specific composition):
  1. **Analysis stance description** — what KIND of analysis the AI produces at this level; what apparatus channels open.
  2. **Per-level depth specification** — qualitative + content-type-density-per-major-passage operational guidance.
  3. **Content-type-by-level table reference + A7↔A8 boundary spec + harmony-report-location at this level + multi-meaning analysis at this level** — the operational substance.
  4. **Cross-axis interaction note** — A4 matrix match + A5/A6/A7 interactions + FRAMEWORK-CLOSURE marker.

- **KI13** Receptive-only NOT APPLICABLE (parallel to A4, A5, A6, A7). A8 is translator-strategy / user-configuration / analysis depth — not a reader-property. Explicit non-inheritance.

- **KI14** Said Nursi corpus per A8 level (with edition-tradition exemplars):
  - A8=none: oral-recitation passage (performance-purpose); pure translation, no separate analysis sections.
  - A8=surface: pop-translation Risale-i Nur paperback; minimal front matter (brief publisher's note); maybe a 2-page glossary of the most-needed Sufi/kalam terms only.
  - A8=standard: standard scholarly Risale-i Nur edition (devotional default per A4); brief scholarly introduction; glossary of major Sufi/kalam terms; cross-references to other Words/Letters; brief footnotes on key polysemous concepts (`din`, `nur`, `iman`).
  - A8=deep: Norton-Critical-style Risale-i Nur edition (scholarly default per A4); scholarly introduction; analysis chapter per major Word/Letter; extensive cross-references; exegetical history paragraphs for key concepts; rhetorical analysis of nazm; target-language-equivalent justification for major translation choices; theological commentary; historical-critical context.
  - A8=scholarly: full critical apparatus Risale-i Nur edition (language-learning default per A4); comprehensive introduction (Said Nursi's life, intellectual context, Risale-i Nur project history); exhaustive glossary; etymology of every key term; rhetorical analysis per passage; exhaustive cross-references; full exegetical history per polysemous concept; lexical-history of target-language equivalents; full target-language-equivalent analysis; extensive theological commentary; full historical-critical apparatus; philological apparatus (manuscript variants if available); cross-tradition references (other Sufi/kalam authors discussing same concepts).

- **KI15** Edition-tradition exemplars per A8 level:
  - A8=none: utility translation / oral recitation text / road sign translation.
  - A8=surface: Penguin Classics paperback (no analysis chapter; minimal front matter).
  - A8=standard: Oxford World's Classics with brief introduction + select notes; Loeb Classical Library with introduction.
  - A8=deep: Norton Critical Edition (translation + extensive analysis chapter + criticism collection).
  - A8=scholarly: SBL Greek NT critical apparatus edition; Robert Alter scholarly Hebrew Bible translation with apparatus; Brill critical edition; Cambridge Companion + Critical Edition.

- **KI16** DEPTH-FAMILY CLOSURE marker (1/1) + FRAMEWORK CLOSURE marker (8/8). After this inquiry: Reader 3/3 + Purpose 1/1 + Strategy 3/3 + Depth 1/1 = 8/8 axes complete. What unblocks: schema commit (pydantic dataclasses); per-purpose × per-axis default matrix synthesis; translator-AI prompt assembly with all 8 axes; Layer 1A UX preset catalog (deferred per root); Layer 2 POLICY operational specs (deferred); Layer 3 SOURCE-DESCRIPTION schema (deferred); UX-layer runtime conflict surface (deferred). Framework-closure synthesis inquiry possibility: a meta-inquiry rolling up all 8 findings into single canonical spec (this is itself a future inquiry; mentioned in Next Actions).

### Structural Points
- **SP1** 5 ordinal levels `none | surface | standard | deep | scholarly` (root proposed 4; substantively REVISED to 5 with explicit `none`).
- **SP2** Per-level depth: qualitative + content-type-density-per-major-passage operational guidance.
- **SP3** Content-type-by-level table (12 content-types × 5 levels) — the operational substance (replaces A7's action-permission table; structural distinction).
- **SP4** A7↔A8 boundary with three complementary framings (LOCATION / SCOPE / AUDIENCE-INTERACTION).
- **SP5** Harmony-report-location: A6 channel standalone; cross-references at high A8.
- **SP6** Multi-meaning analysis at A8 high levels (distinct from A7's render).
- **SP7** NEW A8-adapted 4-component template.
- **SP8** Default `standard` via dual-tier resolution (A4 chain when A4 set; conservative-bias fallback when A4 silent).
- **SP9** A4 finding refinement note: language-learning A8 = `scholarly` (not `deep+scholarly`).
- **SP10** A8↔A5/A6/A7 orthogonality preserved (each axis owns its dimension).
- **SP11** Receptive-only NOT APPLICABLE.
- **SP12** DEPTH-family-closure marker (1/1).
- **SP13** FRAMEWORK-closure marker (8/8) — UNIQUE TO THIS INQUIRY.
- **SP14** Substantive DEPTH_PROFILE inheritance validation: refined (added `none`; kept 4 original labels).

### Foundational Principles
- **FP1** Plain-ordinal pattern.
- **FP2** Separate-sections apparatus channel (A8's channel; distinct from A7's text-surface channel).
- **FP3** Content-types as A8's operational vocabulary (replaces A7's action-permission vocabulary).
- **FP4** Translator-strategy axis (receptive-only NOT APPLICABLE).
- **FP5** Inherits DEPTH_PROFILE labels (refined with `none` addition).
- **FP6** Dual-tier default: A4 chain when A4 set; conservative-bias fallback when A4 silent.
- **FP7** Language-agnostic at concept level.
- **FP8** Content-type-by-level table is deterministic; AI consults at content-production time.

### Meaning-Nodes
- **MN1** Analysis depth — extent of interpretive material in separate-sections channel.
- **MN2** Content-type — kind of analysis (etymology / rhetorical / exegetical / etc.).
- **MN3** Separate-sections channel — distinct from A7's text-surface channel.
- **MN4** Content-type-by-level table — A8's operational substance.
- **MN5** Framework closure — A8's unique completion marker.

### Meta-Inspection after SV2 (H4, H5)
- **H4 (concept names):** "Content-type" distinguishes A8's role from A7's action vocabulary. "Separate-sections channel" cleanly distinguishes from "text-surface channel." Labels `none | surface | standard | deep | scholarly` preserve DEPTH_PROFILE inheritance (positions 2-5) plus the explicit-zero level (position 1, parallel to A6=`off` / A7=`off`).
- **H5 (motivating examples):** Edition exemplars per level (utility / Penguin / Oxford World's Classics / Norton Critical / SBL Greek NT) + Said Nursi per level provide cross-cultural anchoring.

### SV2 — Anchor-Informed Understanding

5 levels `none | surface | standard | deep | scholarly` (revised from root's 4) with per-level depth (qualitative + content-type-density operational guidance); content-type-by-level table (12 content-types × 5 levels — the operational substance); A7↔A8 boundary with 3 framings; harmony-report-location standalone with cross-references; multi-meaning analysis at A8 high levels (distinct from A7 render); NEW A8-adapted 4-component template; dual-tier default `standard` (conservative-bias fallback); A4 finding refinement note (language-learning A8 = `scholarly`); cross-axis orthogonality preserved; receptive-only NOT APPLICABLE; DEPTH-family + FRAMEWORK closure markers.

---

## Phase 2 — Perspective Checking

### Technical / Logical
5 levels ordinally distinct (positions 1-5 with clean cardinality-anchoring at edition-traditions). Content-type-by-level table deterministic per level. Multi-meaning analysis per level deterministic. Cross-axis orthogonality holds at all 5×5×5×5 combinations of A5/A6/A7/A8.

### Human / User
User wants nuanced control over analysis depth and recognizes the DEPTH_PROFILE labels from `.env.example`. Adding `none` at position 1 is a substantive expansion the user may not have anticipated, but it serves the casual / performance use cases cleanly. Project default `standard` reflects conservative-bias fallback rather than A4 chain `surface` — defensible at cold-start.

### Strategic / Long-term
After A8: Strategy 3/3; Depth 1/1; FRAMEWORK 8/8. The framework is now closeable for schema commit. Cascading downstream-unblock list: pydantic dataclasses; per-purpose × per-axis default matrix synthesis; UX presets (Layer 1A); POLICY operational specs (Layer 2); SOURCE-DESCRIPTION schema (Layer 3); UX-layer runtime conflict surface.

### Risk / Failure
- R1: A8=none could be confused with A8=surface. CORRECTIVE: explicit operational distinction — `none` produces ZERO separate-sections content; `surface` produces minimal (introduction-only or glossary-only) content.
- R2: Content-type-by-level table at high cardinality (12 × 5 = 60 cells) may be ergonomically heavy in the translator-AI prompt. CORRECTIVE: the table compresses to a clear pattern — at each level, the relevant content-types fire; the AI consults the table once per major passage, not per word.
- R3: A7↔A8 boundary ambiguity at edge cases (e.g., a long footnote could feel like analysis-section content). CORRECTIVE: the three-framing rule (LOCATION + SCOPE + AUDIENCE-INTERACTION) gives the AI three converging tests. When in doubt, default to LOCATION (in-page or not).
- R4: Harmony-report-location decision could cascade-affect A6 spec (does this contradict A6's finding?). CORRECTIVE: A6 finding's resolution (harmony report at Levels 3+ as separate apparatus channel) is preserved; A8 finding doesn't move it; A8 finding only adds cross-reference machinery at high A8 levels.
- R5: Dual-tier default may conflict with single-tier default pattern of A5/A6/A7. CORRECTIVE: explicit dual-tier reflects A8's unique cold-start position (the only axis where A4 chain's casual default is "no/minimal" rather than "moderate/rich" — A4 chain pulls A8 toward sparse; conservative-bias defends midpoint).
- R6: DEPTH_PROFILE inheritance refinement (adding `none`) requires .env.example update. NOT a structural risk; documented for downstream implementation.

### Resource / Feasibility
Content-type-by-level table operational. Per-passage content-type-density guidance feasible. Multi-meaning analysis per level feasible. AI prompt context fits all 8 axes including A8.

### Definitional / Internal Consistency
Interpretation consistent with root + A1-A7 chain. Multi-meaning POLICY invariant; A7 controls render; A8 controls analysis (when polysemy fires AND A8 high). Cross-axis orthogonality preserved. A6 finding's harmony-report apparatus channel preserved.

### Definitional / Frame-exit Completeness (GATING)
- (i) Inherited terms: YES (A8 uses "Analysis Depth," "DEPTH_PROFILE," "separate-sections apparatus," "harmony report," "multi-meaning preservation" all inherited).
- (ii) Used across ≥2 values: YES (analysis depth used across 5 levels; content-types used across 5 levels; harmony-report-location used across A8 levels with cross-reference rule).
- Gating FIRES.

1. **Existence:** "Analysis depth" project-wide → A8 axis (Layer 1); separate from A6 harmony report (separate apparatus channel); separate from A7 text-surface scaffolding (in-page apparatus). "Separate-sections apparatus" project-wide → front matter / endnotes / appendix / sidebars in published edition tradition. LAYER: A8 is Layer 1 user-facing; A6's harmony report is also Layer 1 output but in a distinct apparatus channel. AGENT: user-side configuration of A8; translator-AI side production of A8 content.
2. **Role:** Out-of-scope: A7 text-surface (different channel); source-text intended commentary (Layer 3); pipeline-internal flags (Layer 4); UX-layer config-time conflict surface (deferred). KEEP OUT.
3. **Verdict Rigor:** "5 levels with explicit `none`" verdict:
   - Counter: keep root's 4 levels; treat `surface` as covering both no-analysis and minimal-analysis cases.
   - Why fails: collapses operationally-distinct cases (oral-recitation-clean-text vs Penguin-paperback-with-brief-intro); makes A4 matrix performance and casual produce identical output despite operationally different intents (performance wants ZERO; casual wants minimal-nonzero); breaks cross-axis pattern of explicit-zero levels in A6 / A7.
   - HOLDS at HIGH.
4. **Residual:** None.

### Phase / Calibration-State
A8 dual-tier default depends on calibration-state-aware framing: at cold-start (no user feedback yet), conservative-bias `standard` fallback applies when A4 silent. As feedback accumulates, the conservative-bias fallback may shift toward typical-use bias (per root finding's pattern for all axes). Trigger: after 30+ real translation configurations, examine A4-silent A8 distribution; recalibrate fallback if `standard` is consistently over- or under-shooting.

### Ethical / Systemic
Analysis depth at high levels (deep / scholarly) reflects ethical commitment to source-text fidelity and reader-accessibility-with-context. Counter: over-analysis can be patronizing or impose scholarly framework on devotional/casual readers. Mitigation: A4 matrix gives per-purpose A8 defaults; users select per-purpose; over-analysis is opt-in, not default.

### SV3 — Multi-Perspective Understanding

Confirms 5 levels `none | surface | standard | deep | scholarly` with per-level depth + content-type-by-level table + A7↔A8 boundary (3 framings) + harmony-report-location (standalone + cross-references) + multi-meaning analysis at A8 + dual-tier default `standard` + A4 refinement note (language-learning = `scholarly`) + cross-axis boundaries + DEPTH-family + FRAMEWORK closure. Frame-exit handled. Phase/calibration-state handled (cold-start conservative-bias). Ethical considerations addressed.

---

## Phase 3 — Ambiguity Collapse

### Ambiguity A1: Cardinality (4 vs 5 with `none`)
**Counter (4):** Keep root's `surface | standard | deep | scholarly`. Treat `surface` as covering minimal-or-no analysis.
**Why fails:** Collapses oral-recitation (ZERO apparatus) with Penguin-paperback-brief-intro (minimal-nonzero apparatus). A4 matrix performance and casual both default to `surface` but operationally differ — performance wants ZERO; casual wants minimal-nonzero. Cross-axis pattern: A6 has `off`; A7 has `off`; A8 should have `none`. The explicit-zero level enables clean A7=off + A8=none "pure-translation" configuration.
**Confidence:** HIGH
**Resolution:** 5 levels `none | surface | standard | deep | scholarly`.

### Ambiguity A2: Default-when-A8-silent (`surface` via A4 chain vs `standard` via conservative-bias)
**Counter (A4 chain `surface`):** Apply A4 chain pattern consistently with A5/A6/A7 — A8 silent → A4 silent → A4 casual default → A4 matrix casual A8 = `surface`.
**Why partially fails:** A4 chain gives `surface` for casual; but A4 silent (cold-start, no purpose set) is the genuine cold-start case where conservative-bias fallback applies per root finding. The conflict only arises in the A4-silent case.
**Counter (conservative-bias `standard`):** Apply root's conservative-bias-fallback principle when A4 is also silent — `standard` is midpoint preserving some analysis without overcommitting.
**Why succeeds:** A8's A4 matrix is uniquely cold-start-sparse (casual gives `surface` which is operationally near-empty); conservative-bias `standard` defends at framework-cold-start before any user calibration. Dual-tier resolution preserves both principles: A4 chain when A4 set; conservative-bias when A4 also silent.
**Confidence:** HIGH
**Resolution:** Dual-tier default. A8 silent + A4 set → A4 matrix value. A8 silent + A4 silent → A8 = `standard`.

### Ambiguity A3: Harmony-report-location (4 candidates)
**Counter (migrate to A8 at high A8):** Harmony report relocates into A8 analysis section when A8 ≥ deep; consolidates apparatus into one channel.
**Why fails:** Erodes A6's harmony-report-as-distinct-apparatus identity (translator's meta-analytic commentary). A6 finding committed harmony report at Levels 3+ as separate channel; A8 finding cannot retroactively move it. Conflates channels.
**Counter (merges into A8 when both high):** Same as above + deduplication.
**Why fails:** Same; PLUS A6's harmony report content (Tier preservation tracking) differs from A8's rhetorical-analysis content (reader-facing interpretation). They overlap but aren't identical; merging loses A6's translator-perspective framing.
**Counter (standalone always with cross-references):** A6 harmony report stays in A6 channel regardless; at high A8 (deep / scholarly), A6 and A8 cross-reference each other for reader navigation.
**Why succeeds:** Preserves A6↔A8 orthogonality (each axis owns its channel); enables reader navigation via cross-references; consistent with A6 finding's apparatus-channel commitment; doesn't require A6 finding revision.
**Confidence:** HIGH
**Resolution:** Standalone always; cross-references between channels at A8 = deep / scholarly.

### Ambiguity A4: Content-type-by-level granularity
**Counter (collapse to fewer content-types):** Use 5-6 content-types instead of 12; coarser per-level granularity.
**Why fails:** Loses operational distinction between cleanly-distinct content-types (etymology vs lexical-history vs target-language-equivalent analysis are operationally different even if conceptually adjacent). The 12 content-types map cleanly to recognizable apparatus components in scholarly edition traditions.
**Counter (expand to 15+ content-types):** Add finer-grained content-types (e.g., split "etymology" into Arabic-root etymology + comparative etymology).
**Why fails:** Over-granular; AI prompt context bloats; per-level table becomes hard to consult.
**Confidence:** HIGH
**Resolution:** 12 content-types × 5 levels = 60-cell content-type-by-level table.

### Ambiguity A5: A7↔A8 boundary framing
**Counter (single-framing — LOCATION only):** Use only "in-page vs separate-sections" to distinguish.
**Why partially fails:** LOCATION alone is operationally clear at most cases but ambiguous at edge cases (long footnote feels like analysis). SCOPE (per-reference vs per-passage/corpus) and AUDIENCE-INTERACTION (inline interruption vs deferred study) give the AI multiple converging tests.
**Counter (no formal framing — let AI judge):** Let translator-AI decide case-by-case.
**Why fails:** Non-deterministic; A7↔A8 channel-bleed risk.
**Confidence:** HIGH
**Resolution:** Three complementary framings (LOCATION + SCOPE + AUDIENCE-INTERACTION). All three apply simultaneously; LOCATION is the default tiebreaker.

### Ambiguity A6: Multi-meaning analysis at A8 (does A8 produce exegetical-history of polysemy?)
**Counter (A7 handles all multi-meaning concerns; A8 stays neutral):** Polysemy is A7's domain (render at text surface); A8 doesn't separately analyze.
**Why fails:** A8's apparatus channel naturally hosts exegetical-history analysis — that's what scholarly editions do (commentary tradition + lexical-philological argument). A7 controls the WORD-LEVEL render of preserved senses; A8 controls the CONCEPT-LEVEL exegetical-history analysis. Both are needed at high levels; they don't duplicate.
**Confidence:** HIGH
**Resolution:** A8 produces exegetical-history analysis at deep / scholarly levels (distinct from A7's render). Per-level rule specified in KI8.

### Ambiguity A7: Receptive-only applicability
**Counter:** Maintain parity with Reader axes (A1-A3 have receptive-only mode).
**Why fails:** A8 is translator-strategy / user-configuration / analysis depth — not a reader-property. Parallel to A4 / A5 / A6 / A7 non-applicability.
**Confidence:** HIGH
**Resolution:** NOT APPLICABLE.

### Ambiguity A8: NEW A8-adapted template structure
**Counter (reuse A5/A6/A7 template verbatim):** Use the same 4-component template without A8-specific adaptation.
**Why fails:** A5/A6/A7 template's 3rd component is "action-permission table reference" (A6/A7) or "stance/strategy operational substance" (A5). A8 has no action-permission table — its operational substance is content-type-by-level table + A7↔A8 boundary + harmony-report-location + multi-meaning analysis. The 3rd component must adapt.
**Confidence:** HIGH
**Resolution:** NEW A8-adapted 4-component template per KI12.

### Ambiguity A9: DEPTH_PROFILE inheritance validation outcome
**Counter (full validate — keep 4 labels exactly as in .env.example):** No refinement; pure inheritance.
**Why fails:** Misses the explicit-`none` case that A4 matrix performance and casual purpose surface.
**Counter (replace all 4 labels with new vocabulary):** Refactor entire label set.
**Why fails:** Loses user-recognizability of DEPTH_PROFILE labels; gratuitous change.
**Confidence:** HIGH
**Resolution:** Validated WITH REFINEMENT — keep 4 DEPTH_PROFILE labels (positions 2-5); add `none` at position 1. Note for downstream: .env.example DEPTH_PROFILE should be updated to accept `none` at next maintenance pass.

### Ambiguity A10: Framework-closure synthesis inquiry scope
**Counter (in scope here):** A8 inquiry produces framework-level synthesis combining all 8 axes into single canonical spec.
**Why fails:** Out of scope — this inquiry's scope is A8 axis specification; framework-level synthesis is a separate inquiry. Per A7 finding pattern of Strategy-family-closure marker (mentions closure but doesn't synthesize).
**Counter (out of scope; mention in Next Actions):** A8 finding mentions framework closure and lists what a synthesis inquiry would do, but does not produce the synthesis.
**Why succeeds:** Preserves inquiry-scope discipline; A8 inquiry is THIS axis; synthesis is FUTURE inquiry.
**Confidence:** HIGH
**Resolution:** Out of scope; A8 finding marks FRAMEWORK CLOSURE and lists framework-synthesis-inquiry as a Next Actions COULD item.

### SV4 — Clarified Understanding

After 10 ambiguity collapses:
- 5 levels `none | surface | standard | deep | scholarly` (revised from root 4)
- Per-level depth: qualitative + content-type-density operational guidance (not hard caps)
- Content-type-by-level table (12 × 5 = 60 cells; A8's operational substance)
- A7↔A8 boundary: three complementary framings (LOCATION + SCOPE + AUDIENCE-INTERACTION); LOCATION default tiebreaker
- Harmony-report-location: A6 channel standalone; cross-references at A8 deep / scholarly
- Multi-meaning analysis at A8: distinct from A7 render; produces exegetical-history at high A8 levels
- NEW A8-adapted 4-component template (3rd component reworked from action-permission to content-type-by-level + boundary + harmony + multi-meaning)
- Dual-tier default: A4 chain when A4 set; conservative-bias `standard` when A4 silent
- A4 finding refinement note: language-learning A8 = `scholarly` (not `deep+scholarly` ambiguous)
- DEPTH_PROFILE validated WITH REFINEMENT (add `none`; keep 4 labels)
- Receptive-only NOT APPLICABLE
- Cross-axis orthogonality preserved (A5/A6/A7 unchanged)
- DEPTH-family-closure marker (1/1)
- FRAMEWORK-closure marker (8/8) — the unique meta-closure
- Framework-synthesis inquiry: OUT OF SCOPE; mentioned in Next Actions COULD

---

## Phase 4 — Degrees-of-Freedom Reduction

### Variables fixed
- VF1: 5 ordinal levels `none | surface | standard | deep | scholarly`.
- VF2: Per-level depth = qualitative + content-type-density operational guidance.
- VF3: Content-type-by-level table = 12 content-types × 5 levels = 60 cells.
- VF4: A7↔A8 boundary = LOCATION + SCOPE + AUDIENCE-INTERACTION; LOCATION default tiebreaker.
- VF5: Harmony-report-location = A6 channel standalone; cross-references at A8 deep / scholarly.
- VF6: Multi-meaning analysis at A8 high levels = exegetical-history paragraph (deep) / full apparatus (scholarly).
- VF7: NEW A8-adapted 4-component template.
- VF8: Dual-tier default = A4 chain when A4 set; `standard` when A4 silent.
- VF9: A4 finding refinement note: language-learning A8 = `scholarly`.
- VF10: DEPTH_PROFILE validated with `none` addition.
- VF11: Receptive-only NOT APPLICABLE.
- VF12: DEPTH-family-closure (1/1).
- VF13: FRAMEWORK-closure (8/8).
- VF14: Framework-synthesis inquiry OUT OF SCOPE (mentioned in Next Actions).

### Options eliminated
- 4-level cardinality (collapses `none` and `surface` cases).
- A4 chain `surface` as A8-silent + A4-silent default (cold-start under-defends).
- Harmony report migration to A8 at high A8 (erodes A6 channel identity).
- Harmony report merge with A8 when both high (loses A6 translator-perspective framing).
- Content-type collapse to 5-6 (loses operational distinction).
- Content-type expansion to 15+ (over-granular).
- LOCATION-only A7↔A8 framing (ambiguous at edge cases).
- AI-judge-case-by-case A7↔A8 (non-deterministic).
- A7-only multi-meaning handling (misses exegetical-history at high A8).
- Receptive-only applied to A8.
- DEPTH_PROFILE labels refactored without inheritance preservation.
- Framework-level synthesis IN SCOPE here (creep beyond A8 axis spec).

### Viable paths
- VP1: 5 per-level definitions with NEW template.
- VP2: Content-type-by-level table.
- VP3: A7↔A8 boundary spec.
- VP4: Harmony-report-location spec.
- VP5: Multi-meaning analysis at A8 spec.
- VP6: A4 cross-validation + refinement note.
- VP7: Cross-axis boundaries (A5↔A8, A6↔A8, A7↔A8 confirmed).
- VP8: Said Nursi per level.
- VP9: Edition exemplars per level.
- VP10: DEPTH-FAMILY-CLOSURE marker.
- VP11: FRAMEWORK-CLOSURE marker.
- VP12: IC re-test.
- VP13: Next Actions including framework-synthesis-inquiry COULD item.

### SV5 — Constrained Understanding

Solution space: per-level definitions with full operational substance (depth + content-type-by-level + A7↔A8 + harmony + multi-meaning); A4 cross-validation + refinement note; cross-axis boundaries; Said Nursi per level; edition exemplars per level; DEPTH-FAMILY + FRAMEWORK closure markers; IC re-test; Next Actions including framework-synthesis-inquiry COULD.

---

## Phase 5 — Conceptual Stabilization

**A8 — Analysis Depth** = 5 ordinal levels (`none | surface | standard | deep | scholarly`) of interpretive material in the separate-sections apparatus channel — distinct from A7's text-surface apparatus channel. Plain-ordinal. Inherits DEPTH_PROFILE labels (positions 2-5) with REFINEMENT (`none` added at position 1).

**Three special characteristics:**
1. **FRAMEWORK-CLOSING axis.** With A8 specified: 8/8 axes complete. Cascading downstream-unblock list (schema commit, default matrix synthesis, presets, policies, source-description, UX surface).

2. **CONTENT-TYPE-BY-LEVEL TABLE as operational substance.** Unlike A7's action-permission table (gating A1/A3 actions), A8's operational substance is which CONTENT-TYPES (etymology / rhetorical / cross-references / exegetical / lexical-history / target-language-equivalent / theological / historical-critical / philological / cross-tradition / introduction / glossary) fire at each level. 12 content-types × 5 levels = 60-cell table.

3. **SEPARATE-SECTIONS APPARATUS CHANNEL distinct from A7's TEXT-SURFACE.** The A7↔A8 boundary uses three complementary framings (LOCATION + SCOPE + AUDIENCE-INTERACTION) with LOCATION as default tiebreaker. Harmony report (A6) stays in A6 channel standalone with cross-references at A8 deep / scholarly. Multi-meaning analysis at A8 high levels produces exegetical-history (distinct from A7's render mechanism).

Per-level depth specification = qualitative + content-type-density operational guidance (not hard caps). NEW A8-adapted 4-component template (analysis stance + per-level depth + content-type-by-level + cross-axis interaction; 3rd component reworked from A7's action-permission to A8's content-type emphasis). Dual-tier default: A4 chain when A4 set; conservative-bias `standard` when A4 silent. A4 finding refinement note: language-learning A8 = `scholarly`. Receptive-only NOT APPLICABLE. A8↔A5/A6/A7 orthogonality preserved. CLOSES DEPTH family at 1/1. CLOSES FRAMEWORK at 8/8.

### Accommodation trigger check
NO patching. 10 ambiguities settled HIGH. No anchors destabilize the model. Structural integrity holds.

### SV6 — Stabilized Model

A8 stabilized:
- 5 ordinal levels: `none | surface | standard | deep | scholarly`
- Per-level depth (qualitative + content-type-density operational guidance)
- Content-type-by-level table per level (12 content-types × 5 levels = 60 cells; operational substance)
- A7↔A8 boundary: LOCATION + SCOPE + AUDIENCE-INTERACTION (3 framings; LOCATION default tiebreaker)
- Harmony-report-location: A6 channel standalone; cross-references at A8 deep / scholarly
- Multi-meaning analysis at A8 high levels: exegetical-history paragraph (deep) / full apparatus (scholarly); distinct from A7's render
- NEW A8-adapted 4-component template (analysis stance + per-level depth + content-type-by-level/boundary/harmony/multi-meaning + cross-axis interaction)
- Dual-tier default: A4 chain when A4 set; `standard` when A4 silent
- A4 finding refinement note: language-learning A8 = `scholarly`
- DEPTH_PROFILE validated with `none` addition
- Receptive-only NOT APPLICABLE
- A8↔A5/A6/A7 orthogonality
- DEPTH-family-CLOSURE (1/1)
- FRAMEWORK-CLOSURE (8/8) — unique meta-closure
- Framework-synthesis inquiry: OUT OF SCOPE; mentioned in Next Actions COULD

**Difference from SV1:** Major. Cardinality REVISED from root's 4 to 5 (added `none`); labels validated with refinement; per-level depth (qualitative + content-type-density) decided; content-type-by-level table (the A7-action-permission-equivalent operational substance) specified at 60 cells; A7↔A8 boundary (3 framings) decided; harmony-report-location (standalone + cross-references at high A8) decided; multi-meaning analysis at A8 (distinct from A7 render) decided; dual-tier default decided; A4 refinement note added; DEPTH_PROFILE validated with refinement; NEW A8-adapted template; DEPTH-family + FRAMEWORK closure marked.

---

## Saturation
- Perspective: APPROACHING.
- Ambiguity: 10/10 HIGH; 0 OPEN.
- SV delta: major.
- Anchor diversity: 8 Constraints, 16 Key Insights, 14 Structural Points, 8 Foundational Principles, 5 Meaning-Nodes. DIVERSE.

**Saturation: HIGH. PROCEED.**

## Inherited Commitments Re-tested

| # | IC | Source | Verdict |
|---|---|---|---|
| IC1 | A8 plain-ordinal | root | RE-TESTED OK |
| IC2 | A8 4-level cardinality proposed | root | RE-TESTED & REVISED to 5 levels with `none` addition |
| IC3 | A8 inherits from DEPTH_PROFILE knob | root | RE-TESTED & VALIDATED WITH REFINEMENT (4 labels preserved; `none` added) |
| IC4 | A8 distinct from A7 (text-surface vs separate-sections) | root | RE-TESTED & EXPANDED to 3 framings (LOCATION + SCOPE + AUDIENCE-INTERACTION) |
| IC5 | A8 distinct from A4 (why-reading vs how-much-commentary) | root | RE-TESTED OK |
| IC6 | A4 per-purpose A8 defaults | A4 finding | RE-TESTED & CROSS-VALIDATED with refinement note (language-learning = `scholarly` not `deep+scholarly`) |
| IC7 | A7↔A8 cross-validation needed | A7 finding | RE-TESTED & RESOLVED with 3-framing rule |
| IC8 | Harmony report location across A8 levels | A7 finding tentative | RE-TESTED & RESOLVED — A6 channel standalone; cross-references at A8 deep / scholarly |
| IC9 | NEW translator-strategy 4-component template | A5+A6+A7 | RE-TESTED & ADAPTED for A8 (3rd component reworked from action-permission to content-type emphasis) |
| IC10 | Receptive-only NOT APPLICABLE | A4+A5+A6+A7 | RE-TESTED OK |
| IC11 | Chain default via A4 matrix | A5+A6+A7 | RE-TESTED & EXPANDED to dual-tier (A4 chain when A4 set; conservative-bias when A4 silent) |
| IC12 | Language-agnostic at concept level | root + chain | RE-TESTED OK |
| IC13 | DEPTH family 1/1 closure | root | RE-TESTED & DOCUMENTED |
| IC14 | Framework closing at 8/8 | root | RE-TESTED & DOCUMENTED — unique closure marker |
| IC15 | 5 ordinal levels `none | surface | standard | deep | scholarly` | NEW | NEW |
| IC16 | Per-level depth (qualitative + content-type-density operational guidance) | NEW | NEW |
| IC17 | Content-type-by-level table (12 × 5 = 60 cells) | NEW | NEW |
| IC18 | A7↔A8 boundary 3 framings | NEW | NEW |
| IC19 | Harmony-report-location standalone + cross-references | NEW | NEW |
| IC20 | Multi-meaning analysis at A8 (exegetical-history at deep+) | NEW | NEW |
| IC21 | Dual-tier default (A4 chain + conservative-bias fallback) | NEW | NEW |
| IC22 | A4 finding refinement note (language-learning = `scholarly`) | NEW | NEW |
| IC23 | DEPTH_PROFILE refinement (add `none`; update .env.example) | NEW | NEW |
| IC24 | NEW A8-adapted 4-component template | NEW | NEW |
| IC25 | DEPTH-family-CLOSURE marker (1/1) | NEW | NEW |
| IC26 | FRAMEWORK-CLOSURE marker (8/8) — UNIQUE TO THIS INQUIRY | NEW | NEW |

## Frontier Flags for Decomposition / Critique

- FF1: Framework-synthesis inquiry (rolling up all 8 axes into single canonical spec) — Next Actions COULD; future inquiry.
- FF2: .env.example DEPTH_PROFILE refinement (add `none`) — Next Actions MUST at maintenance pass.
- FF3: Per-purpose × per-axis default matrix synthesis — now possible after framework closure; Next Actions COULD.
- FF4: Per-target-language analysis-content feasibility — does scholarly apparatus differ per target language tradition? Future inquiry.
- FF5: UX-layer config-time conflict surface for the now-complete framework — future UX inquiry.
- FF6: Layer 2 POLICY operational specs (5 deferred per root) — future POLICY inquiries; now unblocked.
- FF7: Layer 3 SOURCE-DESCRIPTION schema — future inquiry; now unblocked.
- FF8: A4 finding maintenance pass (language-learning A8 refinement + propagate prior A6/A7 refinements bundle) — Next Actions MUST.
