# State: Firing-Time Categories for Principles

## Flow-type
articulated-surfacing-routed

## Pipeline
A → Su → W → S → D → I → C → R (always)

## Progress
- [x] Articulate-Simple
- [x] Surfacing
- [x] Warm
- [x] Sensemaking
- [x] Decomposition
- [x] Innovation
- [x] Critique
- [x] Routelister

## Warm Loop
round: 0/2 | last-anchor: firing-time-useful-but-form-refined-against-heterogeneous-corpus (fixpoint) | status: settled

## Iteration
1

## Status
COMPLETE

## Next Discipline
—

## Relationships
- CONTINUES FROM: devdocs/inquiries/2026-07-11_00-24__semantic_priority_before_harmony_generation/ (the prior finding established the two-gate 3-Pass structure this proposal would populate with firing-time tags; the firing-time categories formalize its generation-time / post-draft gates)
- RELATED: devdocs/inquiries/2026-07-10_23-03__translation_error_root_cause_skill_adherence/finding.md (its critique-corrected "dual-shaped principles" claim is the exact premise this proposal rests on; re-tested here, not inherited)

## History
- 2026-07-11_01-49: CONCLUDE complete. Status COMPLETE. Answer: firing-time categorization SURVIVES only as a small organizing generalization — the clean fireable_at_first/second_pass tag fails (heterogeneous corpus; "both" is the majority; a comprehension phase is missing), but the possibility→NECESSITY reframe isolates a useful CONFIG-INDEPENDENT check-spine (the always-run fidelity checks: omission/no-add/no-merge-split + whole-draft structural), whose reliable core is the mechanically-checkable subset and whose value is the organizing axis (config-dependence), not new machinery. Wrote finding.md (frontmatter refines: BOTH priors; Changes-from-Prior REFINES 2026-07-10_23-03 + EXTENDS 2026-07-11_00-24; Inherited Commitments Re-test of 4 commitments — dual-shaped [confirmed+frame-revised: dual is majority], config-keyed-agenda-with-omission-diff-spine [confirmed], two-gate/3-Pass wiring [confirmed; spine populates Pass-3], word/clause-vs-sentence granularity caveat [confirmed+frame-revised: spine adds a third whole-draft granularity]; MUST=decide reach + gated COULDs; doubly-gated caveat prominent). Archived 7 discipline files to docarchive/ (articulate_simple, surfacing, articulate_warm, sensemaking, decomposition, innovation, critique); kept _branch.md/_state.md/finding.md/routelister.md/_route.md in root. Structural check PASS.
- 2026-07-11_01-44: Routelister (exhaust) complete. Mode root/project-space, entry fresh. 8 onward routes (4 teleological / 4 epistemic): R1 apply-corrected-spine-to-Pass-3 (DEVELOP, core·@after-prior-fix — doubly-gated), R2 finalized-corrected-spine-spec (DEVELOP, core — the design-reach), R3 validate-vs-7-error-translation (TEST, supporting), R4 backlink-prior-2026-07-11_00-24 EXTENDS (REFINE), R5 backlink-prior-2026-07-10_23-03 REFINES+generalizes (REFINE), R6 consolidate-memory-config-independent-vs-derived-axis (CONSOLIDATE), R7 branch-mechanical-checkability-taxonomy (PURSUE-SEED, peripheral), R8 branch-harden-soft-rim (INVESTIGATE-FRONTIER, peripheral·@if-R3-weak). Excluded (4, with reasons): corpus-wide per-principle tagging (dead/metadata-theater); "does firing-time survive?" (settled); the OUTPUT-REACH fork DECISION (control-flow disposition, routed OUT per NOT-list); schemas.py-stage-field/simulated-intermediate (belong to prior 2026-07-11_00-24). Wrote routelister.md + _route.md, BOTH in inquiry root (neither to docarchive). No LAYER 1/2 flags. Verdict PROCEED. Structural check PASS. Next: CONCLUDE (load protocols/conclude.md; deliver A+B incl. the Inherited Commitments Re-test of BOTH priors + refines: both in frontmatter; fold in critique's third-axis + magnitude-floor + membership-audit + two-filter-scope + doubly-gated corrections; present piece D reach with C as the offered reach; archive the 7 discipline files, keep routelister.md/_route.md/_branch.md/_state.md in root).
- 2026-07-11_01-42: Critique complete. 7 weighted dimensions (D1 earns-its-keep / D2 coherence-with-priors / D3 orthogonality / D4 elegance / D5 scope-boundary / D6 external-anchor / D7 frame-premise), external-anchor NOT quarantined (SKILL text grounded). Adversarial STRONG. Verdicts: fix-design SURVIVES with REFINES. THREE substantive corrections for CONCLUDE: (1) NEW THIRD AXIS — the spine's genuine core is the TRIPLE intersection must-wait ∧ config-independent ∧ MECHANICALLY-CHECKABLE; the innovation's two-axis intersection-cell omitted reliability. omission-diff/no-add/no-merge-split are mechanical (rubber-stamp-RESISTANT) = the robust core; escalation/ring/havuz are judgment checks (rubber-stamp-PRONE) = a soft rim. Parallels Inquiry-2's granularity correction. (2) MAGNITUDE FLOOR upheld (RE-TEST TRIGGER from innovation confirmed) — near-ZERO novel content (omission-diff exists in prior 2026-07-10_23-03; no-add/no-merge exist as harmony_layer hard constraints; structural checks descend from Tier-1/advanced_principles); what survives = the config-independent AXIS + the GENERALIZATION (single omission-diff → a named closed family), neither in either prior. "Survives modestly" → "survives as a small organizing generalization." (3) MEMBERSHIP MISFILING — word-sense-correctness is config-INDEPENDENT (correct sense fixed by local source construction per the project polysemy principle, NOT by A1), so the innovation's demo columns mis-sorted it into config-derived; apply the criterion by TEST not by topic. Plus: scope = TWO filters (membership rule + a type-gate: spine = source↔draft-correspondence-checks only; explains the no-smoothing/meta-principle exclusion), NOT one clean entailment. Plus: two-axis reframe SURVIVES but phrasing refined — firing-time=placement does real work (lands a check in Pass-3 vs a Pass-1 hold) so NOT pure face-saving, but must NOT imply parity: config-independence is the discriminating/novel axis, firing-time the necessary-but-modest coordinate. DIAGNOSIS A/B: REFINES+EXTENDS CONFIRMED (supersede rejected — priors' agendas intact/larger; dissolves rejected — axis+generalization new); whole-draft-granularity CONFIRMED as compatible extension (a third check-TYPE at a third granularity, not a contradiction of the prior's word/clause-granular omission-diff). FRAME-PREMISE P2 lands: the spine is DOUBLY-GATED / inert until BOTH the prior 3-Pass wiring is applied AND this metadata layer is added — surface prominently (contingent on two unapplied upstream decisions). Signal TERMINATE; landscape CHANGED (third axis surfaced — refinement not reversal); clean SURVIVE exists; PROCEED→FLAG for CONCLUDE. No failure modes. Structural check PASS. Next: Routelister (exhaust — sweep this inquiry's finished artifacts into the typed onward route-field).
- 2026-07-11_01-37: Innovation complete. Standard-default mode (contrarian sub-directive honored locally, not run-wide; alternative Contrarian-rethink marked-inapplicable — Sensemaking already adjudicated survival). Full coverage 4G+3F. Piece C = meta-decision (properties i/ii/v) → Intervention-Shape-Axis Inversion MANDATORY + satisfied. CONVERGENCE (Combination + Domain-transfer[software invariants] + system-level Inversion + Lens) on the INTERSECTION-CELL / INVARIANT-SUITE model: the spine = (must-wait ∧ config-independent), the two axes ORTHOGONAL. Design commitments: C1 criterion as two-part closure-RATIONALE (not per-principle booleans — metadata-theater killed by Constraint-ADD); C2 a two-column Pass-3 checklist (config-independent spine | config-derived agenda) inside the prior 2026-07-11_00-24 gate, GATED on that wiring; C3 scope ENTAILED by the criterion (config policies fail part-b; meta-principles not draft-checkable; ~80 detection principles are comprehension-time); C4 3-phase = one-line acknowledgment only (the third exclusion rationale); C5 design-discipline = one-line closure note, DEFERRED to a maintenance rule; C6 reframe RESOLVED — config-independence is the VALUE-carrying axis, firing-time the PLACEMENT axis (not dispensable, not the novelty). Intervention shape = HYBRID (REORGANIZE existing Pass-3 + small ADD-CONTENT of the whole-draft structural checks). Inherited Frame Audit ran, did NOT fire (seed assumption + REFINES+EXTENDS + ADD-CONTENT each explicitly challenged). Assembly = the two-column Pass-3 architecture; axis-coverage 4 axes each varied. RE-TEST TRIGGER (S5) raised for CONCLUDE/piece-A: sharpen headline (config-independence=value, firing-time=placement) + cost/benefit floor (near-ZERO novel content; existence-axis inversion shows omission-diff/no-add/no-split already exist, only escalation/ring/havuz are new and descend from Tier-1 — yield is the ORGANIZING BIPARTITION, not new checks). Telemetry PROCEED; no failure modes. Structural check PASS. Next: Critique (adjudicate C's soundness + the honest-floor sharpening; stress-test A/B's earns-its-keep + REFINES+EXTENDS-not-supersede).
- 2026-07-11_01-27: Decomposition complete. DV1; 4 pieces (A evaluation-core+reframe+cost/benefit-verdict · B prior-reconciliation+commitment-re-test · C fix-design[criterion+config-independent-check-spine] · D reach-gate). Boundaries top-down (OUTPUT-REACH valley) + bottom-up (8 atoms→4 pieces) AGREE, HIGH conf; no hidden coupling. Same 4-piece shape as the two sibling inquiries (single unified evaluation+design; forcing more = Over-Decomposition). A→B always-delivered; D gates C. Determination-mechanism check PASS — the runtime-determination sub-question ("is THIS principle must-wait?") IS the selection criterion, living in C. CONCLUDE flagged: Synthesis Trigger consumes 2 priors → finding needs frontmatter refines: BOTH (the dual-shaped claim of 2026-07-10_23-03; the Pass-3 agenda of 2026-07-11_00-24) + an Inherited Commitments Re-test of both (4 commitments: dual-shaped [confirm+sharpen]; config-keyed-agenda-with-omission-diff-spine [confirm; the spine is what firing-time generalizes]; the two-gate/3-Pass wiring [confirm; firing-time populates Pass-3]; the word/clause-check vs sentence-foundation granularity caveat [re-test — must-wait structural checks are whole-draft granularity, a compatible extension]). Self-eval 7/7 PASS. Next: Innovation (works fix design-space C — how to express the criterion + enumerate the spine + whether the standing design-discipline earns inclusion).
- 2026-07-11_01-24: Sensemaking complete. SV1→SV6 stabilized. MODEL: firing-time SURVIVES as its own contribution but modestly and precisely. The clean-2-category-over-everything form FAILS on 3 structural grounds (heterogeneous corpus; dual-majority → possibility-partition non-discriminating; missing comprehension phase → pipeline has 3 moments not 2). The single reframe that rescues it: possibility→NECESSITY — tag not "CAN fire at second pass" (≈everything, useless) but "MUST wait for the finished draft" (few, useful). That small set = the CONFIG-INDEPENDENT check-spine (omission-diff, no-add, no-merge/split, whole-draft structural-fidelity checks like escalation/ring/havuz preserved) — ORTHOGONAL to the priors' CONFIG-DERIVED agenda (too-hard-word/word-sense/naturalness keyed to A1-A8). Firing-time's surviving contribution stated precisely: a SELECTION CRITERION ("must this wait for the finished draft?") that (a) GENERALIZES prior 2026-07-10_23-03's single 'mechanical omission-diff spine' into a principled config-independent set, and (b) COMPLEMENTS its config-derived checks; it plugs into prior 2026-07-11_00-24's Pass-3 and is GATED on that wiring. Fork 1 = OPERATIONAL (not inert), contribution = the selection criterion, not a standalone corpus-wide scheme. Fork 2 = scoped (substantive principles + hard constraints; exclude config policies + meta-principles), necessity-framed, actionable output = the small must-wait membership list; 3 phases acknowledged conceptually but NOT tagged exhaustively (heavy per-principle 3-phase tagging = metadata-theater, rejected). Rivals: REDUNDANCY partially lands (much is already in priors) but firing-time survives via the generalization + config-independent axis (HIGH — config-keying structurally cannot emit config-independent checks); OVER-ENGINEERING lands against the heavy scheme (rejected), not the lean spine; DEEP rival — does NOT fully dissolve (config-independent ≠ config-derived) but contribution is modest. TWIST (specific-vs-pattern): the user's own examples ("allegorical"/"work") are FIRST-pass-fixable → they argue for the generation-time hold-set (≈ priors' work), not a new second-pass scheme; firing-time's novel yield is the config-independent second-pass spine (smaller prize than framed, but real). Relationship = REFINES (dual-shaped claim) + EXTENDS (Pass-3 agenda), not supersede. Structural check PASS. Next: Decomposition.
- 2026-07-11_01-20: Warm complete. MED-FLAG. Re-anchor settled at FIXPOINT round 0 (0 re-surfaces) — anchor concretized (the corpus's heterogeneity / dual-majority / missing-comprehension-phase), NOT relocated. MQ2 committed: the task bears on the ACTUAL corpus as surfaced (heterogeneous; dual-majority; comprehension-phase unaccounted; alongside the already-designed 3-Pass wiring), with a directional commitment that the useful form is REFINED (3-phase and/or must-wait-reliability tag, over a scoped subset), NOT a clean 2-way partition of everything — which refined form left open. On-trigger re-runs: MQ4 moved (exclusions added — config INPUT axes A1-A8 are not taggable principles; meta-principles may be out of scope, FF4); MQ1 moved (category-structure gains 3-phase + a reliability-vs-possibility reframe); MQ3 moved (auto-generate-agenda sharpens to "isolate the small must-check-later minority"); WHY carried (motivations confirmed, not moved); MQA re-run (reconciled: tag is operational-only and its value is isolating the small must-wait agenda; firing-time does NOT cleanly partition the heterogeneous corpus → scheme must be refined). Content-conflict fired NON-SEVERE: the user's instinct is AFFIRMED by surfaced reality (firing-time is a real, useful axis — the two gates formalize it; dual-shaped premise confirmed), only the FORM is refined (clean-2-way mis-fits). Conflict-block gate: PROCEED. Structural check PASS. Next: Sensemaking.
- 2026-07-11_01-17: Surfacing complete. 18 items / 7 regions (11 core). Read the actual corpus. 6 DECISIVE findings: (DF1) the corpus is HETEROGENEOUS — ≥4 kinds (harmony components [harmony_layer Tier 1-4] / ~80 "On X" analytical-detection principles [translation_principals+notes] / config-driven policies [schemas.py] / methodological meta-principles [two-step, no-smoothing, preserve-all-valid-meanings]) — "firing-time" applies differently to each. (DF2) DUAL ("both") is the MAJORITY, not the exception — most harmony components + detection principles can fire at generation AND be checked → a binary partition where most land in "both" is a weak discriminator. (DF3) the 2-category scheme may be MISSING A PHASE — the largest bloc (the ~80 detection principles + escalation-chain + istilzam) fires at COMPREHENSION time (reading the source), neither generate nor check; the real pipeline has ≥3 moments (comprehend → generate → check) and the 3-Pass has 3 passes → 2 tags mismatch both. (DF4) the Tier system already classifies harmony components but on preserve-WEIGHT (orthogonal to firing-TIME); Tier 3 adds a context-condition axis → firing-time = a 3rd axis, metadata load is a real cost. (DF5) Fork 1 leans OPERATIONAL-OR-LITTLE-VALUE with a self-undercutting twist: the tag earns its keep only by driving the two gates, but because most are "both," the auto-generated second-pass check-agenda ≈ the whole corpus, failing to deliver the small focused agenda that IS the value prop; the genuinely second-pass-DOMINANT items are few (the near-mechanical hard constraints: omission-diff, no-add, no-merge/split). (DF6) the prior finding's two gates DO map onto first/second pass (formalization confirmed) with a 2-vs-3-pass granularity caveat. 4 frontier flags: FF1 3-phase vs 2?; FF2 the useful reframe — tag "must-WAIT-for-second-pass" (reliability) not "can-fire-at-second-pass" (possibility), isolating the small check-agenda; FF3 value beyond the already-designed 3-Pass wiring?; FF4 restrict tagging to harmony-components+detection-principles, excluding policies+meta-principles? Structural check PASS. Next: Warm.
- 2026-07-11_01-13: Created. Question: is it useful to tag the SKILL's principles/policies with firing-time categories (fireable_at_first_pass / fireable_at_second_pass / both), and if so what is the scheme? Articulation: HIGH-PROCEED (Itemize count = 1; flagged: none). Layer Commitment = STRUCTURAL (add a firing-time classification dimension to the principle/policy corpus), with PROCESS (consumption-wiring into the 3-Pass) sequential and largely inherited from the prior 2026-07-11_00-24 fix. Synthesis Trigger: consumes TWO priors (the dual-shaped-principle claim; the two-gate 3-Pass fix) → CONCLUDE needs an Inherited Commitments Re-test. Two preserved forks: (1) CATEGORIZATION-PURPOSE inert-metadata vs operational-driver; (2) TAGGING-TARGET well-definedness (does firing-time cleanly apply to each heterogeneous principle/policy, or are some dual/context-dependent/inert). Next: Surfacing.
