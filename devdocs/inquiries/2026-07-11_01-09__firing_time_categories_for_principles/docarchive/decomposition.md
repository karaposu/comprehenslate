# Decomposition — Firing-Time Evaluation + Design, Partitioned

## User Input

`_branch.md`. Stabilized model (sensemaking.md): firing-time categorization SURVIVES modestly — the clean-2-category-over-everything form fails structurally (heterogeneous corpus; dual-majority makes a possibility-partition non-discriminating; a comprehension phase is missing → 3 pipeline moments not 2). Rescue = possibility→NECESSITY reframe: tag "MUST wait for the finished draft" (few) not "CAN fire at second pass" (≈everything). That small set = the CONFIG-INDEPENDENT check-spine, orthogonal to the priors' CONFIG-DERIVED agenda. Firing-time's surviving contribution = a SELECTION CRITERION that GENERALIZES prior `2026-07-10_23-03`'s single omission-diff spine into a principled config-independent set and COMPLEMENTS its config-derived checks; plugs into prior `2026-07-11_00-24`'s Pass-3, gated on that wiring. Fork 1 = OPERATIONAL. Fork 2 = scoped + necessity-framed + membership-list (not corpus-wide tagging; 3 phases conceptual only). REFINES+EXTENDS the priors. Span the OUTPUT-REACH fork; flag the Inherited Commitments Re-test (2 priors). Save to `decomposition.md`.

---

## Step 1 — Coupling Topology

**Elements:**
E1 evaluation-core (does firing-time earn its keep; the 3 structural failures of the clean form; the survives-modestly verdict) · E2 the load-bearing reframe (possibility→necessity; the config-independent-vs-config-derived axis; the "selection criterion" concept) · E3 prior-reconciliation (REFINES the dual-shaped claim + EXTENDS the Pass-3 agenda; gated on the 3-Pass wiring) · E4 the commitment re-test (both priors' load-bearing commitments) · E5 fix-design (the criterion + the config-independent check-spine membership; scope; conceptual 3-phase; the standing design-discipline) · E6 the honest cost/benefit verdict (does even the lean spine add enough, given the prior's omission-diff already covers the biggest must-wait item?) · E7 OUTPUT-REACH gate.

**Coupling gradient:**
- E1↔E2 **strong** (the verdict is read off the necessity/config-independent reframe — remove the reframe and the verdict collapses). One cluster.
- E1↔E6 **strong** (the cost/benefit verdict is the sharp edge of "does it earn its keep"; E6 is where the honest "it's a small prize" lives). Same cluster.
- E3↔E4 **strong** (the re-test IS the concrete validation of the REFINES+EXTENDS relationship; the relationship decides which commitments to re-test). One cluster.
- E1→E3 **moderate** (the reconciliation reuses the verdict but adds the two priors as inputs and makes a distinct relationship claim). Distinct piece.
- E1→E5 **strong** (the design instantiates the criterion the verdict endorses).
- E3→E5 **moderate** (the design must plug into the prior's Pass-3 and complement its config-derived agenda; the re-test of the prior's check-agenda informs the design's complement-relationship).
- {E1,E2,E3,E4,E6}→{E5} **weak** — the evaluation + reconciliation + verdict are complete without building the design; the design is the *reach*. **Dominant boundary (OUTPUT-REACH valley).**
- E7 is a **scope gate**, weakly coupled, governing whether E5 is in final scope.

**Clusters → pieces:** A = {E1,E2,E6} evaluation-core + reframe + cost/benefit verdict · B = {E3,E4} prior-reconciliation + commitment-re-test · C = {E5} fix-design · D = {E7} reach gate.

## Step 2 — Boundaries (top-down)
Lowest-coupling cut = the **OUTPUT-REACH valley** between the evaluation {A,B} and the design {C}, gated by D. Secondary cut: A|B (the-verdict-and-its-reframe vs the-prior-relationship-and-its-re-test).

## Step 3 — Boundary validation (bottom-up)
Irreducible atoms: {does-it-earn-its-keep}, {the possibility→necessity reframe}, {the config-independent axis}, {the cost/benefit honesty}, {the REFINES+EXTENDS claim}, {the priors' re-tested commitments}, {the criterion + spine design}, {the reach choice}. These group into A (first four), B (next two), C (design), D (reach) — top-down and bottom-up **agree** → HIGH confidence. Four pieces (matching the two prior sibling inquiries) — correct: this is a single unified evaluation-plus-design; forcing more would be Over-Decomposition (failure mode #5).

## Step 4 — Question Tree

**Q-A — Evaluation-Core + Verdict** *(always in scope):* Does firing-time categorization earn its keep, and in what refined form?
- [ ] the clean-2-category form's **three structural failures** named on evidence: heterogeneous corpus (≥4 kinds); dual-majority (a possibility-partition is non-discriminating); missing comprehension phase (3 pipeline moments, not 2)
- [ ] the **possibility→necessity reframe** stated (tag "must-wait-for-the-finished-draft", not "can-fire-at-second-pass") as the single move that rescues the idea
- [ ] the **config-independent vs config-derived axis** named (firing-time's must-wait set = config-independent structural checks; orthogonal to the priors' config-derived, A1-A8-keyed checks)
- [ ] the **honest cost/benefit** stated: the lean spine's novel yield beyond the prior's already-existing omission-diff spine is real but modest (it *generalizes* the single omission-diff into a principled set + adds the config-independent whole-draft structural checks); the heavy per-principle 3-phase corpus tagging is rejected as metadata-theater
- [ ] verdict = **survives-modestly-as-a-selection-criterion** (instinct affirmed; scope much smaller than first framed; the user's own examples are first-pass-fixable, so the spark points mostly at the priors' work)

**Q-B — Prior-Reconciliation + Commitment Re-test** *(always — Synthesis Trigger, 2 priors):* How does this relate to the two priors, and do their commitments survive re-test?
- [ ] relationship named **REFINES** (the dual-shaped-principles claim of `2026-07-10_23-03` — this inquiry confirms dual is the *majority*, which is *why* possibility-tagging fails) **+ EXTENDS** (the Pass-3 check-agenda of `2026-07-11_00-24` — adds the config-independent spine), **not supersede**
- [ ] the **gated-on-the-3-Pass-wiring** dependency stated (firing-time tags have nowhere to fire unless the prior `2026-07-11_00-24` fix is applied; the proposal is that fix's metadata layer)
- [ ] each prior's load-bearing commitments listed + re-tested with cited evidence: from `2026-07-10_23-03` — (i) dual-shaped principles [CONFIRMED + sharpened: dual is the majority]; (ii) the config-keyed scoped check-agenda with a mechanical omission-diff spine [CONFIRMED; the spine is what firing-time generalizes]. From `2026-07-11_00-24` — (iii) the two gates / 3-Pass wiring [CONFIRMED; firing-time populates the Pass-3 gate]; (iv) the "check stays word/clause-granular, foundation sentence-granular" caveat [re-test — the must-wait structural checks operate at whole-draft granularity, a compatible extension]

**Q-C — Fix-Design** *(gated by D):* What is the concrete design — the selection criterion + the config-independent check-spine?
- [ ] states the **criterion**: "must this principle wait for the finished draft to be reliably verified?" — the determination mechanism that selects spine membership
- [ ] enumerates the **spine membership** (the config-independent must-wait set): omission-diff (no source clause dropped), no-add (nothing invented), no-merge/no-split (sentence boundaries intact), + whole-draft structural-fidelity checks (escalation / ring-composition / havuz-convergence / cause-effect chain preserved)
- [ ] respects **scope**: applies to substantive principles (harmony components + detection principles) + the hard constraints; EXCLUDES config policies (trivially dual, low-stakes) and meta-principles (about the process)
- [ ] handles the **conceptual 3-phase** (comprehend / generate / check) as clarification only — NOT a heavy per-principle tag; optionally notes which principles are comprehension-time (connects to the prior's config-blind-Pass-1 question)
- [ ] **additive + placed**: says where it lives (the small spine as `2026-07-11_00-24`'s Pass-3 check-list, complementing the config-derived checks); decides whether the standing design-discipline ("ask must-this-wait for each new principle") is worth stating

**Q-D — Reach Gate** *(scope decision, surfaced to user):* How far past evaluation should the deliverable travel?
- [ ] the three reaches named (evaluate-only / +design-the-criterion-and-spine / +apply-the-SKILL-edit)
- [ ] default recommended (A+B always delivered; C offered for user authorization)
- [ ] gate presented, not silently resolved

## Step 5 — Interface Map

| Source | Target | What flows | Dir |
|---|---|---|---|
| surfacing (corpus enumeration) | A, C | the actual principle/policy corpus + the config-independent hard constraints — prerequisite | → |
| sensemaking (stabilized model) | A | the verdict + the two resolved forks + the reframe (prerequisite) | → |
| A | B | the "survives-modestly" verdict the relationship builds on (data) | → |
| A | C | the selection criterion the design instantiates (dependency) | → |
| two prior findings | B | the commitments to re-test (data) | → |
| B | C | the complement-relationship (config-independent extends config-derived) + the gated-on-wiring dependency | → |
| D | C | reach authorization (gate) | → |

**Assumptions-not-data check (hidden-coupling guard):** C assumes A's criterion is right (explicit, A's output) AND that the prior 3-Pass wiring is the insertion point (explicit, from B — and gated on that fix being applied). B assumes both priors' commitments are as stated (explicit — named inputs). C assumes B's complement-relationship (explicit B→C interface). No unstated format/timing/state assumptions across boundaries. **No hidden coupling.**

## Step 6 — Dependency Order
1. **surfacing + sensemaking** (done) → the corpus + the stabilized model.
2. **A** (evaluation + verdict + reframe) — first.
3. **B** (prior reconciliation + re-test) — after A (needs the verdict + the two priors).
4. **D** (reach gate) — decidable early; GATES C. Default: deliver A+B, offer C.
5. **C** (fix-design) — after A+B, if D authorizes.

No circular dependencies. For the pipeline: **Innovation** works the fix design-space (C) — esp. how to express the criterion + enumerate the spine, and whether the standing design-discipline earns inclusion; **Critique** adjudicates C + stress-tests A/B (esp. the "earns its keep / not fully redundant with the prior omission-diff" claim and the REFINES+EXTENDS relationship); **CONCLUDE** delivers A+B (including the Inherited Commitments Re-test of BOTH priors) and presents D with C as the offered reach.

## Step 7 — Self-Evaluation

| Dimension | Verdict | Note |
|---|---|---|
| **Independence** | PASS | each Q answerable via defined interfaces alone (A from model+corpus; B from A's verdict + the priors; C from A+B+surfacing; D standalone) |
| **Completeness** | PASS | user's ask = A (is it useful + what form) + C (design it); B (prior relationship + re-test) required by the Synthesis Trigger; D (reach). Nothing falls through |
| **Reassembly** | PASS | A+B (+D-gated C) + interfaces = the full evaluation-plus-design with preserved reach and the two-prior re-test |
| **Determination-mechanism** | PASS | the runtime-determination-shaped sub-question ("is THIS principle must-wait?") IS the selection criterion, and it lives inside C (Q-C's first criterion) — not presupposed without a piece |
| **Tractability** | PASS | each piece = one focused pass |
| **Interface clarity** | PASS | all flows explicit; assumptions checked; no hidden coupling |
| **Balance** | PASS | A and C carry the weight (appropriate — the verdict IS the answer, the criterion+spine IS the reach); B moderate (the re-test is real work — 2 priors); D light. Not one-piece-80% |
| **Confidence** | HIGH | top-down (OUTPUT-REACH valley) and bottom-up (8 atoms → 4 pieces) agree |

**Decomposition version:** DV1 (single pass sufficient). No hidden-coupling or wrong-boundary signals → no DV2 trigger. **CONCLUDE flag:** because `_branch.md` declared a **Synthesis Trigger** consuming TWO prior findings, CONCLUDE must include the `## Inherited Commitments Re-test` section testing BOTH priors' commitments; the finding's frontmatter carries the relationship to both (`refines:` the dual-shaped claim of `2026-07-10_23-03`; `refines:`/extends the Pass-3 agenda of `2026-07-11_00-24`).
