# Decomposition — schemas_rationale_and_policy_list

## User Input

```text
/Users/ns/Desktop/projects/comprehenslate/devdocs/inquiries/2026-06-14_23-14__schemas_rationale_and_policy_list/_branch.md

Upstream outputs in same folder: articulate_simple.md + surfacing.md + sensemaking.md. The SV6 stabilized model is the substrate to decompose: three-layer architecture (TC / Policy / PC) generated by FP2 "Don't declare what the LLM can infer", plus a Policy-list catalog filtered through 4 tests, plus the Inherited Commitments Re-test with verdicts on 3 priors. Two items must be answered (rationale + policy list).

Save decomposition output to: /Users/ns/Desktop/projects/comprehenslate/devdocs/inquiries/2026-06-14_23-14__schemas_rationale_and_policy_list/decomposition.md
```

---

## Step 1 — Perceive Coupling Topology

### Elements in the whole

The deliverable is one finding answering two items. Stabilized substrate from Sensemaking produces these elements:

- **E1** — the FP2 principle: *"Don't declare what the LLM can infer."*
- **E2** — the three-layer architecture (TC / Policy / PC) with per-layer kind / holds / decision-axis / defaults-driver / consumer columns.
- **E3** — the four filters for the Item 2 catalog: structural shape (single-field BaseModel + Literal[] enum); language-agnosticism; authorial-edge-case category; LLM-can't-infer.
- **E4** — the dropped/changed commitments inventory (3 priors × verdicts per inherited commitment).
- **E5** — the Item 1 rationale narrative (the prose explaining why schemas.py simplification is sound).
- **E6** — the Item 2 catalog content (6 strong + 4 moderate + 5 deferred Policy candidates).
- **E7** — the Inherited Commitments Re-test section per CONCLUDE protocol.
- **E8** — the Next Actions (correction notes propagating to the 3 impacted prior findings).
- **E9** — the Open Questions (calibration-gate residual; cross-corpus completeness frontier; source_language_fluency homelessness).
- **E10** — the per-Policy-candidate sketch (class signature + default + edge-case description + filter-pass verdict).

### Coupling map

| Pair | Strength | Why |
|---|---|---|
| E1 ↔ E2 | STRONG | Architecture is generated by principle; changing the principle changes the architecture |
| E1 ↔ E3 | STRONG | Filters operationalize the principle |
| E1 ↔ E5 | STRONG | Narrative names the principle as load-bearing |
| E2 ↔ E5 | STRONG | Architecture is the load-bearing content of Item 1's rationale |
| E3 ↔ E6 | STRONG | Filters generate the catalog content |
| E3 ↔ E10 | STRONG | Per-candidate sketches use the filters as quality gates |
| E4 ↔ E7 | STRONG | Dropped commitments ARE the Inherited Commitments Re-test material |
| E4 ↔ E8 | MODERATE | Correction notes target the INVALID-tagged commitments |
| E6 ↔ E10 | STRONG | E10 is the per-item structure of E6 |
| E4 ↔ E5 | MODERATE | Item 1 narrative references dropped commitments as examples of FP2 application |
| E5 ↔ E6 | WEAK | Items 1 and 2 are conceptually paired but content-independent — each can be read alone |
| E9 ↔ E1 | MODERATE | Open question about calibration is generated by the principle's calibration-dependence |
| E9 ↔ E3 | MODERATE | Cross-corpus frontier is generated by Item 2's filter scope |
| Each pair within E10 (per-candidate-to-per-candidate) | WEAK | Each Policy candidate is independent of the others |

### Clusters

- **Cluster A (conceptual substrate):** E1 + E2 + E3 — the principle, architecture, and filters.
- **Cluster B (Item 1 content):** E5 + (uses E1 + E2 + E4 as inputs).
- **Cluster C (Item 2 content):** E6 + E10 + (uses E3 + parts of E2 as inputs).
- **Cluster D (Synthesis-trigger administrative):** E4 + E7 + E8.
- **Cluster E (residual signals):** E9.

### Major boundaries

- Between Cluster A and Clusters B/C: the conceptual substrate (principle/architecture/filters) supplies but does not depend on the narrative or catalog content. One-way dependency.
- Between Cluster B (Item 1) and Cluster C (Item 2): weak coupling. They share substrate but produce independent outputs.
- Between Cluster D and the others: re-test verdicts depend on the principle (Cluster A) but produce independent administrative content (the verdict table + correction notes).
- Between Cluster E and the others: residual signals are downstream of all reasoning; come last.

---

## Step 2 — Detect Boundaries (Top-Down)

The natural cuts are at the cluster boundaries identified above. Six pieces:

- **P1 — Conceptual substrate.** FP2 principle + three-layer architecture + four filters. (Cluster A.)
- **P2 — Item 1 rationale narrative.** The prose explaining why schemas.py simplification is sound. (Cluster B.)
- **P3 — Item 2 Policy-list catalog.** The list with per-candidate sketches. (Cluster C.)
- **P4 — Inherited Commitments Re-test.** Per-commitment verdicts on the 3 priors. (First half of Cluster D.)
- **P5 — Next Actions.** Correction notes propagation to impacted findings. (Second half of Cluster D.)
- **P6 — Open Questions.** Residuals carried forward. (Cluster E.)

---

## Step 3 — Validate Boundaries (Bottom-Up Check)

Atomic elements identifiable in the deliverable:

- **a1.** The literal phrase *"Don't declare what the LLM can infer."*
- **a2.** Each row of the three-layer architecture table (one row per TC / Policy / PC).
- **a3.** The NonMainLangPartsPolicy reference (the canonical Policy example).
- **a4.** Each individual Policy candidate (SourceApparatusPolicy, VoiceMarkingPolicy, ArchaicRegisterPolicy, HonorificsPolicy, FormulaicOpeningPolicy, EmbeddedPoetryPolicy, TransliterationStandardPolicy, PriorTranslationStancePolicy, AnachronismHandlingPolicy, CitationReferenceFormatPolicy, ScriptDirectionPolicy [deferred], PassageTypologyPolicy [deferred], ConsumptionModePolicy [deferred], ReadingSessionPolicy [deferred], OutputFinalityPolicy [deferred]).
- **a5.** Each specific inherited commitment (TC.chunking_granularity; SD.canonical_level; corpus mappings; split-placement principle; A4-driven defaults; Tier 1-2 preservation; the 4 edge-case SD additions; etc.).
- **a6.** Each correction note (REFINES X; CORRECTS Y; impact summary to add as Correction Notice).
- **a7.** Each open question item (calibration-gate; cross-corpus frontier; source_language_fluency homelessness; source_temporal_register routing as ArchaicRegisterPolicy).

### Atom grouping

| Atom | Belongs in | Top-down/bottom-up match? |
|---|---|---|
| a1 | P1 | YES |
| a2 | P1 | YES |
| a3 | P1 (canonical example) and P3 (as the first instance of the Policy layer) | YES — minor overlap, referenced from both |
| a4 | P3 | YES |
| a5 | P4 (status verdicts) and P2 (cited as examples of FP2 application) | YES — minor cross-reference flow |
| a6 | P5 | YES |
| a7 | P6 | YES |

No atom is split across pieces inappropriately; no atom is grouped with pieces it shouldn't be. **Boundary confidence: HIGH.**

---

## Step 4 — Express as Question Tree

### P1 — Conceptual substrate

**Question:** What principle generates the schemas.py simplification, and what architecture does it produce?

**Verification criteria:**
- [ ] FP2 stated verbatim: *"Don't declare what the LLM can infer."*
- [ ] The principle's operational form is given (the LLM-inferable test predicate: at integration time, ask "can the LLM derive this value from the source text plus the rest of the config?")
- [ ] Three-layer architecture table with rows for TC / Policy / PC and columns for kind / holds / decision-axis / defaults-driver / consumer.
- [ ] NonMainLangPartsPolicy positioned as the canonical example of the Policy layer.
- [ ] The four filters for Item 2 stated: structural shape (single-field BaseModel + `Literal[]` enum); language-agnosticism; authorial-edge-case category; LLM-can't-infer.
- [ ] The principle's relationship to FP1 (chunking finding's "schema ownership matches data ownership") stated explicitly — FP2 is a refinement, not a replacement.

### P2 — Item 1 rationale narrative

**Question:** Why does schemas.py make more sense than the 4-schema design (TC + SD + EmbeddedLanguageProfile + PC)?

**Verification criteria:**
- [ ] Names FP2 explicitly and traces its application.
- [ ] Shows how FP2 applies to each dropped/changed class: `SourceDescriptor` dropped (corpus facts are LLM-inferable); per-language `EmbeddedLanguage` replaced by language-agnostic `NonMainLangPartsPolicy`; `chunking_granularity` moved off TC to PC; `canonical_level` mapping dropped.
- [ ] Contrasts the architectures: before = 4 schemas with mixed declaration/policy content; after = 3 layers cleanly separated by who-decides-what.
- [ ] Names what's preserved (the 8 TC axes; the split-placement principle as FP1; the chunking_deep_dive's hybrid harmony-aware default mechanism).
- [ ] Flags residual issues: homeless `source_temporal_register` re-homed as `ArchaicRegisterPolicy` (forward reference to P3 catalog); calibration-gate dependency on current-LLM-capability for source-language inference.

### P3 — Item 2 Policy-list catalog

**Question:** What other scenarios fit the NonMainLangPartsPolicy shape (Policy layer)?

**Verification criteria:**
- [ ] At least 6 strong candidates, each with: class name + `Literal[]` enum values + sensible default + brief description of the authorial edge-case it governs + Nursi or general theological-translation anchor.
- [ ] 4 moderate candidates with shape-fit caveat (which filter they nearly fail).
- [ ] 5 deferred / out-of-shape candidates with explicit failing-filter notation.
- [ ] Each candidate's enum values are language-agnostic and tradition-agnostic.
- [ ] Each candidate's edge-case is authorial (not translator/publication/reader-side).
- [ ] The catalog includes `ArchaicRegisterPolicy` carrying the homeless `source_temporal_register` field from the edge-cases finding.

### P4 — Inherited Commitments Re-test

**Question:** What is the status of each commitment inherited from the 3 prior findings?

**Verification criteria:**
- [ ] Every commitment from `chunk_types_vs_mechanisms` finding enumerated with verdict (`RE-TESTED — confirmed` / `confirmed-but-frame-revised` / `found INVALID` / `INHERITED-WITHOUT-RE-TEST`) and structural evidence cited.
- [ ] Every commitment from `chunking_deep_dive` finding enumerated with verdict.
- [ ] Every commitment from `edge_cases_into_config_schema` finding enumerated with verdict, including per-edge-case sub-status (defer / drop / re-home / replaced) where applicable.
- [ ] Each `INVALID` verdict cites the structural contradiction (not just precedent).
- [ ] Each `confirmed-but-frame-revised` verdict names the frame premise that shifted and why.

### P5 — Next Actions

**Question:** What correction propagation is required to the impacted prior findings?

**Verification criteria:**
- [ ] List of impacted finding paths (the 3 priors) with per-finding correction summary.
- [ ] Frontmatter declarations in this finding name the priors via `REFINES:` (for confirmed-with-frame-revised) and `CORRECTS:` (for INVALID).
- [ ] Per-impacted-finding: text to add as Correction Notice section + which inherited commitments it overrides.
- [ ] Each action has Who / Gate / Why per the MUST/COULD/DEFERRED format from the finding template.

### P6 — Open Questions

**Question:** What open questions remain after this finding?

**Verification criteria:**
- [ ] Calibration-gate residual: the LLM-inferable test assumes current LLM capability for source-language inference; rare/dead languages may require declarative fields the current architecture omits. Stated with revival trigger.
- [ ] Cross-corpus completeness frontier: the Policy catalog was filtered by Nursi-load-bearing-ness; cross-corpus extension may surface additional candidates. Stated with revival trigger.
- [ ] `source_language_fluency` residual: this field had a path on SD but with SD dropped it has no schema home and isn't load-bearing for current work. Stated with revival trigger.
- [ ] Each open question is typed (Monitoring / Blocked / Research Frontier / Refinement Trigger) per the finding template.

---

## Step 5 — Map Interfaces

| Source | Target | Direction | What flows | Type |
|---|---|---|---|---|
| P1 | P2 | one-way | FP2 verbatim + three-layer architecture table + relationship to FP1 | Information / contract |
| P1 | P3 | one-way | Four filters + Policy class shape definition + canonical example pointer | Information / contract |
| P1 | P4 | one-way | FP2 principle as the test predicate used to determine which commitments contradict the new architecture | Information |
| P1 | P6 | one-way | The principle's calibration-dependence (generates the calibration-gate open question) | Information |
| P3 | P6 | one-way | The catalog's filtering scope (generates the cross-corpus completeness frontier) | Information |
| P4 | P5 | one-way | INVALID-tagged commitments → correction targets | Data |
| P2 | P5 | weak one-way | P2 references the Next Actions for narrative flow | Reference |
| P4 | P2 | weak one-way | P4's verdicts feed P2's "what was simplified past" examples | Data |

### Assumptions-not-data check

- **P2 assumes** P1 commits to the verbatim wording *"Don't declare what the LLM can infer."* Hidden coupling risk if P1 paraphrases. **Made explicit:** P1's verification includes verbatim form.
- **P3 assumes** the four filters from Sensemaking remain stable names through Innovation. **Made explicit:** P1's verification includes the four filters with stable names.
- **P4 assumes** per-commitment structural evidence is available in upstream artifacts (Sensemaking Phase 3 Ambiguity 6). **Verified.**
- **P5 assumes** Comprehenslate's finding-correction protocol (REFINES / CORRECTS frontmatter, Correction Notice sections). **Standard convention; not coupled to other pieces.**
- **P6 assumes** Sensemaking's Open flags + R7 frontier flag from Surfacing are accessible. **Verified.**
- **P3 assumes** each Policy candidate's edge-case description does not depend on the other candidates' descriptions (atomic catalog entries). **Verified.**

No hidden coupling detected.

---

## Step 6 — Dependency Order

```
Level 0 (no dependencies):
  P1 — Conceptual substrate

Level 1 (depend on P1; independent of each other):
  P2 — Item 1 rationale  ‖  P3 — Item 2 catalog  ‖  P4 — Inherited Commitments Re-test

Level 2 (depend on Level 1 outputs):
  P5 — Next Actions (depends on P4)  ‖  P6 — Open Questions (depends on P1 + P3)
```

P2, P3, P4 can be worked on in parallel after P1 is established.
P5 and P6 can be worked on in parallel after their respective Level-1 dependencies are complete.

No circular dependencies. P1 has no incoming dependencies, so it must be done first.

---

## Step 7 — Self-Evaluation

### Minimum 3 dimensions

| Dimension | Check | Pass? |
|---|---|---|
| **Independence** | Each piece's question answerable without reading sibling pieces (except through defined interfaces) | **PASS** — P2/P3/P4 each have a clear question that doesn't require the others (they share P1's substrate via the interface map). P5 and P6 consume Level-1 outputs through the explicit interfaces. |
| **Completeness** | Pieces cover the whole | **PASS** — every required section of a finding under the Synthesis Trigger is covered: rationale (P2), application/catalog (P3), inherited re-test (P4), next actions (P5), open questions (P6), shared conceptual substrate (P1). No aspect of the deliverable falls through gaps. |
| **Reassembly** | Pieces + interfaces reconstruct the whole | **PASS** — assembling P1 substrate into P2 and P3, plus the P4/P5/P6 sections, produces a finding that satisfies the CONCLUDE template + the Synthesis Trigger enforcement + both inquiry items. |

### Full 7-dimension evaluation

| Dimension | Result | Notes |
|---|---|---|
| Independence | PASS | See minimum above |
| Completeness | PASS | See minimum above |
| Reassembly | PASS | See minimum above |
| **Tractability** | PASS | Each piece is a single-focused-pass deliverable. P3 is the heaviest (~15 candidates × short sketches), but each per-candidate entry is independently tractable. |
| **Interface clarity** | PASS | All cross-piece flows explicit; assumptions-not-data check passed. |
| **Balance** | ACCEPTABLE | P2 and P3 are the largest (~25-30% of finding each); P4 is moderate (~15%); P1/P5/P6 are smaller. Distribution is acceptable — no one piece carries 80% of the work. |
| **Confidence** | HIGH | Top-down (cluster identification) and bottom-up (atom validation) agree on boundaries. |

### Determination-mechanism piece check

Does the question tree include a piece addressing HOW the LLM-inferable test is operationalized at runtime? P1's verification was augmented to include: *"the principle's operational form is given (the LLM-inferable test predicate: at integration time, ask 'can the LLM derive this value from the source text plus the rest of the config?')."* This satisfies the check — the test predicate is named, not left as implicit machinery.

### Failure mode check

| Mode | Detected? |
|---|---|
| 1 Premature Decomposition | NO — Sensemaking produced a stabilized SV6 model; decomposition cuts at the natural clusters. |
| 2 Wrong Boundaries | NO — cuts are at weak-coupling regions (between substrate and content, between items, between re-test and actions). |
| 3 Hidden Coupling | NO — Assumptions-not-data check applied; no unstated assumptions detected. |
| 4 Missing Pieces | NO — Reassembly passes; determination-mechanism check passes (P1 names the operational predicate). |
| 5 Over-Decomposition | NO — 6 pieces is appropriate for a multi-section finding with Synthesis Trigger; each piece is single-pass-tractable. Sub-decomposing P3 by candidate-tier would be over-decomposition. |
| 6 Ignoring Dependencies | NO — Dependency order explicit; parallel opportunities identified. |
| 7 Imbalanced Decomposition | NO — Balance check ACCEPTABLE; no single piece dominates. |

### Verdict

**PROCEED.** All 7 self-evaluation dimensions pass; no failure modes fired.
