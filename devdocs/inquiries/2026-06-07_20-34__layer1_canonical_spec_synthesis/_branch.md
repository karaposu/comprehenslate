# Branch: layer1_canonical_spec_synthesis

## Question

**Context.** The 8-axis Layer 1 (USER-FACING AXES) framework of Comprehenslate's translation-configuration system is now FULLY SPECIFIED across 9 prior findings: the root architectural finding (axis identity + meaning + concept) + 8 axis-level-definition findings (A1 cultural-reference-recognition through A8 analysis-depth). Each axis finding settled per-axis cardinality, level names, per-level operational substance, cross-axis boundaries, A4 matrix cross-validation, special patterns (composite-axis at A1; categorical at A4; explicit-zero at A6/A7/A8), and inheritance commitments. The A8 finding closed the framework at 8/8 axes complete and marked framework-synthesis as a Next Actions COULD item — the synthesis being THIS inquiry's purpose.

The synthesis produces the SINGLE CANONICAL LAYER 1 SPEC consolidating all 8 axes into one authoritative reference document. The deliverable serves as the single-source-of-truth for: (a) pydantic dataclass commit; (b) translator-AI prompt assembly with all 8 axes; (c) the per-purpose × per-axis default matrix synthesis (5 A4 purposes × 8 axes = 40-cell matrix); (d) any future framework-level changes (versioning anchor); (e) downstream Layer 1A UX preset catalog reference.

The synthesis is a STRUCTURAL operation (organizing 9 prior outputs into a single canonical document; the cognitive layer is the artifact shape, not redefinition of any axis). No axis-level decisions are re-litigated; the synthesis CONSOLIDATES decisions already made, RESOLVES carry-forward refinement notes (A4 maintenance pass; DEPTH_PROFILE refinement), and PRODUCES the cross-axis tables and matrices that none of the per-axis findings could produce alone (40-cell default matrix; cross-axis orthogonality verification matrix; receptive-only-NOT-APPLICABLE pattern compendium).

State the question:

- **Subject:** Canonical Layer 1 spec — single authoritative document consolidating all 8 user-facing axes of the translation-configuration framework.
- **Action:** Synthesize / consolidate (no axis-level redefinition; cross-axis consolidation + refinement-note resolution + matrix production).
- **Level:** Framework-level (cross-cutting all 8 axes).
- **Observation targets** (multiple, preserved separately):
  1. The SCHEMA — all 8 axis field names + level enums (including A1's composite-axis sub-fields with per-sub-field overrides; A2-A8 plain ordinal or categorical).
  2. The PER-AXIS PROSE — operationalizable per-level descriptions ready for translator-AI prompt context (consolidating each axis finding's per-level sections).
  3. The CROSS-AXIS INTERACTIONS — consolidated table specifying which axes interact (A1↔A3 / A5↔A6 / A5↔A7 / A6↔A7 / A7↔A8 / A6↔A8 / A5↔A8 / A4↔all-others-via-matrix / policy interactions).
  4. The PER-PURPOSE × PER-AXIS DEFAULT MATRIX — 40-cell matrix (5 A4 purposes × 8 axes) consolidating per-axis A4 cross-validation tables from each axis finding.
  5. The SPECIAL PATTERNS COMPENDIUM — composite-axis pattern (A1); categorical pattern (A4); explicit-zero pattern (A6/A7/A8); asymmetric pattern (A5 no `heavily-domesticated`); dual-tier default (A8); content-type-by-level table pattern (A8); action-permission table pattern (A7); harmony report apparatus channel (A6); multi-meaning render rules (A7); multi-meaning analysis at A8 (three-layer treatment).
  6. The RECEPTIVE-ONLY NOT APPLICABLE PATTERN — consolidated across A4/A5/A6/A7/A8 (5 axes are translator-strategy; receptive-only applies only to Reader axes A1/A2/A3 if at all).
  7. The DEFAULT-DERIVATION CHAINS — A4 chain (A5/A6/A7); dual-tier (A8 = A4 chain + conservative-bias); conservative-bias fallback (root principle).
  8. The CARRY-FORWARD REFINEMENT NOTES RESOLUTION — A4 finding refinement notes from A6 (`moderate` → `light` for A6), A7 (`moderate` → `standard`; `MAX rich` → `scholarly` for A7), A8 (`deep+scholarly` → `scholarly` for A8); DEPTH_PROFILE refinement (add `none` to .env.example); these should be bundled into a single A4 maintenance pass and a single .env.example pass; the synthesis specifies the bundle.
  9. The CROSS-AXIS ORTHOGONALITY VERIFICATION — explicit table showing each pair of axes either orthogonal (independent) or interacting (with cross-validation rule); 28 unordered pairs (8 choose 2).
  10. The FRAMEWORK COMPLETION DECLARATION — 8/8 Layer 1 axes specified; Layer 2 POLICY (5 principles); Layer 3 SOURCE-DESCRIPTION (deferred); Layer 4 SYSTEM-FLAGS (out of scope); Layer 1A UX preset catalog (deferred); schema commit (next).
  11. The PER-AXIS IC AGGREGATION — total ICs across the 8 axis findings (count + categorization: inherited-OK / refined / new); meta-IC re-test (commitments inherited from root by each axis finding).
  12. The POLICY INTERACTION MAP — how axes interact with the 5 always-on policies (multi-meaning preservation policy fires at all polysemy cases; A7 renders + A8 analyzes; register-alternation policy ties to A6; polysemy-via-local-construction policy is invariant; nazm preservation policy activates at A6 ≥ light; no-smoothing policy is invariant).
  13. The TRANSLATOR-AI PROMPT ASSEMBLY GUIDANCE — how the canonical spec maps to AI prompt context (per-axis prose + cross-axis tables + matrices + policies — assembled at runtime).
  14. The PYDANTIC DATACLASS STRUCTURE HINT — what the canonical spec implies for the pydantic shape (Literal types per axis; composite-axis for A1; receptive-only NOT APPLICABLE explicit; default values per axis).
  15. The VERSIONING ANCHOR — the canonical spec is v1.0 of the Layer 1 framework; future changes increment version + log in a Changelog section.
  16. The DOWNSTREAM-UNBLOCK LIST — what becomes possible after canonical spec commit (schema commit; default matrix synthesis is THIS inquiry; UX presets; policy operational specs; source-description schema; runtime conflict resolution; framework-level UX).

- **Deliverable shape:** Single canonical Layer 1 spec document (finding.md IS the canonical reference) containing: framework summary + 4-layer architecture recap + per-axis sections (8 axes; each with concept / user-question / pattern / cardinality / level enum / per-level prose / cross-axis boundaries / default chain / receptive-only applicability) + cross-axis interaction tables + per-purpose × per-axis default matrix (40 cells) + special patterns compendium + carry-forward refinement notes resolution (bundled A4 + .env.example maintenance) + policy interaction map + receptive-only NOT APPLICABLE pattern + framework completion declaration + versioning anchor + downstream-unblock list + Inherited Commitments Re-test (synthesis-level).

**State the question:** **What is the single canonical Layer 1 spec consolidating all 8 user-facing axes of Comprehenslate's translation-configuration framework — combining schema (axis fields + level enums) + per-axis prose (operationalizable per-level descriptions) + cross-axis interaction tables + per-purpose × per-axis default matrix (5 × 8 = 40 cells) + special patterns compendium (composite-axis / categorical / explicit-zero / asymmetric / dual-tier / content-type-by-level / action-permission / harmony report / multi-meaning three-layer) + receptive-only NOT APPLICABLE pattern across translator-strategy axes + carry-forward refinement notes resolution (bundled A4 + .env.example maintenance) + policy interaction map (5 always-on policies × 8 axes) + framework completion declaration (8/8 Layer 1 axes specified) + versioning anchor (v1.0) + downstream-unblock list (schema commit / preset catalog / policy specs / source-description / runtime conflict resolution / UX surface) — without re-litigating any axis-level decision already settled in the 9 prior findings?**

## Goal

- **Criterion.** A single authoritative document serving as: (a) the single-source-of-truth for the framework; (b) the input to pydantic dataclass commit; (c) the input to translator-AI prompt assembly; (d) the versioning anchor (v1.0) for future framework changes; (e) the reference for Layer 1A UX preset catalog design. Each per-axis section operationalizable as AI prompt context. Cross-axis tables and matrices deterministic for runtime conflict resolution.

- **Use case.** The user (and downstream engineers / writers) will commit the canonical spec; pydantic dataclasses derive from it; translator-AI prompt context is assembled from it; UX presets reference it; runtime conflict resolution consults the cross-axis tables in it.

- **Desired outcome.** Stable canonical Layer 1 spec at v1.0 with all consolidation tasks complete: schema unified; per-axis prose consolidated; cross-axis interactions tabulated; default matrix (40 cells) produced; special patterns compiled; receptive-only NOT APPLICABLE pattern explicit; refinement notes bundled; policy interaction mapped; framework completion declared; downstream-unblock list specified.

- **What would fail.**
  - Re-litigating any axis-level decision (cardinality / labels / per-level definitions / patterns) — out of scope; per-axis findings are settled.
  - Missing the per-purpose × per-axis default matrix (40-cell consolidation of per-axis A4 tables).
  - Missing the cross-axis interaction table (28 pair table).
  - Missing the special patterns compendium (multiple novel patterns identified across A1-A8).
  - Missing the receptive-only NOT APPLICABLE pattern compendium (5 translator-strategy axes).
  - Failing to bundle the carry-forward A4 + .env.example refinement notes (4 carry-forwards: A6 + A7 + A8 to A4; A8 to DEPTH_PROFILE).
  - Failing to specify the framework completion declaration or the downstream-unblock list.
  - Producing a document so long it loses operability as canonical reference.
  - Failing to include policy interaction map (5 always-on policies × per-axis interaction).

## Source Input

```text
now lets synthesize all 8 axes into canonical Layer 1 spec
```

## Scope Check

Question covers goal: YES.

**Specific-vs-pattern check.** "Synthesize all 8 axes into canonical Layer 1 spec" pinpoints the synthesis task. The inquiry addresses the full canonical spec — schema + per-axis prose + cross-axis tables + default matrix + special patterns + receptive-only + refinement notes resolution + policy interaction + framework completion + versioning + downstream-unblock.

**Decoupling.** All 8 axis findings + root finding inherited and re-tested at the synthesis level (NOT re-litigated; verified for cross-axis consistency).

## Layer Commitment

Primary layer: **structural**.

The synthesis is a structural commit — it organizes 9 prior outputs into a single canonical artifact. The artifact's sections, organization, schema, and shape are the synthesis's primary work. No axis-level meaning is being redefined; no axis-level process is being re-specified. The deliverable is the canonical artifact's structure.

Other layers considered and out of scope:
- **Meaning** — the axis identities and meanings are settled in root + per-axis findings; not being redefined here.
- **Process** — the translator-AI runtime process is downstream of this canonical spec; not being re-specified here.

## Synthesis Trigger

This inquiry consolidates / synthesizes commitments from 9 prior inquiry outputs:

- `devdocs/inquiries/2026-06-05_14-14__translation_config_axes/finding.md` — root architectural finding. Commits to: 4-layer architecture (USER-FACING / POLICY / SOURCE-DESCRIPTION / SYSTEM-FLAGS); 8 axes organized into 4 families (Reader / Purpose / Strategy / Depth); composite-axis pattern (A1); 5 always-on policies (Layer 2); 2-tier default principle (Purpose-driven + conservative-bias fallback).

- `devdocs/inquiries/2026-06-06_11-47__a1_cultural_reference_recognition_levels/finding.md` — A1 final sub-field (closes A1 composite). Commits to: cultural-reference-recognition sub-field with handling actions vocabulary (INLINE-GLOSS / FOOTNOTE / DOMESTICATE-disfavored / KEEP-AS-IS / EXPLICATE-FUNCTION); per-level action density.

- `devdocs/inquiries/2026-06-06_12-37__a2_domain_expertise_levels/finding.md` — A2 Domain Expertise. Commits to: 5 levels `lay | aware | educated | trained | expert`.

- `devdocs/inquiries/2026-06-06_13-33__a3_source_culture_levels/finding.md` — A3 Source Culture. Commits to: 5 levels `outsider | acquainted | familiar | heritage | source-native`; A3 handling actions vocabulary.

- `devdocs/inquiries/2026-06-06_14-05__a4_purpose_categories/finding.md` — A4 Purpose. Commits to: 5 categorical levels `scholarly | devotional | casual | language-learning | performance`; per-purpose × per-other-axis default matrix (the primary input for THIS synthesis's 40-cell matrix); receptive-only NOT APPLICABLE.

- `devdocs/inquiries/2026-06-06_14-38__a5_source_fidelity_levels/finding.md` — A5 Source Fidelity. Commits to: 4 asymmetric levels `foreignized-max | foreignized | balanced | lightly-domesticated` (no `heavily-domesticated` — policy embedded structurally); NEW translator-strategy 4-component template; receptive-only NOT APPLICABLE; chain default via A4 matrix.

- `devdocs/inquiries/2026-06-06_14-59__a6_form_preservation_levels/finding.md` — A6 Form Preservation. Commits to: 5 levels `off | minimal | light | standard | maximum`; ties to Tier 1-4 system from harmony_layer.md; A6 produces harmony report at Levels 3+ as separate apparatus channel; receptive-only NOT APPLICABLE; A4 finding refinement note (`moderate` → `light` for A6).

- `devdocs/inquiries/2026-06-07_19-07__a7_scaffolding_levels/finding.md` — A7 Scaffolding. Commits to: 5 levels `off | minimal | standard | rich | scholarly`; scaffolding budget gating A1/A3 actions (action-permission table); 5 multi-meaning render rules + A7=off → EXPLICATE-FUNCTION fallback; runtime conflict resolution (strict + fallback + harmony-report flag); receptive-only NOT APPLICABLE; A4 finding refinement note (`moderate` → `standard`; `MAX rich` → `scholarly` for A7).

- `devdocs/inquiries/2026-06-07_19-51__a8_analysis_depth_levels/finding.md` — A8 Analysis Depth. Commits to: 5 levels `none | surface | standard | deep | scholarly` (REVISED from root's 4 — `none` added at position 1); content-type-by-level table (12 × 5 = 60 cells); A7↔A8 boundary spec (3 framings); harmony-report-location (standalone + cross-references at A8 deep+); multi-meaning analysis at A8 (three-layer treatment); dual-tier default (A4 chain + conservative-bias); A4 finding refinement note (`deep+scholarly` → `scholarly` for A8); DEPTH_PROFILE refinement (add `none` to .env.example); receptive-only NOT APPLICABLE; DEPTH-family CLOSURE (1/1) + FRAMEWORK CLOSURE (8/8).

Inherited commitments to re-test at the synthesis level:
- 4-layer architecture (root) — re-confirm; canonical spec carries forward.
- 8-axis structure + 4 families (root) — re-confirm; canonical spec is structured by family.
- Composite-axis pattern (root + A1) — re-confirm; pattern compendium documents.
- 5 always-on policies (root) — re-confirm; policy interaction map documents.
- 2-tier default principle (root) — re-confirm; refined by A8's dual-tier; canonical spec documents both.
- Each axis's per-level definitions — re-confirm; canonical spec carries forward per-axis sections.
- Each axis's A4 cross-validation — re-confirm; canonical spec consolidates into 40-cell matrix.
- Each axis's receptive-only applicability (A1/A2/A3 receptive-only possible; A4/A5/A6/A7/A8 NOT APPLICABLE) — re-confirm; pattern documented.
- Each axis's cross-axis boundaries — re-confirm; canonical spec consolidates into orthogonality verification matrix.
- Carry-forward refinement notes (A6 + A7 + A8 to A4; A8 to DEPTH_PROFILE) — RESOLVE by bundling into a single A4 maintenance pass + single .env.example pass; canonical spec specifies the bundle.
- A7 finding's frontier flags (harmony-report-location; multi-meaning analysis at A8; A7↔A8 boundary) — resolved by A8; canonical spec confirms.
- A8 finding's DEPTH_PROFILE refinement — RESOLVE in canonical spec by specifying the .env.example update.
- FRAMEWORK CLOSURE marker (A8) — canonical spec carries the meta-claim of 8/8 specification.
