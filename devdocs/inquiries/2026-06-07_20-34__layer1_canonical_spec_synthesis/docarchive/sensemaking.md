# Sensemaking — layer1_canonical_spec_synthesis

## User Input
`/Users/ns/Desktop/projects/comprehenslate/devdocs/inquiries/2026-06-07_20-34__layer1_canonical_spec_synthesis/_branch.md` (with surfacing output at the same folder)

---

## SV1 — Baseline Understanding

Synthesize all 8 axes (root + A1-A8) into single canonical Layer 1 spec — self-contained dense (per user). Consolidate schema + per-axis prose + cross-axis interaction tables + 40-cell default matrix + special patterns compendium + receptive-only NOT APPLICABLE pattern + carry-forward refinement notes resolution (bundled A4 + .env.example maintenance) + policy interaction map + framework completion declaration (8/8) + versioning anchor (v1.0) + downstream-unblock list — without re-litigating any axis-level decision already settled in the 9 prior findings.

---

## Phase 1 — Cognitive Anchor Extraction

### Constraints
- **C1** Synthesis-not-redefinition: no axis-level decisions re-litigated; the 9 priors are settled.
- **C2** Self-contained dense (per user's explicit choice): full per-axis prose reproduced in the canonical spec.
- **C3** STRUCTURAL layer commitment (Layer Commitment section in _branch.md): the synthesis's primary work is artifact organization.
- **C4** Synthesis Trigger active: REQUIRED `## Inherited Commitments Re-test` section per CONCLUDE protocol; 9 priors carry commitments to re-test.
- **C5** All 8 axes specified; FRAMEWORK CLOSURE at 8/8 (per A8).
- **C6** 5 always-on policies enumerated at root; operational specs deferred but POLICY × AXIS interaction map is in-scope here.
- **C7** A4 matrix verbiage inconsistent with downstream-axis labels — 4 carry-forward refinement notes accumulated across A5/A6/A7/A8 findings.
- **C8** DEPTH_PROFILE .env.example refinement (add `none`) accumulated from A8.

### Key Insights

- **KI1** The canonical spec's STRUCTURE is the synthesis's primary artifact. Sections are determined by content categories from the 9 priors organized for canonical-reference use. Settled structure (synthesis-level):
  1. **Frontmatter + spec metadata** — version v1.0; status; model; refines list (the 9 priors).
  2. **Framework Overview** — 4-layer architecture; 4-family organization; 8-axis inventory; what's in-scope (Layer 1) vs out-of-scope (Layers 2/3/4 + Layer 1A presets).
  3. **Architectural Patterns Compendium** — composite-axis; categorical; asymmetric; explicit-zero level; dual-tier default; content-type-by-level; action-permission table; harmony report; multi-meaning three-layer treatment. Each pattern documented with where it applies + why it exists.
  4. **Per-Axis Sections** (A1-A8, one section each) — each containing: concept + user-question + pattern + cardinality + level enum + per-level prose (all components per per-axis 4-component template) + cross-axis boundaries + default chain + receptive-only applicability + handling action vocabulary (if applicable). Reproduces the full per-axis operational prose.
  5. **Cross-Axis Interaction Matrices**:
     - 5.1 Per-purpose × per-axis default matrix (40 cells: 5 A4 × 8 axes)
     - 5.2 Cross-axis orthogonality verification matrix (28 pairs: 8 choose 2)
     - 5.3 Apparatus channel separation table (3 channels × 8 axes)
     - 5.4 Action-vocabulary map (which axes have own actions; which modulate; which produce content directly)
  6. **Policy Interaction Map** — 5 always-on policies × 8 axes = 40-cell table; cells noted as "invariant" / "activation gate at level X" / "modulates render" / "produces analysis at high level" / "not applicable" with rationale.
  7. **Receptive-only Applicability Compendium** — APPLIES (A1, A2, A3); NOT APPLICABLE (A4-A8 translator-strategy axes).
  8. **Default-Derivation Chains** — A1-A3 conservative-bias-LOWER; A4 silent → `casual`; A5/A6/A7 chain via A4 matrix; A8 dual-tier (A4 chain when A4 set; conservative-bias `standard` when A4 silent).
  9. **Pydantic Dataclass Hint** — Literal types per axis + composite class for A1; receptive-only mode notes; not actual code (the canonical spec stays at framework-spec layer, not implementation layer).
  10. **Said Nursi Corpus Illustrative Mapping** — per axis, the Nursi-corpus level the AI uses as the project's primary anchor.
  11. **Schema Commit Status + Versioning** — v1.0; status: active; changelog format; revision triggers.
  12. **Carry-Forward Maintenance Bundle** — A4 finding refinement notes (4-5 items) + .env.example DEPTH_PROFILE refinement — bundled as single Next Actions MUST.
  13. **Downstream-Unblock List** — Next Actions COULD items (preset catalog; policy operational specs; source-description schema; runtime conflict resolution; UX surface).
  14. **Inherited Commitments Re-test** — synthesis-level table.

- **KI2** The 40-cell per-purpose × per-axis default matrix consolidates 5 per-axis A4 cross-validation tables. Each per-axis finding provides a row (per-purpose A4 → per-axis-level default). The synthesis produces the matrix:

  | Purpose | A1 headline | A2 | A3 | A5 | A6 | A7 | A8 |
  |---|---|---|---|---|---|---|---|
  | scholarly | advanced | educated | any (often outsider-with-study) | foreignized | standard | rich | deep |
  | devotional | conversational | aware-to-educated | source-native / heritage | foreignized-max | standard | standard | standard |
  | casual | daily | lay-to-aware | outsider-to-acquainted | balanced | light | rich | surface |
  | language-learning | conversational | lay | outsider | foreignized | standard | scholarly | scholarly |
  | performance | conversational | any | any | balanced | maximum | minimal | none (or surface) |

  Refinement notes applied during synthesis (vs A4 finding's original verbiage):
  - A6 column: A4 "moderate" → `light`; A4 "high" → `standard`; A4 "MAXIMUM" → `maximum` (per A6 finding refinement note).
  - A7 column: A4 "moderate" → `standard`; A4 "rich (help unfamiliar)" → `rich`; A4 "MAX rich" → `scholarly`; A4 "minimal" → `minimal` (per A7 finding refinement note).
  - A8 column: A4 `surface` → `surface`; A4 `standard` → `standard`; A4 `deep` → `deep`; A4 `deep+scholarly` → `scholarly` (per A8 finding refinement note); A4 `surface` (performance) → `surface` (or `none` for pure oral).
  - A5 column: A4 `balanced-to-foreignized` (language-learning) → `foreignized` (per A5 finding refinement note).

- **KI3** Cross-axis orthogonality verification matrix (28 pairs from 8 choose 2). Most pairs documented in individual findings; synthesis tabulates with one-line interaction rule per cell.

  Settled at synthesis (compact):
  - **A1↔A2**: orthogonal (general fluency vs specialist knowledge).
  - **A1↔A3**: orthogonal (competence vs identity; four-corners).
  - **A1↔A4**: orthogonal (reader vs purpose).
  - **A1↔A5**: orthogonal-with-modulation (A5 modulates which A1 actions fire).
  - **A1↔A6**: orthogonal (reader-fluency vs structural-form).
  - **A1↔A7**: gated (A7 budget gates A1 cultural-reference-recognition's INLINE-GLOSS/FOOTNOTE/DOMESTICATE actions).
  - **A1↔A8**: orthogonal (A8 produces in separate channel; A1 reader-side determines style of analysis content).
  - **A2↔A3**: orthogonal (competence vs identity; four-corners).
  - **A2↔A4**: orthogonal (expertise vs purpose).
  - **A2↔A5**: orthogonal-with-modulation (A5 modulates A2 action density).
  - **A2↔A6**: orthogonal (expertise vs form).
  - **A2↔A7**: orthogonal-with-modulation (A7 budget affects A2 specialist-debate FOOTNOTE density).
  - **A2↔A8**: orthogonal (A8 content style adapts to A2 reader).
  - **A3↔A4**: orthogonal (identity vs purpose).
  - **A3↔A5**: orthogonal-with-modulation (A5 modulates which A3 actions fire).
  - **A3↔A6**: orthogonal (cultural identity vs structural form).
  - **A3↔A7**: gated (A7 budget gates A3 cultural-handling actions: TRANSLITERATE-WITH-GLOSS / FLAG-CULTURAL-CONTEXT / BRIDGE-CULTURAL-DISTANCE).
  - **A3↔A8**: orthogonal (A8 content style adapts to A3 reader).
  - **A4↔A5**: drives-default (A4 matrix supplies A5 default).
  - **A4↔A6**: drives-default (A4 matrix supplies A6 default).
  - **A4↔A7**: drives-default (A4 matrix supplies A7 default).
  - **A4↔A8**: drives-default (A4 matrix supplies A8 default).
  - **A5↔A6**: orthogonal (lexical surface vs structural form; four-corners independence).
  - **A5↔A7**: orthogonal-with-modulation (A5 stance affects which budget-FREE foreignization-preserving actions A7=off falls back to).
  - **A5↔A8**: orthogonal (A5 modulates A8 content emphasis: foreignized → etymology emphasis; lightly-domesticated → target-language-equivalent justification emphasis).
  - **A6↔A7**: orthogonal (harmony report channel vs text-surface scaffolding channel; both apparatus but distinct channels).
  - **A6↔A8**: orthogonal (harmony report channel vs separate-sections channel; cross-references at A8 deep+ + A6 Levels 3+).
  - **A7↔A8**: orthogonal (text-surface vs separate-sections; LOCATION + SCOPE + AUDIENCE-INTERACTION 3-framing rule).

  Categories of relations: **orthogonal** (independent), **gated** (one axis structurally constrains another's actions), **orthogonal-with-modulation** (one axis affects another's action selection but axes remain independent), **drives-default** (A4 supplies defaults for translator-strategy axes via matrix).

- **KI4** Apparatus channel separation table (3 channels × 8 axes):

  | Channel | A1 | A2 | A3 | A4 | A5 | A6 | A7 | A8 |
  |---|---|---|---|---|---|---|---|---|
  | Text-surface (in-page) | produces (via A7 budget) | produces (via A7 budget) | produces (via A7 budget) | drives defaults | modulates selection | does not contribute | OWNS | does not contribute |
  | Harmony report (meta-analytic) | does not contribute | does not contribute | does not contribute | drives defaults | does not contribute | OWNS (at Levels 3+) | flags conflicts | cross-references at deep+ |
  | Separate-sections | reader-side informs content style | reader-side informs content style | reader-side informs content style | drives defaults | content emphasis modulator | cross-references at deep+ | does not contribute | OWNS |

  This table makes apparatus channel ownership explicit. A7 owns in-page; A6 owns harmony report; A8 owns separate-sections. Other axes contribute, drive defaults, or modulate selection.

- **KI5** Action-vocabulary map (which axes do what at runtime):

  | Axis | Own actions? | Role |
  |---|---|---|
  | A1 (5 sub-fields) | YES (per sub-field) | Reader-side; AI selects actions per encountered word/reference based on sub-field level |
  | A2 | YES (9 actions in 2 categories + 1 bridge) | Reader-side; AI selects per technical term and discourse-level reference |
  | A3 | YES (10 actions in 4 categories) | Reader-side; AI selects per cultural item |
  | A4 | NO (drives defaults for other axes) | User-configuration; configuration-time only |
  | A5 | NO (modulates A1 + A3 actions) | Translator-strategy; sets preference order over existing vocabularies |
  | A6 | NO (3-Pass methodology + per-tier preservation rules) | Translator-strategy; methodology applies at runtime per source passage |
  | A7 | NO (scaffolding budget gates A1/A3 actions + 5 multi-meaning render rules) | Translator-strategy; budget enforcement at runtime |
  | A8 | YES (12 content-types produced directly) | Translator-strategy; A8 PRODUCES content (the only axis with content-production role) |

- **KI6** Policy interaction map (5 always-on policies × 8 axes = 40 cells). Each cell notes the policy's behavior at this axis. Most cells are "not applicable" (the policy fires regardless of axis); load-bearing cells are the gates and render mechanisms.

  | Policy | A1 | A2 | A3 | A4 | A5 | A6 | A7 | A8 |
  |---|---|---|---|---|---|---|---|---|
  | Multi-meaning preservation | informs cultural-ref handling at high levels | informs technical-term ambiguity | informs cultural-religious ambiguity | drives render emphasis (devotional max) | modulates render bias | not applicable (orthogonal to form) | RENDERS preserved senses per A7 level (5 mechanisms + EXPLICATE-FUNCTION fallback at off) | ANALYZES preserved senses at deep+ (exegetical history) |
  | Register-alternation preservation | feeds A6 Tier 1 register-rotation | not applicable | informs cultural-register specificity | drives form emphasis | modulates lexical register choice | INVARIANT at Levels 3+; Tier 1/2 entry | not applicable | not applicable |
  | Polysemy-via-local-construction | informs sense selection | not applicable | informs cultural-religious sense | not applicable | not applicable | not applicable | not applicable | not applicable |
  | Nazm preservation | not applicable | not applicable | informs source-cultural prosody | drives form emphasis | not applicable | ACTIVATION GATE at Level 3 `light`; INVARIANT at Levels 3+ | not applicable | not applicable |
  | No-smoothing | INVARIANT | INVARIANT | INVARIANT | INVARIANT | INVARIANT | INVARIANT | INVARIANT | INVARIANT |

  Load-bearing cells (where policy interacts substantively with axis):
  - Multi-meaning × A7 (render) and × A8 (analysis) — three-layer treatment.
  - Register-alternation × A6 (Tier 1/2 structure).
  - Nazm preservation × A6 (activation gate).
  - No-smoothing × all (invariant; lesser-evil principle).

- **KI7** Receptive-only applicability compendium (consolidated):
  - **APPLIES** to A1 (5 sub-fields), A2, A3 — Reader family axes. The reader RECOGNIZES content; doesn't produce. Receptive-only is the default mode.
  - **NOT APPLICABLE** to A4, A5, A6, A7, A8 — translator-strategy / user-configuration axes. The axis captures the user's strategic choice or the translator-AI's stance, NOT a reader property. Explicit non-inheritance is documented in each of A4-A8 findings.

  The pattern is consistent across the translator-strategy axes: Layer 1's translator-strategy axes (A4-A8) are user-configuration; the Reader family (A1-A3) is reader-property. The receptive-only / productive distinction applies only to reader-property axes (and even there, productive is currently out-of-scope — readers RECEIVE translations; they don't write).

  At the synthesis level: the canonical spec documents both APPLIES + NOT APPLICABLE patterns explicitly so future axes can be classified cleanly.

- **KI8** Default-derivation chains (consolidated):
  - **A1 (5 sub-fields)**: conservative-bias-LOWER per sub-field; headline level propagates to sub-field defaults; sub-field overrides supersede.
  - **A2**: conservative-bias-LOWER; default `lay`.
  - **A3**: conservative-bias-LOWER; default `outsider`.
  - **A4**: default `casual` when silent (categorical equivalent of conservative-bias-LOWER).
  - **A5**: A4 chain → matrix default; A4 silent → casual → A5 = `balanced`.
  - **A6**: A4 chain → matrix default; A4 silent → casual → A6 = `light` (activation gate threshold — project defaults to nazm-policy-active).
  - **A7**: A4 chain → matrix default; A4 silent → casual → A7 = `rich`.
  - **A8**: DUAL-TIER. A4 set → matrix default. A4 silent → conservative-bias `standard` (A8 unique among translator-strategy axes — A4 chain casual default `surface` would under-defend cold-start; conservative-bias defends).

  At the synthesis level: A8's dual-tier is the only multi-tier default chain; A5/A6/A7 use single-tier chain through A4; A1/A2/A3 use single-tier conservative-bias-LOWER directly.

- **KI9** Carry-forward refinement notes RESOLUTION:
  - **A4 finding maintenance bundle** (4-5 refinements should ride a single pass):
    - From A5 finding: language-learning A5 default `balanced-to-foreignized` → `foreignized` (A5 4-level structure has no in-between).
    - From A6 finding: A4 "moderate" → A6 `light`; A4 "high" → A6 `standard`; A4 "MAXIMUM" → A6 `maximum`.
    - From A7 finding: A4 "moderate" → A7 `standard`; A4 "MAX rich" → A7 `scholarly`.
    - From A8 finding: A4 language-learning A8 `deep+scholarly` → `scholarly` (resolves ambiguity).
  - **`.env.example` DEPTH_PROFILE refinement** (from A8 finding): add `none` to accepted values (or document historical default as `surface` for legacy compatibility while exposing `none` as the explicit-zero case).

  At the synthesis level: these are bundled into TWO Next Actions MUST items in the canonical spec — one A4 maintenance pass + one .env.example pass. The canonical spec's own 40-cell matrix already reflects the refinements (KI2); these refinements need to PROPAGATE BACK to A4 finding for source-of-truth alignment.

- **KI10** Pydantic dataclass hint (canonical spec stays at framework-spec layer, not implementation layer):
  - The canonical spec documents what the pydantic structure WILL LOOK LIKE (composite class for A1; Literal types per other axes; receptive-only mode as optional field on Reader-family axes only) but does NOT write the actual code.
  - Code generation is a downstream task (Next Actions COULD) — possibly auto-generated from the canonical spec by a tool.

- **KI11** Said Nursi corpus mapping included (per user's "self-contained dense" choice; the project's primary anchor):
  - Each per-axis section in canonical spec includes the Nursi-corpus level the AI uses (already documented in per-axis findings; canonical spec consolidates).
  - The corpus mapping serves as the project's CONCRETE OPERATIONAL ANCHOR.

- **KI12** Cross-axis interaction taxonomy formalized (4 relation types — KI3 enumerated):
  - **orthogonal** — fully independent; no interaction at runtime.
  - **gated** — one axis structurally constrains another's action vocabulary (A7 gates A1/A3 budget-consuming actions).
  - **orthogonal-with-modulation** — one axis affects another's action selection but axes remain independent (A5 modulates A1/A3 preference order; A7 affects A2 FOOTNOTE density).
  - **drives-default** — A4 supplies defaults for other axes via matrix (special role).

  The 28-pair matrix uses these 4 relation types as cell labels.

- **KI13** Versioning + Changelog:
  - Canonical spec is v1.0 (first canonical synthesis after 8/8 axis closure).
  - Future revisions increment version + log in a Changelog section within the canonical spec (inline; not separate file).
  - Refinement triggers: any axis-level revision (cardinality / labels / per-level definitions); per-target-language refinements; sub-field additions to A1.

- **KI14** What the canonical spec is + what it isn't:
  - **IS**: single-source-of-truth Layer 1 reference; AI prompt context source; downstream-implementation input; reference for UX presets, policy operational specs, source-description schema.
  - **IS NOT**: pydantic implementation (downstream); runtime behavior specification (downstream); UX layer (deferred); Layer 2 policy operational specs (separate inquiries); Layer 3 source-description schema (separate inquiry); Layer 4 system-flags spec (out of scope).

- **KI15** Downstream-unblock list (canonical spec's enabling effects):
  - Schema commit (pydantic dataclass) — MUST.
  - Per-purpose × per-axis default matrix already PRODUCED here.
  - Layer 1A UX preset catalog — COULD (deferred per root).
  - Layer 2 POLICY operational specs (5 policies; multi-inquiry) — COULD.
  - Layer 3 SOURCE-DESCRIPTION schema — COULD.
  - UX-layer runtime conflict surface — COULD.
  - Translator-AI prompt assembly with all 8 axes + policies — MUST.

### Structural Points
- **SP1** Canonical spec structure: 14 sections per KI1.
- **SP2** 40-cell per-purpose × per-axis default matrix produced inline (KI2).
- **SP3** 28-pair orthogonality verification matrix with 4 relation types (KI3).
- **SP4** 3-channel apparatus separation table (KI4).
- **SP5** Action-vocabulary map (KI5).
- **SP6** Policy interaction map 5 × 8 = 40 cells (KI6).
- **SP7** Receptive-only APPLIES + NOT APPLICABLE compendium (KI7).
- **SP8** Default-derivation chains documented per axis (KI8).
- **SP9** A4 maintenance bundle + .env.example refinement = 2 carry-forward Next Actions MUST items (KI9).
- **SP10** Pydantic dataclass HINT not actual code (KI10).
- **SP11** Said Nursi corpus mapping per axis as concrete operational anchor (KI11).
- **SP12** Cross-axis interaction taxonomy 4 relation types: orthogonal / gated / orthogonal-with-modulation / drives-default (KI12).
- **SP13** Versioning v1.0 + inline Changelog section (KI13).
- **SP14** Canonical spec IS / IS NOT scope statement (KI14).
- **SP15** Downstream-unblock list with MUST/COULD classification (KI15).
- **SP16** Inherited Commitments Re-test (synthesis-level) — REQUIRED per CONCLUDE; aggregates inherited commitments from 9 priors.

### Foundational Principles
- **FP1** Synthesis-not-redefinition: axis-level decisions not re-litigated.
- **FP2** Self-contained dense (user choice): full per-axis prose reproduced.
- **FP3** Single-source-of-truth: canonical spec serves all downstream consumers.
- **FP4** Versioning anchor v1.0: first canonical synthesis after framework closure.
- **FP5** Carry-forward bundling: accumulated refinement notes resolved into two single Next Actions MUST items.
- **FP6** Pattern compendium: novel patterns (composite-axis / categorical / asymmetric / explicit-zero / dual-tier / content-type-by-level / action-permission / harmony report / multi-meaning three-layer) documented for future reuse.
- **FP7** Apparatus channel ownership: 3 channels with clear axis ownership.
- **FP8** Default-derivation chain explicit: each axis has documented default mechanism.

### Meaning-Nodes
- **MN1** Canonical Layer 1 spec — the single authoritative reference.
- **MN2** 40-cell default matrix — the per-purpose × per-axis consolidation.
- **MN3** 28-pair orthogonality matrix — cross-axis interaction taxonomy.
- **MN4** Apparatus channel separation — 3 channels owned by A6/A7/A8.
- **MN5** Action-vocabulary map — own-actions vs modulates vs produces vs drives-default.
- **MN6** Policy interaction map — 5 always-on policies × 8 axes.
- **MN7** Carry-forward bundle — A4 maintenance + .env.example pass.
- **MN8** Versioning v1.0 — the canonical spec's identity stamp.
- **MN9** Downstream-unblock list — what becomes possible after canonical spec commit.

### Meta-Inspection after SV2 (H4, H5)
- **H4 (concept names):** "Canonical Layer 1 spec" reads correctly as artifact name. "40-cell matrix" / "28-pair matrix" / "3-channel separation table" / "policy interaction map" are operationally clear. "Apparatus channel ownership" introduces a NEW vocabulary at synthesis level — load-bearing for the spec.
- **H5 (motivating examples):** Said Nursi corpus per-axis mapping serves as concrete project anchor. Cross-domain edition-tradition exemplars from per-axis findings (Penguin / Norton Critical / SBL) preserved in per-axis sections.

### SV2 — Anchor-Informed Understanding

Canonical Layer 1 spec at v1.0: 14 sections per KI1; self-contained dense per user choice; produces 40-cell per-purpose × per-axis default matrix + 28-pair orthogonality verification matrix + 3-channel apparatus separation table + action-vocabulary map + 5 × 8 policy interaction map + receptive-only APPLIES/NOT-APPLICABLE compendium + default-derivation chains + carry-forward refinement bundle (A4 maintenance + .env.example pass) + pydantic structure HINT + Said Nursi corpus mapping + versioning + downstream-unblock list + Inherited Commitments Re-test. No axis-level redefinition.

---

## Phase 2 — Perspective Checking

### Technical / Logical
The 14-section structure is logically coherent: each section serves a distinct purpose and the sections are independently consultable. The 40-cell matrix is deterministic given per-axis A4 cross-validation tables. The 28-pair orthogonality matrix uses 4 relation types deterministically. The 3-channel table is exhaustive across 3 channels × 8 axes. The policy interaction map is exhaustive across 5 policies × 8 axes. Receptive-only applicability compendium is binary per axis.

### Human / User
The canonical spec is dense (per user's "self-contained dense" choice). Reading time is significant but the structure enables sectional reference — a downstream consumer reads only the relevant sections. Section 4 (per-axis prose; the largest section) reproduces per-axis findings' per-level definitions; the consumer accustomed to per-axis findings will recognize the prose. Matrices in Section 5 + Section 6 are the new synthesis-level contributions.

### Strategic / Long-term
v1.0 is the first canonical synthesis. As future axis-level revisions accumulate, the canonical spec increments. The inline Changelog section anchors version history. The canonical spec serves as a STABLE REFERENCE while individual per-axis findings might receive maintenance updates; the canonical spec absorbs the maintenance refinements and re-versions.

### Risk / Failure
- R1: Canonical spec becomes stale if per-axis findings change without canonical spec update. CORRECTIVE: any per-axis maintenance pass should propagate to canonical spec; the canonical spec's versioning marks alignment.
- R2: The "self-contained dense" choice means the spec is long; risk of reader fatigue. CORRECTIVE: clear section structure with TOC + sectional reference accommodates non-linear consumption.
- R3: The 40-cell matrix may have edge cases (multi-purpose configurations; per-axis overrides). CORRECTIVE: the matrix supplies DEFAULTS; user can override any cell; documented at the matrix level.
- R4: New patterns may emerge in future axis revisions not anticipated by the compendium. CORRECTIVE: the compendium documents patterns but is extensible; new patterns added at canonical spec increment.
- R5: Pydantic implementation might diverge from canonical spec hint. CORRECTIVE: pydantic dataclass commit is downstream; should re-validate against canonical spec.

### Resource / Feasibility
Self-contained dense canonical spec is ~1000-1500 lines (per the user's initial sizing intuition). Manageable for AI prompt context (well within typical context windows). Sectional reference makes it consumable.

### Definitional / Internal Consistency
The canonical spec must be INTERNALLY CONSISTENT with all 9 priors. Settled mechanism: synthesis applies the refinement notes inline in the matrix (KI2) so the canonical spec is internally consistent; the maintenance bundle (KI9) PROPAGATES BACK to per-axis findings for source-of-truth alignment. The canonical spec becomes the v1.0 source-of-truth; per-axis findings are predecessors maintained but referencing the canonical spec going forward.

### Definitional / Frame-exit Completeness (GATING)
- (i) Inherited terms: YES (every axis label, pattern, action vocabulary inherited from priors).
- (ii) Used across ≥2 values: YES (e.g., `light` for A6 + `light` not used elsewhere; `scholarly` used as A8 level AND A4 purpose; "scholarly" as A4 purpose has different meaning than `scholarly` A8 level — multi-use term).
- Gating FIRES.

1. **Existence:** "Scholarly" project-wide → A4 purpose `scholarly` + A8 level `scholarly` + A7 level `scholarly` + A6 level (not used; A6 uses `maximum`). The term is RE-USED across axes with axis-distinct meanings. LAYER: each use is within its own axis's enum. AGENT: user (each axis configured independently).
2. **Role:** Out-of-scope: the user might confuse "A4=scholarly" with "A8=scholarly." Different concepts (A4 = purpose; A8 = analysis depth). CORRECTIVE: the canonical spec's per-axis sections clarify each use; the orthogonality matrix shows A4 drives-default for A8 (scholarly A4 → A8 `deep` per matrix).
3. **Verdict Rigor:** "Self-contained dense canonical spec" verdict:
   - Counter: compact navigator + references to per-axis findings would be more maintainable.
   - Why fails: user explicitly chose self-contained dense. The canonical spec serves AI prompt assembly without loading 9 priors. The maintenance burden is acceptable (per-axis maintenance triggers canonical spec re-version).
   - HOLDS at HIGH.
4. **Residual:** None.

### Phase / Calibration-State
The canonical spec is v1.0 — pre-calibration. As real translations and user feedback accumulate, the canonical spec may revise (per-axis refinement triggers). The canonical spec's STRUCTURE is calibration-state-independent; the per-axis CONTENT may revise.

### Ethical / Systemic
The DOMESTICATE-disfavored cross-cutting policy is preserved across the canonical spec (per-axis sections, action vocabularies, policy interaction map). The project's ethical commitment to foreignization-preserving alternatives is structurally embedded in the canonical spec (A5 asymmetric range with no `heavily-domesticated`; A3 honorific handling; A1 cultural-reference-recognition action policy). No ethical tension surfaces at the synthesis level.

### SV3 — Multi-Perspective Understanding

Confirms 14-section structure; 40-cell + 28-pair + 3-channel + action-vocabulary + 5×8 policy matrices; receptive-only compendium; default-derivation chains; carry-forward bundle (2 MUST items); pydantic structure HINT; Said Nursi mapping; v1.0 versioning + inline Changelog; downstream-unblock list (MUST + COULD). Frame-exit handled. Ethical considerations addressed.

---

## Phase 3 — Ambiguity Collapse

### Ambiguity A1: Pydantic dataclass — write class or hint at structure?
**Counter:** Write the actual class — downstream pydantic commit becomes trivial.
**Why partially fails:** Canonical spec at framework-spec layer; implementation choices (validators, custom serializers, base-class) are downstream. Writing the actual class commits to implementation details that may not be appropriate at canonical-spec layer.
**Counter (refined):** Write a STRUCTURAL HINT in pydantic-like syntax — Literal types per axis, composite class for A1, receptive-only optional field — without committing to implementation specifics.
**Why succeeds:** Aligns canonical spec with framework-spec abstraction layer; downstream pydantic implementer reads the hint and decides implementation specifics.
**Confidence:** HIGH
**Resolution:** Pydantic dataclass HINT (not actual code; pydantic-like syntax for structural clarity).

### Ambiguity A2: 40-cell default matrix — apply refinement notes inline OR keep A4 finding's original verbiage?
**Counter:** Keep A4 finding's original verbiage so the canonical spec exactly mirrors A4 finding.
**Why fails:** A4 finding's verbiage is INCONSISTENT with downstream axis labels (e.g., `deep+scholarly` ambiguous for A8). Canonical spec must be INTERNALLY CONSISTENT. The carry-forward refinement bundle exists precisely to align A4 with downstream axis labels; the canonical spec should reflect the post-refinement state.
**Confidence:** HIGH
**Resolution:** Apply refinement notes inline in canonical spec; the maintenance bundle (Next Actions MUST) propagates refinements BACK to A4 finding for source-of-truth alignment.

### Ambiguity A3: 28-pair orthogonality matrix — tabulate all 28 or sample most-load-bearing?
**Counter:** Sample most-load-bearing pairs (e.g., 10 pairs); cover remainder in prose.
**Why fails:** Canonical spec is single-source-of-truth; sampling loses systematic coverage. 28 pairs is finite and tractable.
**Confidence:** HIGH
**Resolution:** Tabulate all 28 pairs with 4 relation types (orthogonal / gated / orthogonal-with-modulation / drives-default).

### Ambiguity A4: Carry-forward refinement bundle structure — single Next Actions item or separate items?
**Counter:** List each refinement as a separate Next Actions item for granular tracking.
**Why partially fails:** 4-5 separate items adds bookkeeping overhead; the refinements naturally bundle (single A4 maintenance pass).
**Counter:** Bundle into TWO Next Actions MUST items: one A4 maintenance pass (4-5 refinements) + one .env.example pass (DEPTH_PROFILE `none` addition).
**Why succeeds:** Two passes are operationally distinct (A4 finding vs .env.example); bundling within each pass is natural.
**Confidence:** HIGH
**Resolution:** TWO bundled Next Actions MUST items.

### Ambiguity A5: Apparatus channel separation — 3-channel table vs prose description?
**Counter:** Prose description of 3 channels suffices.
**Why fails:** Table is more consumable + enforces explicit per-axis ownership. The 3-channel structure is a NOVEL pattern at synthesis level (emerged from A6/A7/A8 consolidation); table format makes it canonical.
**Confidence:** HIGH
**Resolution:** 3-channel table with explicit per-axis ownership labels (produces / owns / does not contribute / drives defaults / modulates / cross-references at high level).

### Ambiguity A6: Policy interaction map — 5×8 = 40 cells or selective?
**Counter:** Selective coverage — fill only substantively-load-bearing cells.
**Why partially fails:** Selective coverage leaves uncertainty about omitted cells (is no-smoothing × A1 truly invariant or is the cell just not yet considered?).
**Counter:** Full 40-cell map with cell labels: "invariant" / "activation gate at level X" / "renders/analyzes per axis level" / "modulates" / "not applicable" / "drives policy emphasis."
**Why succeeds:** Explicit coverage; future readers see exactly which cells are policy-relevant.
**Confidence:** HIGH
**Resolution:** Full 40-cell policy × axis interaction map.

### Ambiguity A7: Said Nursi corpus illustrative pass — include or omit?
**Counter:** Omit for compactness; corpus mapping is project-specific and the canonical spec might serve other corpora in future.
**Why partially fails:** The user's "self-contained dense" choice + Nursi being the project's primary anchor argues for inclusion. The canonical spec stays at framework-spec layer (axis identities + level definitions are language-agnostic); corpus mapping is illustrative (one example per axis).
**Counter:** Include corpus mapping per axis as concrete operational anchor; the canonical spec remains language-agnostic at concept level + corpus-illustrated at example level.
**Why succeeds:** Anchors the spec in concrete project use; future corpora can adapt the per-axis mapping pattern.
**Confidence:** HIGH
**Resolution:** Include Said Nursi corpus mapping per axis section.

### Ambiguity A8: Versioning + Changelog — inline section or separate file?
**Counter:** Separate `Changelog.md` file alongside canonical spec for cleaner separation.
**Why partially fails:** Splits versioning from canonical spec; harder to maintain single-source-of-truth.
**Counter:** Inline Changelog section at end of canonical spec.
**Why succeeds:** Single file = single source of truth; Changelog increments with each canonical spec version increment.
**Confidence:** HIGH
**Resolution:** Inline Changelog section at end of canonical spec.

### Ambiguity A9: Receptive-only field — include in pydantic structure hint or omit?
**Counter:** Omit — receptive-only mode never fully specified at root or per-axis findings; the productive case is currently out of scope (readers RECEIVE translations).
**Why partially fails:** Future production scenarios (e.g., language-learning where reader writes back-translations) may need receptive vs productive distinction.
**Counter:** Note as optional/future field in pydantic structure hint; mark "currently receptive-only" comment; full specification deferred.
**Why succeeds:** Acknowledges the pattern without committing to specification.
**Confidence:** MEDIUM-HIGH
**Resolution:** Pydantic structure HINT notes receptive-only as currently default mode for Reader-family axes; full specification deferred to future inquiry.

### Ambiguity A10: Per-axis prose detail level — full reproduction or condensed summary?
**Counter (condensed):** Summary per axis with reference to per-axis findings for full prose.
**Why fails:** User explicitly chose "self-contained dense"; the canonical spec must serve as single-source-of-truth without loading 9 priors. Condensed summary would defeat that.
**Counter (full):** Full per-level prose per axis reproduced.
**Why succeeds:** Matches user choice; canonical spec is AI prompt context source.
**Confidence:** HIGH
**Resolution:** Full per-level prose per axis.

### Ambiguity A11: Cross-axis interaction taxonomy categories — 4 relation types or more?
**Counter:** Add a 5th category (e.g., "conditional" for the multi-meaning policy × A7 + A8 case).
**Why fails:** The "multi-meaning three-layer treatment" is a POLICY × AXIS interaction documented in Section 6 (policy interaction map), not an axis-axis interaction. The 4 relation types (orthogonal / gated / orthogonal-with-modulation / drives-default) cover the 28 axis-axis pairs.
**Confidence:** HIGH
**Resolution:** 4 relation types for axis-axis pairs; multi-meaning + register-alternation + nazm + no-smoothing × axes documented separately in policy interaction map.

### Ambiguity A12: Section ordering — what order do the 14 sections appear?
**Counter (alphabetical-by-axis-first):** A1 → A8 sections first, then matrices.
**Why partially fails:** Matrices reference per-axis content; reading matrices first is awkward. Per-axis content read first (sections 1-4), then matrices (5-6), then cross-cutting (7-9), then implementation (10), then meta (11-14).
**Counter (top-down):** Framework Overview → Patterns → Per-Axis (4) → Cross-axis matrices (5) → Policy interaction (6) → Receptive-only (7) → Defaults (8) → Pydantic hint (9) → Corpus mapping (10) → Versioning (11) → Maintenance bundle (12) → Downstream-unblock (13) → ICs Re-test (14).
**Why succeeds:** Builds from architectural context → patterns → per-axis content → cross-axis consolidation → meta. Natural reading flow.
**Confidence:** HIGH
**Resolution:** Top-down ordering per the 14-section structure in KI1.

### SV4 — Clarified Understanding

After 12 ambiguity collapses:
- Pydantic dataclass HINT (not actual code; pydantic-like syntax)
- 40-cell default matrix applies refinement notes inline
- 28-pair orthogonality matrix tabulated with 4 relation types
- Carry-forward bundle = TWO Next Actions MUST items (A4 maintenance + .env.example pass)
- 3-channel apparatus separation table
- Full 40-cell policy × axis interaction map
- Said Nursi corpus mapping included per axis
- Inline Changelog at end of canonical spec
- Receptive-only mode hint as currently-default for Reader-family
- Full per-level prose per axis reproduced
- 4 relation types for axis-axis interactions
- Top-down 14-section ordering

---

## Phase 4 — Degrees-of-Freedom Reduction

### Variables fixed
- VF1: 14-section structure per KI1.
- VF2: 40-cell per-purpose × per-axis default matrix with refinement notes applied inline.
- VF3: 28-pair orthogonality verification matrix with 4 relation types (orthogonal / gated / orthogonal-with-modulation / drives-default).
- VF4: 3-channel apparatus separation table (text-surface / harmony-report / separate-sections × 8 axes).
- VF5: Action-vocabulary map (own actions / modulates / produces / drives defaults).
- VF6: 5 × 8 = 40-cell policy interaction map.
- VF7: Receptive-only APPLIES (A1/A2/A3) + NOT APPLICABLE (A4-A8) compendium.
- VF8: Default-derivation chains documented per axis.
- VF9: TWO Next Actions MUST carry-forward items (A4 maintenance pass + .env.example pass).
- VF10: Pydantic structure HINT (not actual code).
- VF11: Said Nursi corpus per-axis mapping included.
- VF12: 4 architectural patterns + 5 axis-specific patterns documented in compendium.
- VF13: Versioning v1.0 + inline Changelog.
- VF14: Downstream-unblock list (MUST + COULD).
- VF15: Inherited Commitments Re-test (synthesis-level table).
- VF16: STRUCTURAL layer commitment (no axis-level re-litigation).
- VF17: Full per-level prose per axis (self-contained dense).

### Options eliminated
- Compact navigator with references (defeats self-contained).
- Selective matrices (loses systematic coverage).
- Separate Changelog file (splits source-of-truth).
- A4 finding original verbiage in matrix (creates internal inconsistency).
- Actual pydantic code (over-commits to implementation).
- 5+ axis-axis relation types (multi-meaning + policies belong in policy map).
- Alphabetical-by-axis ordering (matrices unread first).
- Condensed per-axis summary (defeats self-contained dense).

### Viable paths
- VP1: 14-section canonical spec.
- VP2: Full prose per axis reproduced (self-contained dense).
- VP3: 4 matrices (40-cell default + 28-pair orthogonality + 3-channel apparatus + 40-cell policy interaction).
- VP4: Action-vocabulary map.
- VP5: Receptive-only compendium.
- VP6: Default-derivation chains.
- VP7: Pydantic structure HINT.
- VP8: Said Nursi corpus mapping.
- VP9: Versioning + Changelog.
- VP10: Carry-forward maintenance bundle (2 MUST items).
- VP11: Downstream-unblock list.
- VP12: Patterns compendium (9 patterns).
- VP13: ICs Re-test.

### SV5 — Constrained Understanding

Solution space: canonical Layer 1 spec at v1.0 with 14 sections (Framework Overview / Patterns / Per-Axis A1-A8 / Cross-axis matrices / Policy interaction / Receptive-only / Defaults / Pydantic hint / Corpus mapping / Versioning / Maintenance bundle / Downstream-unblock / ICs Re-test). Self-contained dense with full per-level prose. 4 matrices + action-vocabulary map + 9-pattern compendium + 2 Next Actions MUST bundle items.

---

## Phase 5 — Conceptual Stabilization

**Canonical Layer 1 Spec** = single authoritative reference at v1.0 consolidating all 8 user-facing axes of Comprehenslate's translation-configuration framework. Self-contained dense: full per-level prose reproduced; consumed by pydantic dataclass commit + translator-AI prompt assembly + UX preset catalog + runtime conflict resolution + framework-level changes (versioning).

**14 sections** (top-down ordering):
1. Framework Overview (4-layer + 4-family + 8-axis)
2. Architectural Patterns Compendium (9 patterns)
3. Per-Axis Sections (A1-A8; each with concept + user-question + pattern + cardinality + level enum + per-level prose + cross-axis boundaries + default chain + receptive-only applicability + handling action vocabulary)
4. Cross-Axis Interaction Matrices (4 matrices)
   - 4.1 Per-purpose × per-axis default matrix (40 cells with refinement notes applied)
   - 4.2 Cross-axis orthogonality verification matrix (28 pairs with 4 relation types)
   - 4.3 Apparatus channel separation table (3 channels × 8 axes)
   - 4.4 Action-vocabulary map
5. Policy Interaction Map (5 always-on policies × 8 axes = 40 cells)
6. Receptive-only Applicability Compendium (APPLIES vs NOT APPLICABLE)
7. Default-Derivation Chains (per axis)
8. Pydantic Dataclass Structure HINT (pydantic-like syntax; not actual code)
9. Said Nursi Corpus Illustrative Mapping (per axis)
10. Schema Commit Status + Versioning + Inline Changelog
11. Carry-Forward Maintenance Bundle (Next Actions MUST: 2 items)
12. Downstream-Unblock List (Next Actions COULD)
13. Inherited Commitments Re-test (synthesis-level)
14. Source Input

No axis-level redefinition. Refinement notes applied inline in matrices; bundled into Next Actions for propagation back to per-axis findings.

### Accommodation trigger check
NO patching. 12 ambiguities settled HIGH or MEDIUM-HIGH. No anchors destabilize the structure. The 14-section framework holds.

### SV6 — Stabilized Model

Canonical Layer 1 spec at v1.0 stabilized:
- 14 sections (top-down ordering)
- Self-contained dense (full per-level prose)
- 4 cross-axis matrices (40-cell default + 28-pair orthogonality + 3-channel apparatus + action-vocabulary)
- 5 × 8 policy interaction map
- 9 architectural patterns compendium
- Receptive-only APPLIES (A1-A3) + NOT APPLICABLE (A4-A8)
- Default-derivation chains per axis
- Pydantic structure HINT (not actual code)
- Said Nursi corpus per-axis mapping
- Versioning v1.0 + inline Changelog
- 2 Next Actions MUST carry-forward bundle items (A4 maintenance pass + .env.example DEPTH_PROFILE pass)
- Downstream-unblock list (schema commit + preset catalog + policy operational specs + source-description schema + UX surface)
- Inherited Commitments Re-test from 9 priors

**Difference from SV1:** Major. Section structure decided (14 sections top-down); 4 matrices specified; 5 × 8 policy interaction map specified; 9-pattern compendium named; pydantic structure HINT decided; carry-forward bundle structure decided (2 MUST items); inline Changelog decided; full per-level prose decided; corpus mapping included; versioning v1.0 anchored.

---

## Saturation
- Perspective: APPROACHING.
- Ambiguity: 12/12 HIGH (or MEDIUM-HIGH for A9); 0 OPEN.
- SV delta: major.
- Anchor diversity: 8 Constraints, 15 Key Insights, 16 Structural Points, 8 Foundational Principles, 9 Meaning-Nodes. DIVERSE.

**Saturation: HIGH. PROCEED.**

## Inherited Commitments Re-tested (synthesis-level — full table goes in finding)

The 9 priors each carry commitments. Synthesis-level re-test:

| Source | Commitment | Re-test Status |
|---|---|---|
| root | 4-layer architecture | RE-TESTED OK — canonical spec organized by Layer 1 (the in-scope layer) with explicit mention of Layers 2/3/4 boundaries |
| root | 8 axes in 4 families | RE-TESTED OK — 8 per-axis sections + 4-family organization in Framework Overview |
| root | composite-axis pattern | RE-TESTED & DOCUMENTED in Patterns Compendium |
| root | 5 always-on policies | RE-TESTED OK — Policy Interaction Map covers all 5 × 8 axes |
| root | 2-tier default principle | RE-TESTED & EXTENDED — A8 introduced dual-tier (3rd-tier variant); canonical spec documents in Default-Derivation Chains section |
| A1 | composite-axis with 5 sub-fields | RE-TESTED OK — Per-Axis A1 section |
| A1 | DOMESTICATE-disfavored policy | RE-TESTED OK — preserved across A1/A3/A4/A5 in canonical spec; explicit policy mention in Patterns Compendium |
| A2 | plain-ordinal 5 levels | RE-TESTED OK |
| A3 | identity-meaningful 5 levels | RE-TESTED OK |
| A4 | categorical 5 levels | RE-TESTED OK |
| A4 | per-purpose × per-axis default matrix | RE-TESTED & CONSOLIDATED in 40-cell matrix with refinement notes applied |
| A5 | asymmetric 4-level + policy-embedded structure | RE-TESTED OK — Patterns Compendium |
| A5 | NEW translator-strategy 4-component template | RE-TESTED OK |
| A6 | 5 levels tied to harmony_layer Tier 1-4 | RE-TESTED OK — Per-Axis A6 section |
| A6 | harmony report apparatus channel | RE-TESTED OK — Apparatus Channel Separation Table |
| A7 | 5 levels + scaffolding budget | RE-TESTED OK |
| A7 | action-permission table | RE-TESTED & DOCUMENTED in Patterns Compendium |
| A7 | 5 multi-meaning render rules | RE-TESTED OK — Policy Interaction Map (multi-meaning row × A7 cell) |
| A8 | 5 levels with `none` (revised from root's 4) | RE-TESTED OK — Per-Axis A8 section |
| A8 | content-type-by-level table | RE-TESTED OK |
| A8 | A7↔A8 3-framing rule | RE-TESTED OK — Cross-Axis Orthogonality Matrix (A7↔A8 cell) |
| A8 | dual-tier default | RE-TESTED OK — Default-Derivation Chains section |
| A8 | FRAMEWORK CLOSURE marker | RE-TESTED & EMBODIED — canonical spec IS the closure |

23 inherited commitments re-tested; all OK or documented. No silently-absorbed commitments; all explicitly re-tested.

## Frontier Flags for Decomposition / Critique

- FF1: Section ordering verification — confirm top-down (Overview → Patterns → Per-Axis → Matrices → Policy → Receptive-only → Defaults → Pydantic → Corpus → Versioning → Bundle → Downstream → ICs → Source) at decomposition.
- FF2: Pydantic HINT exact format (Literal types per axis; composite class A1) — decomposition spec.
- FF3: Per-axis section length budget — full per-level prose may produce ~50-100 lines per axis; total ~500-700 lines just for Section 3.
- FF4: Patterns Compendium ordering and detail level.
- FF5: Cross-domain illustrations from per-axis findings — include selectively (e.g., 1 per axis) or omit.
- FF6: Said Nursi corpus mapping format — per axis bullet vs full paragraph.
- FF7: Inline Changelog initial entry format — v1.0 entry.
- FF8: Versioning increment rule — when does v1.1 vs v2.0 happen? Pre-spec or deferred?
