## User Input

Decompose this evaluate-and-deepen inquiry, grounded on the stabilized sensemaking model (do NOT re-derive): the TARGET-UNIT REFRAME (char-vs-structure a false either/or → stable-unit budget snapping to structure), the MEASURE (char human-facing / token truer-internal / word rejected; source-side primary + output-side watched), the EVIDENCE-WEIGHT (existence-proof + direction + provenance for ~3500, not threshold/mechanism; partially lifts 04-48's quarantine), the CONFOUND-ROBUSTNESS (survives even if mechanism is method-not-length), COMPREHENSLATION INTEGRITY (defined), RECONCILE-04-48 (refines chunk-by-structure, partially lifts quarantine, re-confirms complementary-not-rival), RIVALS, and the OPEN residual (exact number + output-side guard). Span the reach fork (deepen → design → apply). Synthesis Trigger re-tests 04-48 + 06-14 → flag CONCLUDE Inherited Commitments Re-test. Full model in sensemaking.md; branch in `_branch.md`.

---

# Structural Decomposition — DV1

## Step 1 — Coupling Topology (coupling map)

Elements in the whole: the unit-reframe · the measure-choice · the source/output side · the evidence-weight · the confound-robustness · the comprehenslation-integrity concept · the 04-48 reconciliation · the quarantine-lift · the complementary-not-rival re-confirmation · the number-calibration · the output-side-guard · the reach decision.

Coupling reading (change-propagation):
- **Cluster 1 — THE UNIT VERDICT** (unit-reframe + measure-choice + source/output side): tightly coupled — the reframe (budget vs snap role) enables the measure question, which entails the side question. Change the reframe → the measure framing changes.
- **Cluster 2 — THE EVIDENCE VERDICT** (evidence-weight + confound-robustness): tightly coupled — the confound-robustness is the direct answer to "so what if the evidence can't isolate the mechanism." Change the evidence-weight → the robustness argument changes.
- **Cluster 3 — THE CONCEPT** (comprehenslation integrity): loosely coupled to 1 and 2 — it names the protected outcome; the verdicts don't depend on its exact wording. A valley separates it.
- **Cluster 4 — PRIOR RECONCILIATION** (refines-04-48 + quarantine-lift + complementary-not-rival re-confirm + commitment re-test): coupled DOWNSTREAM of 1+2 (the reconciliation is a consequence of the unit + evidence verdicts). A valley on its input side (it consumes 1+2's outputs through a clean interface).
- **Cluster 5 — OPEN RESIDUAL** (number-calibration + output-side-guard): coupled downstream of 2 (what's left open after the evidence verdict). Valley on its input side.
- **Cluster 6 — REACH GATE** (deepen/design/apply): orthogonal to all content; the user's decision. Clean valley all around.

Dominant structure: two evaluation clusters (unit, evidence) feeding a reconciliation and a residual, with a loosely-attached concept-definition and an orthogonal reach gate.

## Step 2 — Boundaries (top-down)

Six pieces at the low-coupling valleys: **A** unit-verdict · **B** evidence-verdict · **C** concept-definition · **D** prior-reconciliation + commitment-re-test · **E** open-residual · **F** reach-gate. The valleys: between the two evaluation clusters and their downstream consumers (D, E); around the concept (C, loosely attached); around the reach gate (F, orthogonal).

## Step 3 — Boundaries (bottom-up validation)

Atoms: "is char the right unit?" · "char vs token vs word?" · "source or output side?" · "what do 2-3 points establish?" · "does the intervention survive the confound?" · "what is comprehenslation integrity?" · "how does this relate to 04-48's chunk-by-structure + quarantine?" · "what number, and an output guard?" · "how far to reach?"

Grouping: the first three atoms → A (unit). The next two → B (evidence). Concept atom → C. Reconciliation atom → D. Number+guard atoms → E. Reach atom → F. **Top-down and bottom-up AGREE** → high confidence. No atom is split across a boundary; no independent atoms forced together (the unit and evidence clusters are genuinely separate questions — "what unit" vs "what does the evidence say").

## Step 4 — Question Tree

**A — The unit verdict.** *Is a stable-unit size BUDGET (not a structural level) the right form, and which measure?*
- [ ] confirms "char vs structure" is a false either/or (budget = size TARGET, snap = cut LOCATION; the schema's `chunking_budget` + `chunking_granularity` + `fixed-budget-with-snap` already compose them)
- [ ] states the user's valid narrower point: a structural LEVEL is too size-variable (~15×) to be a reliable size target; a stable unit must be the target, structure stays the snap
- [ ] measure verdict: CHARACTERS = human-facing budget unit (stable/legible/feasible/language-robust); TOKENS = truer-to-load internal measure for the engine later; WORDS rejected
- [ ] side verdict: SOURCE-side primary (pre-generation control point, proxy for the whole working-set); OUTPUT-side watched (the 2.57× over-expansion signal)

**B — The evidence verdict.** *What do the 2–3 measured points establish, and does the char-budget survive the confound?*
- [ ] evidence-weight: existence-proof (big failed, small succeeded) + direction (bigger → harder) + real provenance for ~3500 (= the good example's source, 4,345) → PARTIALLY LIFTS 04-48's "zero evidence" quarantine
- [ ] states what is NOT established: a validated threshold (no failure observed near 3500) and NOT the mechanism (length vs un-wired-3-Pass confounded; `mistakes.md` errors are method-class)
- [ ] confound-robustness (the key survival result): the char-budget helps EVEN IF the mechanism is method-not-length — a smaller chunk lets ANY method survive; the SHORT 4_mesele succeeding DESPITE the un-wired-3-Pass proves it
- [ ] verification: the confound-collapse rival is tested and shown to collapse only the length-mechanism claim, not the intervention

**C — The concept definition.** *What precisely is "comprehenslation integrity"?*
- [ ] defines it: the unified outcome — source meaning comprehended AND faithfully rendered — that the whole SKILL protects, degraded by any principle-drop (register/word-sense/harmony/Tier-1) whether from load OR un-wired method
- [ ] locates it against neighbours: broader than Tier-1/2 preservation (a component) and collapse-in-one-motion (a failure mode); the "what's at stake" umbrella
- [ ] verdict: useful new umbrella term, not a rename (user-language-aligned, coined by the user)

**D — Prior reconciliation + commitment re-test.** *How does this relate to the priors it consumes?*
- [ ] REFINES 04-48's "chunk by structure" → "budget in characters, snap to structure"
- [ ] PARTIALLY LIFTS 04-48's magnitude quarantine (real provenance; still conservative-anchor-pending-calibration; mechanism confounded)
- [ ] RE-CONFIRMS 00-24's complementary-not-rival (via the confound-robustness / short-success-despite-un-wired-3-Pass)
- [ ] RE-TESTS the 06-14 granularity-ladder commitment (the ladder becomes the SNAP, not the size target)
- [ ] overturns nothing

**E — The open residual.** *What stays open, and what would close it?*
- [ ] the exact number: a conservative per-model anchor (~3500–4000 source chars, round down; lower for smaller models), calibratable — the 04-48 experiment, now provenance-anchored
- [ ] the output-side-guard question: should the budget also bound output (the 2.57× over-expansion), or is a source budget + expansion-watch enough?
- [ ] both flagged as OPEN (deepen-understanding doesn't close them)

**F — The reach gate.** *How far should this inquiry's output reach?*
- [ ] enumerates deepen-only → design → apply-the-SKILL-edit (incl. typing the untyped `chunking_budget` field + documenting the char unit)
- [ ] gated on the user's decision + authorization; no SKILL edit without it

## Step 5 — Interface Map

| # | Source → Target | What flows | Direction |
|---|---|---|---|
| I1 | A → B | the unit (characters, source-side) — the measure the evidence is expressed in ("smaller chunk" in B = smaller char-count) | one-way |
| I2 | B → D | the evidence-weight (existence+direction+provenance, not threshold/mechanism) → the quarantine-lift is a direct consequence | one-way |
| I3 | A + B → D | the unit-verdict (→ refines "chunk by structure") + the evidence-verdict (→ lifts quarantine) → the reconciliation | one-way |
| I4 | B → E | the number is a conservative anchor not validated + the over-expansion note → the residual (calibrate + output-guard) | one-way |
| I5 | C → B, D | comprehenslation integrity (the protected outcome) → what the confound-robustness protects (B) + what the reconciliation preserves (D) | one-way |
| I6 | A,B,C,D,E → F | all verdicts + residual → the reach decision is over the whole | one-way |
| **I7** | **00-24 diagnosis → B, D** | **LOAD-BEARING ASSUMPTION:** the confound-robustness (B) + the complementary-not-rival re-confirmation (D) PRESUME the un-wired-3-Pass diagnosis holds (the `mistakes.md` errors ARE method-class). If 00-24's diagnosis were wrong, the confound framing changes. | inherited prereq |

**Assumptions-not-data check (Step 5 refinement):** I7 is the hidden-coupling risk — B and D assume the un-wired-3-Pass is real (not just an alternative). Surfaced explicitly here. Also: A assumes the schema fields mean what the prior work read them as (chunking_budget = the size knob) — confirmed in surfacing (the field is untyped but so-used). No other hidden assumptions.

## Step 6 — Dependency Order

- **Start (parallel):** A (unit-verdict) ‖ C (concept-definition) — independent.
- **Then:** B (evidence-verdict) — after A (I1: B's "smaller chunk" is in A's unit).
- **Then (parallel):** D (reconciliation) ‖ E (residual) — both after B (I2/I3/I4); D also consumes A + C.
- **Last:** F (reach-gate) — after all (I6).
- No cycles. I7 is an inherited prereq (00-24), not an internal cycle.

Order: **(A ‖ C) → B → (D ‖ E) → F.**

## Step 7 — Self-Evaluation (7 dimensions)

| Dimension | Verdict | Note |
|---|---|---|
| **Independence** | PASS | Each piece answerable via its interface without reading siblings' internals (A: what unit; B: what evidence; C: define term; D: relate to priors; E: what's open; F: how far). |
| **Completeness** | PASS | The four eval-verdicts (A+B) + concept (C) + reconciliation (D) + residual (E) + reach (F) cover the whole stabilized model. Nothing falls through. |
| **Reassembly** | PASS | A (unit) + B (evidence+robustness) + C (concept) + D (priors) + E (open) + F (reach) reconstruct the finding: "budget in characters snapping to structure, a confound-robust conservative anchor with real-but-anecdotal provenance, protecting comprehenslation integrity, refining 04-48, with the number + output-guard open." |
| **Tractability** | PASS | Each piece is a single focused pass. |
| **Interface clarity** | PASS | 7 interfaces explicit incl. the load-bearing I7 (the 00-24 assumption). |
| **Balance** | PASS | A and B are the two substantive halves (well-balanced); C/E/F lighter but genuinely distinct; no single piece is 80%. |
| **Confidence** | PASS (HIGH) | Top-down and bottom-up agreed on all six boundaries. |

**Determination-mechanism check:** the soundness questions — is char the right unit (A), what does the evidence establish (B), is the budget confound-robust (B) — are ANSWERED in A + B, not presupposed. The one runtime-determination ("which measure does the engine actually use") is answered in A (characters human-facing; tokens internal-later). PASS — no missing determination piece.

## Relationships (for CONCLUDE)

- **REFINES** `2026-07-11_04-48` (its "chunk by structure" → "budget in chars, snap to structure"; partially lifts its magnitude quarantine).
- **RE-CONFIRMS** `2026-07-11_00-24` (complementary-not-rival, via the confound-robustness).
- **RE-TESTS** the `2026-06-14` chunking design (the granularity ladder becomes the SNAP, not the size target).
- **Synthesis Trigger** consumes/re-tests 04-48 + 06-14 → **CONCLUDE needs an `## Inherited Commitments Re-test`** of: 04-48's chunk-by-structure (REFINED) + its quarantine (PARTIALLY LIFTED); 00-24's complementary-not-rival (RE-CONFIRMED); 06-14's granularity-ladder-as-budget (RE-TESTED → ladder-as-snap). The honest headline (char-vs-structure is a false either/or; the win is defining the untyped unit + provenance-anchoring 3500 + the confound-robustness argument; mechanism stays confounded) must reach the finding.
