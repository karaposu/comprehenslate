# Decomposition — Staged Borders + Middleware: Evaluation + Design, Partitioned

## User Input

`_branch.md` + `sensemaking.md`. Stabilized model: the proposal SURVIVES but decomposes into TWO independent proposals + does NOT solve its own springboard. (1) LATE-SPLIT REAL → STAGING (translation-correctness) vs MIDDLEWARE (SKILL-author maintainability). (2) STAGING earns its keep MODESTLY = "finish + generalize prior `2026-07-11_00-24`" (wire the latent 3-Pass into Step 5 + separate the collapsed config/policy/principle layers); new delta = generalize-to-all-layers; sound as PROBABILITY-RAISER not ENFORCEMENT (soft one-run borders); caveat = no false confidence of guaranteed separation. (3) MIDDLEWARE splits: authoring-CHANGELOG EARNS ITS KEEP (cheap, literal "visible for us"); runtime-PROVENANCE is OVER-ENGINEERING (expensive + self-report ≠ verification) — reject as new artifact, at most defer a version narrowed to the EXISTING harmony report + mechanical checks. (4) MOTIVATING MISFIT: staging+visibility organizes structure but doesn't make judgment checks reliable → maintainability/clarity improvement, not a reliability fix. Relationship: staging EXTENDS/INSTANTIATES `2026-07-11_00-24`; whole REFINES `2026-07-11_01-09`. Span the OUTPUT-REACH fork; flag the Inherited Commitments Re-test (2 priors). Save to `decomposition.md`.

---

## Step 1 — Coupling Topology

**Elements:**
- **E1** two-proposal split verdict (staging vs middleware are separable — different beneficiary/novelty/cost/reliability).
- **E2** staging verdict (finish+generalize prior fix; probability-raiser NOT enforcement; the soft-border caveat).
- **E3** middleware verdict (authoring-changelog earns its keep; runtime-provenance is over-engineering; re-locate onto the existing harmony report).
- **E4** the motivating-MISFIT (staging doesn't fix the reliability problem that sparked it — the honest bottom line).
- **E5** prior-reconciliation (staging EXTENDS `2026-07-11_00-24`; whole REFINES `2026-07-11_01-09`; gated on the 3-Pass wiring) + the commitment re-test.
- **E6** fix-design: the ordered staged Step 5 (config-resolution → comprehension → meaning-lock → harmony-generate → check; the ordering axis; the 3-Pass as harmony sub-stages).
- **E7** fix-design: the authoring-changelog scheme (scope; what counts as a logged change) + the deferred/narrowed harmony-report provenance option.
- **E8** reach gate (per-proposal — staging reach and changelog reach can differ).

**Coupling gradient:**
- E1↔E2↔E3↔E4 **strong** — the evaluation verdict is one integrated judgment: the split IS the verdict; each half's verdict rests on it; the misfit is the honest edge of the whole. One cluster.
- E5↔E1 **moderate** — the reconciliation reuses the verdict but adds the two priors as inputs and makes distinct relationship + re-test claims (Synthesis Trigger). Distinct piece.
- E6↔E7 **weak** — the staging design and the changelog design are two independent artifacts (different beneficiary; the late-split). They co-reside as "the design reach" but do not depend on each other.
- {E1,E2,E3,E4,E5}→{E6,E7} **weak** — the evaluation + reconciliation are complete without building either design; the designs are the *reach*. **Dominant boundary (OUTPUT-REACH valley).**
- E8 is a **scope gate**, weakly coupled, governing whether E6/E7 are in final scope — and it is now **per-proposal** (adopt staging, or the changelog, or both, independently).

**Clusters → pieces:** A = {E1,E2,E3,E4} evaluation-core (split + both verdicts + misfit) · B = {E5} prior-reconciliation + re-test · C = {E6,E7} fix-design (two independent sub-designs) · D = {E8} per-proposal reach gate.

## Step 2 — Boundaries (top-down)
Lowest-coupling cut = the **OUTPUT-REACH valley** between the evaluation {A,B} and the design {C}, gated by D. Secondary cut inside C: staging-design | changelog-design (the late-split — two independent sub-designs, kept under one piece to avoid over-decomposition since they share the evaluation, reconciliation, and reach gate).

## Step 3 — Boundary validation (bottom-up)
Irreducible atoms: {the two-proposal split}, {staging = finish+generalize}, {soft borders = probability-raiser}, {changelog earns its keep}, {runtime-provenance over-engineered}, {the misfit}, {the two priors' relationship}, {the priors' re-tested commitments}, {the staged Step-5 design}, {the changelog design}, {the per-proposal reach}. These group into A (first six), B (next two), C (two designs), D (reach) — top-down and bottom-up **agree** → HIGH confidence. Four pieces (matching the two prior sibling inquiries) — correct: forcing staging-design and changelog-design into separate top-level pieces would over-decompose (they share A, B, D; only the design content differs). The internal two-design split lives inside C's question-tree.

## Step 4 — Question Tree

**Q-A — Evaluation-Core + Verdict** *(always in scope):* Does the staged-borders + middleware proposal make sense, and in what refined form?
- [ ] the **two-proposal split** named on evidence: staging (translation-correctness beneficiary) vs middleware (SKILL-author-maintainability beneficiary) are separable — different novelty, cost, reliability.
- [ ] the **staging verdict**: it earns its keep MODESTLY = "finish + generalize prior `2026-07-11_00-24`" (wire the latent 3-Pass into Step 5 + separate the collapsed layers); the new delta is the generalization-to-all-layers.
- [ ] the **soundness verdict**: soft one-run borders are sound as a **probability-raiser** (an explicit ordered instruction is more-followed than a flat bag — the exact lever the prior fix uses), **NOT as enforcement**; caveat = no false confidence of guaranteed separation.
- [ ] the **middleware verdict**: authoring-changelog EARNS ITS KEEP (cheap, no runtime cost, no reliability question, the literal "visible for us"); runtime-provenance is OVER-ENGINEERING as proposed (expensive + a self-report is not a verification) — reject as a new artifact, at most narrow to the existing harmony report + mechanical checks.
- [ ] the **motivating MISFIT** stated: staging+visibility organizes/exposes structure but does NOT make the unreliable judgment checks reliable — a clarity/maintainability improvement, not the reliability fix that sparked it.

**Q-B — Prior-Reconciliation + Commitment Re-test** *(always — Synthesis Trigger, 2 priors):* How does this relate to the priors, and do their commitments survive re-test?
- [ ] relationship named: **staging EXTENDS/INSTANTIATES** `2026-07-11_00-24` (generalizes its 3-Pass wiring to all layers) **+ the whole REFINES** `2026-07-11_01-09` (confirms tiered reliability — a border/self-report is the "not guaranteed" tier), **not supersede**.
- [ ] the **gated-on-the-3-Pass-wiring** dependency stated (staging's apply-work is mostly the prior fix's apply-work; staging is inert as a *new* contribution until that fix is applied).
- [ ] each prior's load-bearing commitments listed + re-tested: from `2026-07-11_00-24` — (i) the 3-Pass/Step-5 meaning-first wiring [staging generalizes it]; (ii) "real separate step, not narrated in one motion" [this IS the soft-borders question — re-test]. From `2026-07-11_01-09` — (iii) the config-independent/derived split + tiered reliability [the runtime-provenance verdict rests on it]; (iv) the mechanical-core-vs-soft-rim [the narrowed provenance scopes to mechanical checks].

**Q-C — Fix-Design** *(gated by D; two independent sub-designs):* What are the concrete designs?
- [ ] **staging design:** the ordered staged Step 5 — a candidate stage set (config-resolution → comprehension → meaning-lock → harmony-generate → check), with the 3-Pass as the harmony sub-stages and the config-independent spine as the check stage; states the ordering axis (temporal comprehend→generate→check) and carries the soft-border caveat as an explicit in-SKILL note.
- [ ] **changelog design:** the authoring-changelog scheme — what a "middleware md" records per SKILL edit (which rule/policy, what changed, why), where it lives, how heavy the convention is; scoped to SKILL-authoring (not runtime).
- [ ] **the deferred provenance option:** if runtime-provenance is wanted, narrow it to formalizing the EXISTING harmony report for the MECHANICAL checks only — recorded as deferred, not adopted.
- [ ] **additive + placed:** staging plugs into SKILL.md Step 5 (gated on the prior `2026-07-11_00-24` fix); the changelog is a new dev-side convention; neither changes principle-content.

**Q-D — Reach Gate** *(per-proposal scope decision, surfaced to user):* How far past evaluation should each proposal travel?
- [ ] the reaches named PER PROPOSAL (evaluate-only / +design / +apply) — staging and changelog can be adopted independently.
- [ ] default recommended (A+B always delivered; C offered per proposal — the changelog is the cheapest, highest-independent-value adopt; staging is gated on the prior fix).
- [ ] gate presented, not silently resolved.

## Step 5 — Interface Map

| Source | Target | What flows | Dir |
|---|---|---|---|
| surfacing (SKILL structure) | A, C | the real SKILL structure (latent 3-Pass; collapsed Step-5 bag; harmony report; config/core split) — prerequisite | → |
| sensemaking (stabilized model) | A | the two-proposal split + both verdicts + the misfit (prerequisite) | → |
| A | B | the verdicts the relationship builds on (data) | → |
| A | C | the verdicts the designs instantiate (staging design ← staging verdict; changelog design ← middleware verdict) | → |
| two prior findings | B | the commitments to re-test (data) | → |
| B | C | the gated-on-3-Pass dependency (staging design plugs into the prior fix) | → |
| D | C | per-proposal reach authorization (gate) | → |

**Assumptions-not-data check (hidden-coupling guard):** C's staging sub-design assumes A's "probability-raiser not enforcement" verdict (explicit — A's output) AND the prior 3-Pass wiring as insertion point (explicit — from B, gated). C's changelog sub-design assumes A's "changelog earns its keep" verdict (explicit). The two sub-designs of C make NO assumptions about each other (independent — the late-split). B assumes both priors' commitments as stated (explicit — named inputs). No unstated format/timing/state assumptions across boundaries. **No hidden coupling.**

## Step 6 — Dependency Order
1. **surfacing + sensemaking** (done) → the SKILL structure + the stabilized model.
2. **A** (evaluation + both verdicts + misfit) — first.
3. **B** (prior reconciliation + re-test) — after A.
4. **D** (per-proposal reach gate) — decidable early; GATES C. Default: deliver A+B, offer C per proposal.
5. **C** (the two fix-designs) — after A+B, if D authorizes; the two sub-designs are parallel (independent).

No circular dependencies. For the pipeline: **Innovation** works the fix design-space (C) — esp. the staged Step-5 stage set + ordering, the changelog scheme, and whether the deferred provenance narrowing is worth stating; **Critique** adjudicates C + stress-tests A/B (esp. the "staging earns its keep beyond the prior fix / probability-raiser-not-enforcement / runtime-provenance-is-over-engineering" claims and the EXTENDS+REFINES relationship, and the honest MISFIT); **CONCLUDE** delivers A+B (including the Inherited Commitments Re-test of BOTH priors) and presents D with C as the offered per-proposal reach.

## Step 7 — Self-Evaluation

| Dimension | Verdict | Note |
|---|---|---|
| **Independence** | PASS | each Q answerable via defined interfaces alone (A from model+SKILL; B from A + the priors; C from A+B+surfacing; D standalone) |
| **Completeness** | PASS | user's ask = A (does it make sense + what form) + C (design it); B (prior relationship + re-test) required by the Synthesis Trigger; D (per-proposal reach). Nothing falls through |
| **Reassembly** | PASS | A+B (+D-gated C) + interfaces = the full evaluation-plus-design with the two-proposal split, the misfit, and the two-prior re-test |
| **Determination-mechanism** | PASS | no runtime-determination-shaped sub-question is presupposed — the soundness question ("do soft borders get followed?") is ANSWERED in A (the probability-raiser verdict), not left to a missing piece; the design choices in C are authoring decisions, not runtime determinations |
| **Tractability** | PASS | each piece = one focused pass; C's two sub-designs are each small |
| **Interface clarity** | PASS | all flows explicit; assumptions checked; the two C sub-designs are independent (no hidden coupling) |
| **Balance** | PASS | A and C carry the weight (appropriate — the split-verdict IS the answer, the two designs ARE the reach); B moderate (2-prior re-test); D light. Not one-piece-80% |
| **Confidence** | HIGH | top-down (OUTPUT-REACH valley) and bottom-up (11 atoms → 4 pieces) agree |

**Decomposition version:** DV1 (single pass sufficient). No hidden-coupling or wrong-boundary signals → no DV2 trigger. **CONCLUDE flag:** because `_branch.md` declared a **Synthesis Trigger** consuming TWO prior findings, CONCLUDE must include the `## Inherited Commitments Re-test` section testing BOTH priors' commitments; the finding's frontmatter carries the relationship (`refines:` `2026-07-11_01-09`; `extends:`/`refines:` `2026-07-11_00-24` — staging generalizes its wiring). The honest MISFIT (Q-A last criterion) must survive into the finding's headline, not be softened.
