## User Input

Routelister exhaust step of a /traverse inquiry (flow-type `articulated-surfacing-routed`). Territory = this inquiry's own finished artifacts (`_branch.md`, `articulate_simple.md`, `surfacing.md`, `articulate_warm.md`, `sensemaking.md`, `decomposition.md`, `innovation.md`, `critique.md`, `_state.md`). Entry point: fresh. Goal (from `_branch.md`): evaluate whether explicit staged SKILL borders (one run) + per-change middleware files make sense, and design the surviving parts — per-proposal reach-gated.

Stabilized content to route FROM (post-critique): the proposal SURVIVES as TWO separable proposals. STAGING = "finish + generalize prior `2026-07-11_00-24`" — a staged Step 5 (config-resolution → comprehension → meaning-lock → harmony-generate → check; ordered-with-permitted-back-reference; the config-independent mechanical spine as the check-stage); sound as a PROBABILITY-RAISER not enforcement; reliability-HOSTING not reliability-fixing; gated on the prior fix. The lean single-file AUTHORING-CHANGELOG EARNS ITS KEEP (if maintained; consumer = the human author; the soft-border caveat lives HERE, not in runtime text). KILLED: runtime-provenance + "a file per change" — the "file-per-X is the over-engineering shape" (lean form = entries in ONE doc). MISFIT: staging organizes structure but does NOT fix the unreliable-judgment-checks problem that sparked it. Relationship: staging EXTENDS `2026-07-11_00-24`; whole REFINES `2026-07-11_01-09`. Enumerate the typed onward route-field; do not choose. Write `routelister.md` + `_route.md`, both in the inquiry root.

---

# Route-Map — Staged SKILL Borders + Middleware: Onward Route-Field

## Map Header
- **Identities enumerated:** 8 routes (+ 5 Excluded)
- **High-priority:** 4 (R1, R2, R3, R6)
- **Essential-count (`core`):** 2 (R1 `core·@after-prior-fix` · R2 `core`)
- **Mode:** root / project-space (breadth) · **Entry:** fresh · **Goal:** evaluate + design staged borders / middleware, per-proposal reach-gated

## Route Index

| # | Direction | engagement-type | Priority | Essentiality | ✓ |
|---|---|---|---|---|---|
| R1 | Apply the staged Step 5 to SKILL.md | DEVELOP | HIGH | core · @after-prior-fix |  |
| R2 | Adopt the lean single-file authoring-changelog | DEVELOP | HIGH | core |  |
| R3 | Produce the finalized staged-Step-5 spec (design-reach) | DEVELOP | HIGH | core |  |
| R4 | Validate the staged Step 5 against the 7-error translation | TEST | MED | supporting |  |
| R5 | Back-link prior `2026-07-11_00-24` — staging generalizes its wiring | REFINE | MED | supporting |  |
| R6 | Consolidate memory: file-per-X + caveat-audience lessons | CONSOLIDATE | MED-HIGH | supporting |  |
| R7 | Branch: emitted-artifact enforcement (relax one-run) | INVESTIGATE-FRONTIER | LOW-MED | peripheral · @if-enforcement-wanted |  |
| R8 | Branch: gradient middle-rung empirical test | TEST | LOW | peripheral |  |

---

## Per-Route Records

### R1 — Apply the staged Step 5 to SKILL.md
- **engagement-type:** DEVELOP *(teleological · project-space)*
- **Move:** restructure SKILL.md Step 5's flat "Apply:" bag into an ordered staged sequence — *config-resolution → comprehension → meaning-lock → harmony-generate → check* — with the harmony 3-Pass as the harmony-stage's sub-steps and the config-independent **mechanical spine** as the check-stage; stages ordered **with permitted back-reference** to earlier committed output (not a one-way ratchet); NO self-undermining caveat in the runtime text.
- **Lands:** the SKILL executes an explicit ordered stage sequence instead of a flat apply-bag.
- **Touches:** SKILL.md Step 5 (lines 73-79, the flat bag) · harmony_layer.md 3-Pass (the harmony sub-stages) · the config-independent spine (the check-stage, from prior `2026-07-11_01-09`).
- **WHY:** an explicit ordered instruction is more-followed than a flat bag (the exact lever that prevents the collapse-into-one-motion failure); the end goal — a SKILL whose stages actually fire in order — gains the ordered sequence. Line of sight is **phase-deferred**: the staging's correctness core is the prior fix, so it only lands cleanly once that fix is applied.
- **Priority:** HIGH **Confidence:** MED **Essentiality:** core · @after-prior-`2026-07-11_00-24`-fix-applied
- **Guidance Mode:** compact
  - "apply after / together with the prior 3-Pass fix" *(bc staging's correctness core IS that fix; applying staging into a still-flat Step 5 strands it)*
  - "order-with-back-reference, not a one-way ratchet" *(bc the 3-Pass needs Pass-3 to consult the Pass-1 foundation; a strict ratchet forbids it)*
  - "keep the soft-border caveat OUT of runtime text" *(bc a runtime note that says 'borders don't enforce' licenses the model to relax them — put it in the changelog/design-doc instead, R2)*
- **Depth-link:** none

### R2 — Adopt the lean single-file authoring-changelog
- **engagement-type:** DEVELOP *(teleological · project-space)*
- **Move:** create ONE `CHANGELOG.md` / decisions-log (ADR-style dated entries: *which rule/policy · what changed · why · date*) for SKILL edits — NOT a file per change; house the soft-border caveat and other authoring-knowledge here.
- **Lands:** SKILL edits leave a single, maintainable, human-readable trail ("visible for us what rule/policy changed how").
- **WHY:** this is the genuinely-new, genuinely-useful, cheapest-highest-independent-value part of the proposal; the end goal (a SKILL the author can see the evolution of) gains a maintainable record. NOT gated on the prior fix — adoptable now.
- **Priority:** HIGH **Confidence:** HIGH **Essentiality:** core
- **Guidance Mode:** compact
  - "ONE file, dated entries — not a file per change" *(bc file-per-change is the over-engineering shape, same trap as per-rule runtime provenance)*
  - "name the consumer = the human author; it is not read at runtime" *(bc its value is contingent on the author maintaining it — keep it lean so maintenance holds)*
- **Depth-link:** none

### R3 — Produce the finalized staged-Step-5 spec (the design-reach)
- **engagement-type:** DEVELOP *(teleological · project-space)*
- **Move:** write the staged Step 5 as a ready-to-apply spec — the stage set + ordering + the permitted-back-reference rule + the check-stage = the config-independent mechanical spine + where the caveat lives (authoring docs).
- **Lands:** a self-contained spec R1 consumes and R4 tests — the middle reach (design without touching the SKILL).
- **WHY:** turns the evaluation into a ready artifact short of a live edit; the end goal gains the concrete, critique-corrected staged design.
- **Priority:** HIGH **Confidence:** HIGH **Essentiality:** core
- **Guidance Mode:** compact
  - "carry the critique's four corrections" *(bc the innovation's draft shipped the caveat in runtime text + didn't specify back-reference — the spec must fix both, plus the lean-changelog + gradient-honesty)*
- **Depth-link:** none

### R4 — Validate the staged Step 5 against the 7-error translation
- **engagement-type:** TEST *(epistemic · project-space)*
- **Move:** re-translate the İKİNCİ HÜCCET chunk with the staged Step 5 vs the flat bag; does the ordered sequence reduce the 7-error signature?
- **Lands:** evidence on the "probability-raiser" claim (does explicit staging actually raise compliance?) and, if an IR variant is tried, the gradient's middle rung.
- **WHY:** the probability-raiser verdict is reasoned, not evidenced; the end goal gains confidence the staging actually helps (or data that it does not).
- **Priority:** MED **Confidence:** MED **Essentiality:** supporting
- **Guidance Mode:** compact
  - "compare staged vs flat on the SAME chunk" *(bc the whole claim is relative — ordered instruction beats flat bag)*
- **Depth-link:** none

### R5 — Back-link prior `2026-07-11_00-24` (staging generalizes its wiring)
- **engagement-type:** REFINE *(epistemic · project-space)*
- **Move:** annotate the prior finding that staging EXTENDS it — generalizing its harmony 3-Pass Step-5 wiring to ALL layers (config/policy/principle/harmony/check).
- **Lands:** the prior finding records the generalization; the prior-chain stays coherent.
- **WHY:** the relationship is EXTENDS; the end goal (a coherent lineage) gains the forward pointer so staging is not read as a rival to the 3-Pass fix.
- **Priority:** MED **Confidence:** HIGH **Essentiality:** supporting
- **Guidance Mode:** compact
  - "frame as EXTENDS + generalize, not supersede" *(bc staging's correctness core IS the prior fix)*
- **Depth-link:** none

### R6 — Consolidate memory: the file-per-X + caveat-audience lessons
- **engagement-type:** CONSOLIDATE *(epistemic · project-space)*
- **Move:** record two reusable SKILL-design lessons — (i) **"file-per-X is the over-engineering shape"** (runtime-provenance per-rule AND changelog per-change both over-engineer; lean form = entries in ONE doc); (ii) **"caveats/authoring-knowledge belong in authoring docs, not runtime instructions"** (a runtime self-undermining hedge licenses relaxation).
- **Lands:** the lessons persist across sessions for future SKILL-architecture work.
- **Touches:** the memory dir (a new SKILL-design-lessons memory or an extension of the verification-pass memory) · `MEMORY.md` index.
- **WHY:** both lessons generalize beyond this inquiry (any SKILL-authoring decision); the end goal's value outlives this inquiry only if consolidated.
- **Priority:** MED-HIGH **Confidence:** HIGH **Essentiality:** supporting
- **Guidance Mode:** compact
  - "these are SKILL-DESIGN lessons, distinct from the translation-QA memories" *(bc they are about how to structure a SKILL, not how to translate — may warrant their own memory)*
- **Depth-link:** none

### R7 — Branch: emitted-artifact enforcement (relax one-run)
- **engagement-type:** INVESTIGATE-FRONTIER *(teleological · project-space)*
- **Move:** investigate the deferred enforcement upgrade — require the meaning-lock stage to emit a real IR artifact (the forcing function), which within one-run is a stronger-but-still-soft raiser and only truly enforces if "one run" is relaxed.
- **Lands:** a decision on whether to climb the enforcement gradient (and pay the one-run-relaxation cost).
- **WHY:** the user's two ideas reunify here (middleware-as-forcing-artifact), but true enforcement needs relaxing the one-run constraint the user imposed; the end goal (enforced separation) gains a path — indirect, later-phase, gated on the user wanting enforcement over one-run.
- **Priority:** LOW-MED **Confidence:** LOW **Essentiality:** peripheral · @if-enforcement-wanted
- **Guidance Mode:** compact
  - "connects to prior `2026-07-11_00-24`'s deferred simulated-intermediate" *(bc the single-IR forcing function IS that deferred artifact)*
- **Depth-link:** none

### R8 — Branch: the gradient middle-rung empirical test
- **engagement-type:** TEST *(epistemic · project-space)*
- **Move:** test whether an emitted meaning-lock IR *within one pass* actually beats a prose stage-header *within one pass* at reducing the collapse failure — the gradient's unverified middle rung.
- **Lands:** evidence turning the gradient's middle rung from PLAUSIBLE to confirmed/refuted.
- **WHY:** the gradient is asserted; the end goal (knowing whether the artifact upgrade is worth it before R7) gains the empirical basis. Gated behind R4 (needs the staged baseline first).
- **Priority:** LOW **Confidence:** LOW **Essentiality:** peripheral
- **Guidance Mode:** compact
  - "run only after R4 gives the staged baseline" *(bc this is a delta-test against that baseline)*
- **Depth-link:** none

---

## Excluded

| Candidate | Reason rejected |
|---|---|
| **Runtime-provenance as a new per-rule artifact** | KILLED in critique — expensive + a self-report is not a verification; the "file-per-X" over-engineering shape. Engaging it advances nothing. |
| **"A middleware md file per change"** (the user's literal plural-files form) | KILLED/refined — same file-per-X over-engineering; the lean form is dated entries in ONE changelog (R2). |
| **Shipping the soft-border caveat in the runtime Step-5 text** | Rejected — self-undermining (tells the model the borders are optional → licenses relaxation). Relocated to authoring docs (R2), not a route of its own. |
| **The per-proposal OUTPUT-REACH decision** (evaluate-only / +design / +apply, per proposal) | A control-flow disposition — how far each proposal travels — which routelisting does not produce (NOT-list §1.3). Consumer's to make; gating reflected in R1/R2/R3 Essentiality qualifiers. |
| **Re-deriving the 3-Pass Step-5 wiring** | Belongs to prior `2026-07-11_00-24` — out of THIS inquiry's territory (this inquiry EXTENDS that fix, it does not re-derive it). |

---

## Telemetry
- **Mode:** root / project-space (breadth) · **Entry point:** fresh
- **Identities enumerated:** 8 routes · teleological 4 (R1, R2, R3, R7) · epistemic 4 (R4, R5, R6, R8)
- **High-priority:** 4 · **Essential-count:** core 2 (R1 phase-deferred, R2) · supporting 4 · peripheral 2
- **Individuations made:** 8 · **uncertain-individuations:** 0 (R1 apply-staging vs R2 adopt-changelog kept split — the two-proposal split; R3 spec vs R1 apply kept split — design-vs-apply reach) · **stale entries:** 0 (fresh)
- **Convergence:** reached — sweep of the 8 artifacts yielded no new identities on the second pass
- **Frontier flags:** none (territory small + bounded)
- **LAYER 1 checked:** no Over-merge (staging/changelog/spec/apply all split) · no Wrong-grain (identities, not manifestations) · no Goal-loss (every route biased to the evaluate+design goal) · no Type-misassignment (apply/adopt/spec = teleological; back-link/validate/consolidate/test = epistemic)
- **LAYER 2 checked:** no Selection-creep (no winner picked; the reach-fork Excluded as a disposition) · no Process-coupling (routes are concept-directions; the reach decision routed OUT) · no Description-collapse (routes are prescriptive) · no Manifestation-dump (compact identities)
- **Self-assessment verdict: PROCEED** — territory swept at identity resolution; no LAYER 1/2 flags; both files written.
